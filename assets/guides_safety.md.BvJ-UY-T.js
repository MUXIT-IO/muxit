import{_ as t,o as s,c as i,ag as a}from"./chunks/framework.DkuLC1cV.js";const k=JSON.parse('{"title":"Safety","description":"","frontmatter":{},"headers":[],"relativePath":"guides/safety.md","filePath":"guides/safety.md"}'),o={name:"guides/safety.md"};function n(r,e,l,d,h,c){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="safety" tabindex="-1">Safety <a class="header-anchor" href="#safety" aria-label="Permalink to &quot;Safety&quot;">​</a></h1><p>Muxit controls real hardware. Scripts, AI chats, and dashboard widgets all write voltages, move stages, and open valves. One typo on a number can ruin an expensive part.</p><p>The safety system adds graduated guard rails without forcing a confirmation prompt on every click. You pick <strong>one</strong> safety level; the level controls what happens for each kind of operation.</p><h2 id="licensing" tabindex="-1">Licensing <a class="header-anchor" href="#licensing" aria-label="Permalink to &quot;Licensing&quot;">​</a></h2><p>Each safety level is a separate license feature — the set you see in the picker is whatever the current tier unlocks, nothing is hardcoded to &quot;Free vs Pro&quot;. Server distributions ship with these defaults in <code>MuxitServer/Config/AppSettings.cs</code>:</p><table tabindex="0"><thead><tr><th>Feature id</th><th>Default tier</th></tr></thead><tbody><tr><td><code>safety.level.observe</code></td><td><code>free</code></td></tr><tr><td><code>safety.level.unrestricted</code></td><td><code>free</code></td></tr><tr><td><code>safety.level.assisted</code></td><td><code>pro</code></td></tr><tr><td><code>safety.level.active</code></td><td><code>pro</code></td></tr><tr><td><code>safety.level.custom</code></td><td><code>pro</code></td></tr><tr><td><code>safety.audit</code> (writes to <code>audit.jsonl</code>)</td><td><code>free</code></td></tr></tbody></table><p>Adding a new tier (Maker, Lab, Enterprise, …) is a matter of editing <code>AppSettings.Features</code> and <code>AppSettings.Tiers</code> — no changes to the safety subsystem. If Lab wants Observe + Active + Custom but not Unrestricted, drop its <code>RequiredTierId</code> on those three and tighten Unrestricted; the <code>SafetyGate</code> will pick it up via <code>CheckFeature</code> on the next request.</p><p>The chip is always visible. Levels the current tier can&#39;t select appear in the dialog with a locked radio and a <code>PRO</code> badge (or whatever the gating tier is called). The StatusStrip chip shows the live level on every tier. A workspace moved between tiers that persisted a now-locked level is clamped to <strong>Unrestricted</strong> on the next start — matching the pre-safety-gate behaviour so existing scripts keep working.</p><p>The audit trail is gated at three stops: the <code>safety.audit</code> license feature, the <code>safety.audit.enabled</code> config flag, and the active level&#39;s <code>Audit</code> column (Observe/Assisted/Active all default to <code>On</code>, Unrestricted produces no non-Allow outcomes so the file stays empty).</p><p>The AI confirmation column is <strong>not</strong> gated by any of this — it rides on the existing <code>ai</code> license entitlement.</p><h2 id="the-level-table" tabindex="-1">The level table <a class="header-anchor" href="#the-level-table" aria-label="Permalink to &quot;The level table&quot;">​</a></h2><p>Four built-in levels plus a user-editable <strong>Custom</strong> row. Each column tells the gate what to do with that kind of operation. The six outcomes are <strong>Allow</strong>, <strong>Simulate</strong> (log-only, hardware never sees the write), <strong>Ask once</strong> (cache the approval until the level changes), <strong>Ask every time</strong>, <strong>Typed confirm</strong> (type a release phrase), and <strong>Block</strong> (fail with <code>SAFETY_BLOCKED</code>).</p><table tabindex="0"><thead><tr><th>Level</th><th>Write (in range)</th><th>Write (out of range)</th><th>Large change (&gt;25%)</th><th>Action</th><th>Destructive action</th><th>AI confirmation</th><th style="text-align:center;">Audit</th></tr></thead><tbody><tr><td><strong>Observe</strong></td><td>Simulate</td><td>Block</td><td>Simulate</td><td>Simulate</td><td>Block</td><td>Ask every time</td><td style="text-align:center;">On</td></tr><tr><td><strong>Assisted</strong></td><td>Allow</td><td>Block</td><td>Ask once</td><td>Allow</td><td>Ask every time</td><td>Ask every time</td><td style="text-align:center;">On</td></tr><tr><td><strong>Active</strong></td><td>Allow</td><td>Block</td><td>Allow</td><td>Allow</td><td>Ask once</td><td>Ask every time</td><td style="text-align:center;">On</td></tr><tr><td><strong>Unrestricted</strong> ⚠</td><td>Allow</td><td>Allow</td><td>Allow</td><td>Allow</td><td>Allow</td><td>Ask every time</td><td style="text-align:center;">On</td></tr><tr><td><strong>Custom</strong></td><td><em>(user)</em></td><td><em>(user)</em></td><td><em>(user)</em></td><td><em>(user)</em></td><td><em>(user)</em></td><td><em>(user)</em></td><td style="text-align:center;"><em>(user)</em></td></tr></tbody></table><p><strong>Column definitions:</strong></p><ul><li><strong>Write (in range)</strong> — a property write whose value is inside the connector&#39;s declared <code>limits.min</code>/<code>limits.max</code>.</li><li><strong>Write (out of range)</strong> — a property write outside those hard limits. Only Unrestricted (or a sufficiently permissive Custom row) can let these through.</li><li><strong>Large change</strong> — a write whose delta exceeds <code>limits.largeChangeFraction</code> of the declared range (default 25%). Catches &quot;fat finger&quot; typos where the user meant 30 and typed 300.</li><li><strong>Action</strong> — a non-destructive method call.</li><li><strong>Destructive action</strong> — a method call the connector flagged via <code>confirm: { reset: &quot;always&quot; }</code> or <code>&quot;typed&quot;</code>.</li><li><strong>AI confirmation</strong> — whether the AI must restate numeric writes (with units) and summarize <code>write_script</code>/<code>run_code</code> calls before they execute. <code>Ask every time</code> on every built-in level; only <strong>Custom</strong> may set it to <code>Allow</code>.</li><li><strong>Audit</strong> — whether blocked / simulated / confirmed operations are written to <code>workspace/logs/audit.jsonl</code>.</li></ul><p><strong>Level escalation:</strong> switching levels applies on click. <strong>Unrestricted</strong> additionally requires typing <code>I UNDERSTAND</code> in the dialog — the typed phrase is the guard-rail; once granted, the level stays active until the user picks a different one. <strong>Custom</strong> is treated as equivalent risk to Assisted for escalation purposes, but the dialog flags the case where any Custom column is more permissive than Assisted&#39;s.</p><p>Open the chip in the status strip to change the level. The change propagates to every connected dashboard within one tick.</p><h2 id="custom" tabindex="-1">Custom <a class="header-anchor" href="#custom" aria-label="Permalink to &quot;Custom&quot;">​</a></h2><p>Pick <strong>Custom</strong> in the dialog to set each column individually. The selected values persist to <code>workspace/config/server.json</code> under <code>safety.custom</code>:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;safety&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;level&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Custom&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;lastChange&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;custom&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;writeInRange&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:      </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Allow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;writeOutOfRange&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Block&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;largeChange&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:       </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;AskOnce&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;action&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:            </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Allow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;destructiveAction&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;AskOnce&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;aiConfirm&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:         </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;AskEveryTime&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;audit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:             </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;on&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Missing keys fall back to Assisted&#39;s row, so a minimal Custom block still works.</p><h2 id="per-connector-safety" tabindex="-1">Per-connector safety <a class="header-anchor" href="#per-connector-safety" aria-label="Permalink to &quot;Per-connector safety&quot;">​</a></h2><p>A connector&#39;s <code>.js</code> config can carry an optional <code>safety</code> block:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  driver: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Scpi&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  safety: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    limits: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      voltage: { min: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, max: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, step: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.01</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, largeChangeFraction: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      current: { min: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, max: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    confirm: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      voltage: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;onLargeChange&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// once | always | never | typed | onLargeChange</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      reset: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;always&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// flags the action destructive</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      shutdown: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;typed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// flags destructive + promotes to typed confirm</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    rate: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      voltage: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// max 10 writes/sec</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      reset: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// max 1 per 10s</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    interlock: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      output_on: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;safety_relay.state==true&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    safeState: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;setSafeDefaults&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    overrideLevel: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Active&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// pin this connector to a specific level vs global</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong><code>limits</code></strong> — hard <code>min</code>/<code>max</code>. The level table decides what to do with out-of-range writes; <code>largeChangeFraction</code> (default 0.25) controls what counts as a &quot;large&quot; delta.</p><p><strong><code>confirm</code></strong> — per-item override of the default outcome. <code>once</code> caches the first confirmation per level; <code>always</code> both flags the action destructive <strong>and</strong> asks every time; <code>never</code> skips the prompt even when the level asks for one; <code>typed</code> marks destructive and promotes the confirm to the typed-phrase flow; <code>onLargeChange</code> falls through to the large-change column.</p><p><strong><code>rate</code></strong> — simple ops-per-second cap per item name. Exceeding it blocks with <code>SAFETY_BLOCKED</code>.</p><p><strong><code>interlock</code></strong> — simple form <code>&quot;&lt;connector&gt;.&lt;property&gt;==&lt;value&gt;&quot;</code>. Interlock false blocks the op.</p><p><strong><code>safeState</code></strong> — method to call for emergency safe-state. Stored but not auto-invoked in Phase 1.</p><p><strong><code>overrideLevel</code></strong> — bidirectional pin. <code>&quot;Active&quot;</code> keeps this connector at Active even during a global Observe session (useful for cheap-to-simulate devices you still want &quot;real&quot; in test envs). <code>&quot;Assisted&quot;</code> keeps it gated during a global Unrestricted (protect an expensive connector from a globally permissive session). Accepts <code>Observe</code>, <code>Assisted</code>, <code>Active</code>, or <code>Unrestricted</code>; Custom is not accepted here.</p><h2 id="per-driver-opt-out" tabindex="-1">Per-driver opt-out <a class="header-anchor" href="#per-driver-opt-out" aria-label="Permalink to &quot;Per-driver opt-out&quot;">​</a></h2><p>Some drivers have no path to physical hardware and no destructive actions — a webcam, a file-access driver, an IP camera. Forcing them through the safety gate is friction for no benefit. A driver author can opt out by declaring <code>requiresSafetyGates: false</code> on the driver itself:</p><ul><li><strong>C# DLL drivers:</strong> <code>[assembly: RequiresSafetyGates(false)]</code> in the driver project. The assembly attribute wins over the interface default.</li><li><strong>JS drivers:</strong> <code>meta: { requiresSafetyGates: false, ... }</code> in the <code>.driver.js</code>.</li><li><strong>Built-in drivers:</strong> implement <code>bool RequiresSafetyGates =&gt; false;</code> on the <code>IConnectorDriver</code> class.</li></ul><p>When the flag is <code>false</code>, connectors of that driver never go through the safety gate: no limit checks, no confirmations, no rate caps, no simulate/block logic, and <strong>no audit rows</strong>. Default is <code>true</code> — every existing driver remains gated.</p><p>This is orthogonal to the per-connector <code>safety.overrideLevel</code>. <code>overrideLevel</code> pins a <em>gated</em> connector to a specific level at runtime; <code>requiresSafetyGates: false</code> is a driver-author declaration that the gate doesn&#39;t apply at all. A connector whose driver has <code>requiresSafetyGates: false</code> ignores <code>overrideLevel</code> too — the gate never runs.</p><p>Built-in drivers that ship with the flag off:</p><ul><li><code>Webcam</code> — frame capture only, no hardware writes.</li><li><code>FileAccess</code> — workspace-scoped file I/O, already guarded by workspace state.</li><li><code>Onvif</code> — IP-camera PTZ/streaming, non-destructive and recoverable.</li></ul><p>Built-in drivers that stay gated (default):</p><ul><li><code>TestDevice</code> — kept gated on purpose, so it serves as the easiest example for exploring the safety system (limits, confirms, simulate, audit).</li><li><code>MqttBridge</code> — publishes to topics that may drive downstream IoT devices.</li></ul><p>Use the flag sparingly. If there&#39;s any way a driver call could affect the physical world, keep it on.</p><h2 id="audit-log" tabindex="-1">Audit log <a class="header-anchor" href="#audit-log" aria-label="Permalink to &quot;Audit log&quot;">​</a></h2><p>Every blocked, simulated, or confirmed operation is appended to <code>workspace/logs/audit.jsonl</code> as a flat JSON line:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ts&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-04-22T14:05:11.123Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;event&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;write.blocked&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;caller&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ai:sess-9c2f&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;details&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;connector&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;psu&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;item&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;voltage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">99</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;rule&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;limit.outOfRange&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}}</span></span></code></pre></div><p>Events: <code>level.change</code>, <code>write.blocked</code>, <code>action.blocked</code>, <code>write.simulated</code>, <code>action.simulated</code>, <code>confirm.granted</code>, <code>confirm.denied</code>. Readers tolerate unknown event names so a future phase can log every allowed write without migrating the file.</p><p>Callers appear as <code>script:&lt;name&gt;</code>, <code>ai:&lt;sessionId&gt;</code>, <code>dashboard:&lt;clientId&gt;</code>, <code>agent:&lt;name&gt;</code>, <code>mcp:&lt;id&gt;</code>, or <code>system:&lt;subsystem&gt;</code>.</p><p>Rotation: size-based, default 10 MB (configurable via <code>safety.audit.maxSizeMb</code>). Flip <code>safety.audit.rotateDaily: true</code> for nightly UTC rotation instead.</p><p>Query the last N records over WebSocket with <code>safety.audit.tail { &quot;n&quot;: 200 }</code>.</p><h2 id="ai-confirmation" tabindex="-1">AI confirmation <a class="header-anchor" href="#ai-confirmation" aria-label="Permalink to &quot;AI confirmation&quot;">​</a></h2><p>The AI Chat Panel&#39;s tool-call prompts are driven by the <strong>AI confirmation</strong> column in the safety matrix. Every built-in level ships with <code>Ask every time</code>; only <strong>Custom</strong> can set the column to <code>Allow</code>. Switching levels doesn&#39;t turn AI confirmation off — the guard is universal because voice input makes unit misinterpretation a first-class risk (&quot;15&quot; → 15 V or 15 mV?).</p><p>When the column is on, two classes of tool call are parked for plain-text confirmation:</p><ol><li><strong>Numeric writes and actions</strong> — <code>write_property</code> and <code>call_action</code> whose arguments contain any numeric value. The AI must restate every number with its unit (&quot;setting voltage to 15 V, current limit to 0.35 A — confirm?&quot;) and wait for &quot;yes&quot; before retrying with identical arguments. Numbers the user just typed or spoke are echoed back too, because voice recognition often drops the unit.</li><li><strong>Script authoring and inline code</strong> — <code>write_script</code> and <code>run_code</code>. The AI must summarize what the script will do in plain English (connectors, ranges, step sizes, expected duration) and flag any missing details before it executes. <code>run_script</code> on an already-saved script is <em>not</em> gated — the authoring step already was.</li></ol><p><strong>Simple actions pass through unchanged.</strong> A parameterless or boolean-only call like <code>power_on</code>, <code>reset</code>, or <code>set_output(true)</code> carries no numbers and isn&#39;t a script tool, so the AI executes it without a confirmation round-trip.</p><p>The confirmation is one-shot and keyed on the exact argument JSON. Approval releases that one call; if the user changes any value, the gate re-arms for the new request. When the user declines, the AI drops the operation and continues the conversation normally — no tool runs.</p><p>Turn the column off for an unattended workflow: pick <strong>Custom</strong> in the SafetyChip dialog, set <code>AI confirmation</code> to <code>Allow</code>, and apply. The behaviour persists under <code>safety.custom.aiConfirm</code> in <code>server.json</code>.</p><h3 id="upgrading-from-earlier-workspaces" tabindex="-1">Upgrading from earlier workspaces <a class="header-anchor" href="#upgrading-from-earlier-workspaces" aria-label="Permalink to &quot;Upgrading from earlier workspaces&quot;">​</a></h3><p>Older safety configs had no <code>aiConfirm</code> field. On first read they&#39;re treated as <code>AskEveryTime</code> — the new safe default. The legacy <code>confirm_unknown_values</code> / <code>confirm_all</code> / <code>off</code> modes and their per-level mapping are gone; there is no separate <code>ai.safetyMode</code> to remove.</p><h2 id="websocket-surface" tabindex="-1">WebSocket surface <a class="header-anchor" href="#websocket-surface" aria-label="Permalink to &quot;WebSocket surface&quot;">​</a></h2><ul><li><code>safety.get</code> — returns current snapshot: <code>level</code>, <code>allowedLevels</code>, <code>custom?</code>, <code>lastChangeCaller</code>, <code>lastChangeAt</code>, <code>licensed</code>.</li><li><code>safety.setLevel { target, typedPhrase?, custom? }</code> — request level change. Escalations prompt via <code>PromptManager</code>; Unrestricted requires <code>typedPhrase</code>; Custom persists the <code>custom</code> block to <code>server.json</code>.</li><li><code>safety.audit.tail { n }</code> — last N audit records.</li><li>Broadcast <code>safety.changed</code> — every connected dashboard updates its chip within a tick.</li><li>Broadcast <code>audit.log</code> — raw audit record fan-out for future UI viewers.</li></ul><h2 id="what-s-intentionally-out-of-scope-in-phase-1" tabindex="-1">What&#39;s intentionally out of scope in Phase 1 <a class="header-anchor" href="#what-s-intentionally-out-of-scope-in-phase-1" aria-label="Permalink to &quot;What&#39;s intentionally out of scope in Phase 1&quot;">​</a></h2><ul><li><code>safeState</code> is stored but not auto-invoked. Triggering it on emergency comes with the next phase.</li><li>Interlock parsing supports only the <code>&quot;conn.prop==value&quot;</code> form.</li><li>The audit log captures level changes, blocked/simulated writes, and confirmations. A future phase can add <code>&quot;write&quot;</code> events for every allowed write without schema migration.</li><li>Agent-specific limits in <code>AgentSafetyGate</code> are untouched — they run in parallel with the new gate, not merged.</li></ul>`,60)])])}const u=t(o,[["render",n]]);export{k as __pageData,u as default};

import{_ as o,o as t,c as a,ag as r}from"./chunks/framework.DkuLC1cV.js";const p=JSON.parse('{"title":"Revision History","description":"","frontmatter":{},"headers":[],"relativePath":"reference/revision-history.md","filePath":"reference/revision-history.md"}'),i={name:"reference/revision-history.md"};function s(n,e,d,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[r(`<h1 id="revision-history" tabindex="-1">Revision History <a class="header-anchor" href="#revision-history" aria-label="Permalink to &quot;Revision History&quot;">​</a></h1><p>All notable changes to Muxit are documented here, grouped by version.</p><h2 id="v0-31-0-—-may-8-2026" tabindex="-1">v0.31.0 — May 8, 2026 <a class="header-anchor" href="#v0-31-0-—-may-8-2026" aria-label="Permalink to &quot;v0.31.0 — May 8, 2026&quot;">​</a></h2><h3 id="features" tabindex="-1">Features <a class="header-anchor" href="#features" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong><code>node build.js publish</code> now produces both linux-x64 and win-x64 archives by default on either host OS.</strong> Archive creation moved from platform-specific shell-outs (<code>tar</code> / <code>Compress-Archive</code> / <code>zip</code>) to a single <code>archiver</code>-based helper that explicitly sets the <code>+x</code> bit on <code>muxit</code>, <code>createdump</code>, and <code>*.so*</code> files in the linux tarball — so cross-host builds (e.g. linux-x64 from a Windows dev box) now ship the executable bit correctly. Override with <code>MUXIT_PUBLISH_RID</code> if you only want one platform.</li></ul><h3 id="bug-fixes" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Dev console can now save manifests for Python drivers.</strong> Saving a Python driver&#39;s manifest from the Drivers tab returned <code>unknown driver: python/&lt;name&gt;</code> because the dev console&#39;s driver resolver only listed C# and JS sources; Python drivers are now included so save / build / registry-entry / publish endpoints all resolve them.</li><li><strong>Dev console Release tab: Build release archive no longer makes the log box vanish.</strong> The release panel was repainting 500ms after the build started, which destroyed the log element while the build was still streaming — looking like the operation crashed silently. The refresh now waits for the build&#39;s exit event. Help text clarified to reflect that the local build targets the host OS only (cross-platform matrix runs on tag push).</li></ul><h2 id="v0-30-0-—-may-8-2026" tabindex="-1">v0.30.0 — May 8, 2026 <a class="header-anchor" href="#v0-30-0-—-may-8-2026" aria-label="Permalink to &quot;v0.30.0 — May 8, 2026&quot;">​</a></h2><h3 id="features-1" tabindex="-1">Features <a class="header-anchor" href="#features-1" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><p><strong>Linux now has a one-line installer and a runtime dependency check.</strong></p><p>End-users on Ubuntu / Debian can install with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fsSL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://raw.githubusercontent.com/muxit-io/muxit/main/install.sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> bash</span></span></code></pre></div><p>The installer (<code>installer/install.sh</code>):</p><ul><li>Detects the distro, runs <code>sudo apt install ffmpeg libayatana-appindicator3-1 xdg-utils</code> with confirmation.</li><li>Adds the user to the <code>dialout</code> group if needed (serial-port access).</li><li>Downloads the latest <code>muxit-linux-x64-v*.tar.gz</code> from GitHub Releases and verifies the SHA-256 against <code>checksums.txt</code>.</li><li>Extracts into <code>~/.local/share/muxit/</code> (per-user, no sudo for the install itself; <code>workspace/</code> is preserved across reinstalls).</li><li>Symlinks <code>~/.local/bin/muxit</code> so plain <code>muxit</code> works on <code>$PATH</code>.</li></ul><p>Re-running the same command upgrades to the latest release in place.</p><p>Independently, the server itself now probes its runtime dependencies at startup on Linux and prints a non-fatal <strong>System dependencies</strong> section in the boot banner:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>── System dependencies ─────────────────</span></span>
<span class="line"><span>  ✓ ffmpeg (Onvif IP-camera RTSP streaming)</span></span>
<span class="line"><span>  ⚠ libayatana-appindicator3-1 missing — system tray icon unavailable.</span></span>
<span class="line"><span>    Fix: sudo apt install libayatana-appindicator3-1</span></span>
<span class="line"><span>  ✓ xdg-open (auto-open browser at startup)</span></span>
<span class="line"><span>  ⚠ user &#39;matthijs&#39; is NOT in &#39;dialout&#39; group — serial drivers will</span></span>
<span class="line"><span>    fail with EACCES on /dev/ttyUSB*.</span></span>
<span class="line"><span>    Fix: sudo usermod -aG dialout matthijs &amp;&amp; newgrp dialout</span></span></code></pre></div><p>So users who skip the installer (manual tarball + extract) still get told exactly what&#39;s missing and how to fix it.</p><p>New install guide at <code>/docs/guides/install-linux</code> covers the one-liner, manual install, all four supported package managers (apt / dnf / pacman / zypper), and uninstall. macOS, dnf-native support, .desktop launcher, udev rules and systemd-user service are deliberately scoped out for v1 — see <code>docs/follow-up-linux-installer.md</code> in the dev repo for the punch list.</p></li><li><p><strong>Linux x64 release archives are now published alongside the Windows build.</strong> The release tag (<code>v*</code>) workflow now runs on a matrix (<code>windows-latest</code> + <code>ubuntu-latest</code>) and uploads two assets per release: <code>muxit-win-x64-v&lt;version&gt;.zip</code> and <code>muxit-linux-x64-v&lt;version&gt;.tar.gz</code>. The Linux tarball preserves the executable bit on the <code>muxit</code> binary because it is created on the Linux runner.</p><p><code>build.js publish</code> now defaults to the host platform&#39;s RID and accepts an <code>MUXIT_PUBLISH_RID</code> env override so a single dev machine still produces a relevant archive. Packaging is <code>.tar.gz</code> for <code>linux-*</code> / <code>osx-*</code> RIDs and <code>.zip</code> for <code>win-*</code>.</p><h3 id="linux-runtime-requirements" tabindex="-1">Linux runtime requirements <a class="header-anchor" href="#linux-runtime-requirements" aria-label="Permalink to &quot;Linux runtime requirements&quot;">​</a></h3><p>shimat&#39;s manylinux build of <code>libOpenCvSharpExtern.so</code> ships a static FFmpeg without network protocols (no <code>rtsp://</code>, no <code>http://</code>) and a GStreamer backend that registers but is non-functional. Both <code>VideoCapture(rtspUri, FFMPEG)</code> and <code>VideoCapture(pipeline, GSTREAMER)</code> return <code>IsOpened()=false</code> on Linux for any RTSP URL. Verified with the diagnostic at <code>tools/rtsp-probe/run.sh</code> — five different in-process approaches all fail.</p><p>The Onvif driver therefore delegates RTSP capture on Linux/macOS to the <strong>system <code>ffmpeg</code> binary</strong>: spawn <code>ffmpeg -i rtsp://… -f mjpeg pipe:1</code>, parse JPEGs from stdout, decode each one with <code>Cv2.ImDecode</code> (which works fine in the same wrapper). On Windows the in-process <code>VideoCapture(rtspUri, FFMPEG)</code> path is unchanged — there shimat&#39;s runtime ships a full FFmpeg with RTSP support.</p><p>For Onvif on Linux you therefore need ffmpeg on <code>$PATH</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ffmpeg</span></span></code></pre></div><p>That&#39;s the whole story — no GStreamer packages required. TestDevice&#39;s synthetic video stream and any future Webcam / Vision driver work without ffmpeg (they use the in-process OpenCV pipeline directly, which is fine because they don&#39;t open network streams).</p><p>Optional:</p><ul><li><code>xdg-utils</code> — lets the server auto-open the browser on startup. Without it, the server prints the URL and you open it yourself.</li><li><code>libayatana-appindicator3-1</code> — system-tray icon. Without it, the server starts normally but with no tray.</li></ul><p>The server itself (web UI, scripts, non-video drivers) runs without any of the above.</p><p>Vendor-SDK drivers (Avantes, Fairino) remain Windows-only and are not exercised on the Linux build leg of CI.</p></li></ul><h2 id="v0-29-0-—-may-8-2026" tabindex="-1">v0.29.0 — May 8, 2026 <a class="header-anchor" href="#v0-29-0-—-may-8-2026" aria-label="Permalink to &quot;v0.29.0 — May 8, 2026&quot;">​</a></h2><h3 id="features-2" tabindex="-1">Features <a class="header-anchor" href="#features-2" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Welcome screen now shows system info instead of a tier comparison table.</strong> The Free vs Pro feature table is gone — the home page still shows your current license tier badge, plus a new System panel with the server port, hostname (e.g. <code>muxit.local</code>), Bonjour/mDNS state, remote-access state, the active AI provider, and (for Muxit AI) credit usage.</li></ul><h2 id="v0-28-0-—-may-8-2026" tabindex="-1">v0.28.0 — May 8, 2026 <a class="header-anchor" href="#v0-28-0-—-may-8-2026" aria-label="Permalink to &quot;v0.28.0 — May 8, 2026&quot;">​</a></h2><h3 id="features-3" tabindex="-1">Features <a class="header-anchor" href="#features-3" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><p><strong>System tray now has a &quot;View Logs&quot; entry that opens a live log viewer.</strong> The viewer is served at <code>/logs.html</code> and streams entries from the in-memory ringbuffer over Server-Sent Events with level + substring filters, pause and download. <code>Console.WriteLine</code> / <code>Console.Error.WriteLine</code> calls are now teed into <code>ServerLogger</code>, so startup banners are visible too. Each instance has its own port, so multiple muxit instances on the same machine each surface their own logs without cross-talk.</p><p>Two on-disk files are kept per workspace under <code>%LOCALAPPDATA%\\Muxit\\logs\\&lt;hash&gt;\\</code> (Windows), <code>~/Library/Application Support/Muxit/logs/&lt;hash&gt;/</code> (macOS), or <code>~/.local/share/Muxit/logs/&lt;hash&gt;/</code> (Linux):</p><ul><li><code>startup.log</code> — overwritten on every clean exit, so a screenshot is always available even if the HTTP server is down.</li><li><code>last-crash.log</code> — written from the unhandled-exception handler. When present, the tray gains a one-shot &quot;Open Last Crash Log&quot; entry that opens it in the OS default text editor.</li></ul><p>Nothing is ever appended — exactly two files per workspace, both overwritten in place.</p></li></ul><h2 id="v0-27-2-—-may-7-2026" tabindex="-1">v0.27.2 — May 7, 2026 <a class="header-anchor" href="#v0-27-2-—-may-7-2026" aria-label="Permalink to &quot;v0.27.2 — May 7, 2026&quot;">​</a></h2><h3 id="bug-fixes-1" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-1" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Restart Server and Switch Workspace now work in the published muxit executable.</strong> Previously these actions wrote a marker file and stopped the server, relying on the dev-mode Node launcher (<code>start.js</code>) to respawn the process — which left users running <code>muxit.exe</code> with a stopped server and no automatic restart. The server now respawns itself when no parent launcher is present.</li></ul><h2 id="v0-27-1-—-may-7-2026" tabindex="-1">v0.27.1 — May 7, 2026 <a class="header-anchor" href="#v0-27-1-—-may-7-2026" aria-label="Permalink to &quot;v0.27.1 — May 7, 2026&quot;">​</a></h2><h3 id="bug-fixes-2" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-2" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>AI assistant no longer asks for confirmation twice on numeric writes.</strong> The system prompt now tells the model to call the tool first and let the safety gate prompt the user, instead of pre-confirming in chat and then triggering the gate&#39;s prompt on top.</li></ul><h2 id="v0-27-0-—-may-7-2026" tabindex="-1">v0.27.0 — May 7, 2026 <a class="header-anchor" href="#v0-27-0-—-may-7-2026" aria-label="Permalink to &quot;v0.27.0 — May 7, 2026&quot;">​</a></h2><h3 id="features-4" tabindex="-1">Features <a class="header-anchor" href="#features-4" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Windows release archives are now versioned (<code>muxit-win-x64-v&lt;version&gt;.zip</code>).</strong> The release zip includes the version in its filename so downloads from the GitHub Releases page are self-identifying. The auto-updater (<code>SelfUpdater</code>/<code>UpdateChecker</code>) and the PowerShell installer match the asset by <code>muxit-win-x64*.zip</code> prefix, so existing installs continue to find the latest release without changes.</li></ul><h2 id="v0-26-0-—-may-7-2026" tabindex="-1">v0.26.0 — May 7, 2026 <a class="header-anchor" href="#v0-26-0-—-may-7-2026" aria-label="Permalink to &quot;v0.26.0 — May 7, 2026&quot;">​</a></h2><h3 id="features-5" tabindex="-1">Features <a class="header-anchor" href="#features-5" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Imprint, Privacy Policy, and Terms &amp; License Agreement are now published at muxit.io/legal/imprint, /legal/privacy, and /legal/terms.</strong> The site footer carries persistent links to all three. The in-app workspace acknowledgement bumps to v1.1.0 (forces re-acceptance) and now references the full Terms and Privacy Policy directly. The privacy policy describes the licence machine-fingerprint precisely (machine name, OS user name, up-to-three sorted MAC addresses, and an OS-installation identifier — all hashed locally before transmission), clarifies that MCP-with-your-own-AI-key is available on every tier including Free, and removes the analytics paragraph: muxit.io currently runs no website analytics at all. The references to the EU Online Dispute Resolution platform have been removed from both the imprint and the terms because the European Commission discontinued the platform on 20 July 2025.</li><li><strong>Multi-instance support: one running muxit per workspace, with a tier cap.</strong> The same workspace can no longer be opened twice — the second instance exits with the holder&#39;s PID — so concurrent muxit processes always own distinct workspaces. The new <code>concurrent-instances</code> license feature caps how many workspaces a user can run at once on the same machine (Free 1, Maker 2, Pro 5, Lab/Enterprise unlimited); the cap is enforced via a per-process lock file on Windows (<code>%LOCALAPPDATA%\\Muxit\\locks\\Instance-N.lock</code>) and a Unix-domain socket on Linux/macOS, both of which the OS releases on process death regardless of which thread originally acquired them. Licences are now stored per-machine (under <code>~/.muxit/</code> or <code>%LOCALAPPDATA%\\Muxit\\</code>), so a single activation covers every workspace on the same computer; on first launch the migration scans every catalogued workspace for a <code>.license</code> that actually contains a key (paired with its signed <code>.license-proof</code>) and copies that one up. Launching <code>muxit</code> without <code>--workspace</code> when more than one workspace is registered opens a browser-based workspace picker. If the cap is already reached, the picker shows a tier-aware banner up front (<code>Multi-instance limit reached. Your &lt;Tier&gt; tier allows up to N concurrent muxit instances…</code>) and disables every workspace button instead of redirecting to a dead origin. Port collisions auto-bump to the next free port for the current run only — the workspace&#39;s <code>server.json</code> is no longer rewritten, so a solo relaunch returns to the configured port. Deactivating a licence now sticks across restarts: the server-signed proof is deleted alongside the local <code>.license</code>, and the <code>Deactivate</code> button is visible whenever any non-free tier is active even if the cached key is missing. The active workspace name is shown in the tray menu (<code>Muxit — &lt;name&gt; (port N)</code>) and next to the wordmark in the GUI titlebar.</li><li><strong>Python connector support is now front-and-centre in the docs and on the website.</strong> The marketing site&#39;s &quot;How it works&quot; page lists Python as a fourth driver format alongside SCPI / JS / DLL, and the makers and professionals pages pitch the &quot;drop a <code>.py</code> file in <code>workspace/python/</code> and a connector calls its functions&quot; path. In the docs, getting-started gains a new &quot;I already have a Python script that talks to my device&quot; path, system-requirements documents the optional Python 3.10+ dependency, and a new <a href="/muxit/guides/python-connectors.html">Python Connectors guide</a> covers the generic <code>Python</code> driver — properties, <code>init</code> / <code>shutdown</code>, per-script venvs, common pitfalls — without making readers pick through the full SDK reference.</li><li><strong>AI-assisted SCPI connector authoring is now its own license feature.</strong> Added <code>scpi-authoring</code> (defaults to Maker tier, matching prior behavior) so the authoring workflow — <code>probe_scpi_device</code>, <code>validate_connector_config</code>, <code>search_manual</code>, <code>read_manual_pages</code>, and the rest of the SCPI authoring tool set — can be moved between tiers in <code>AppSettings.cs</code> without touching the rest of the chat surface. The intent classifier now skips the SCPI authoring group entirely when the feature is blocked, so blocked tiers no longer pay for the SCPI system-prompt fragment or the OpenRouter web-search plugin on irrelevant turns.</li><li><strong>Window title now shows the active workspace.</strong> The browser/Electron tab title and the server&#39;s console window (Windows taskbar tooltip) both read <code>muxit-io [WorkspaceName]</code>, making it easy to tell multiple muxit.io instances apart at a glance.</li></ul><h3 id="bug-fixes-3" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-3" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Pricing tiers now match what&#39;s actually shipped.</strong> Pro-tier <code>concurrent-instances</code> was capped at 5 in <code>AppSettings</code>, but the pricing page promises unlimited — bumped Pro to <code>-1</code> so the cap matches the marketing. Autonomous agents are now flagged <em>(in development)</em> on the pricing card and comparison table since the <code>agents</code> feature is still <code>Disabled = true</code> in code. Added Startup scripts and Video recording rows to the pricing card and comparison table so the silent Maker-tier gates on those features are visible to users.</li><li><strong>Pricing and AI docs no longer falsely claim <code>ai(prompt, image)</code> requires Pro.</strong> The script-side <code>ai()</code> global only checks the <code>&quot;ai&quot;</code> license feature (Maker tier), so passing a base64 JPEG to it from a script works on Maker — that was already true in the code, but the docs and pricing card said otherwise. The Pro-tier &quot;Vision AI&quot; benefit is reframed around what&#39;s actually gated: the agentic vision <strong>tools</strong> in the Chat Panel and MCP (<code>take_snapshot</code>, <code>identify_objects</code>, <code>locate_object</code>, OpenCV trackers, spatial mapping). Maker = explicit <code>ai(prompt, image)</code> calls from your scripts; Pro = the AI assistant decides when to look.</li><li><strong>Security: close N1–N4 from the 2026-05-07 follow-up audit.</strong> License-proof verification now binds the signed envelope to the local machine fingerprint — copying a paid user&#39;s <code>.license-proof</code> to another machine no longer elevates that machine&#39;s tier (audit N1). JS and Python drivers can no longer opt out of safety gates: <code>meta.requiresSafetyGates: false</code> (JS) and <code>META[&quot;requiresSafetyGates&quot;] = False</code> (Python) are ignored with a stderr warning, since neither tier has a per-driver signing path that would prevent a hand-dropped driver from bypassing every limit, confirm and audit row (audit N2). The <code>MUXIT_PROXY_BASE_URL</code> and <code>MUXIT_DISABLE_INSTANCE_LIMIT</code> environment-variable bypasses are now gated on <code>MUXIT_DEV=1</code>, so production binaries silently ignore them — without that gate, an end user could redirect license-flow calls to a dead URL (defeating the B2 trial ledger) or bypass the per-tier concurrent-instances cap by setting one variable (audits N3, N4). Built-in drivers that opt out via the C# class override (Webcam, FileAccess, Onvif, MqttBridge) and DLL drivers that opt out via a signed assembly attribute are unaffected — those paths continue to honour declared opt-outs.</li></ul><h2 id="v0-25-47-—-may-5-2026" tabindex="-1">v0.25.47 — May 5, 2026 <a class="header-anchor" href="#v0-25-47-—-may-5-2026" aria-label="Permalink to &quot;v0.25.47 — May 5, 2026&quot;">​</a></h2><h3 id="cleanup-—-drop-legacy-hmac-license-cache-reader" tabindex="-1">Cleanup — drop legacy HMAC license-cache reader <a class="header-anchor" href="#cleanup-—-drop-legacy-hmac-license-cache-reader" aria-label="Permalink to &quot;Cleanup — drop legacy HMAC license-cache reader&quot;">​</a></h3><p>The one-release migration helper from v0.25.46 has done its job. Removed <code>MuxitServer/Licensing/LegacyHmacReader.cs</code> and the legacy-format detection branch in <code>LicenseManager.Cache.cs:LoadFromDisk</code> — the cache loader is now a plain JSON read. Anyone still on a build older than v0.25.46 who upgrades through this version will see &quot;license invalidated, please re-activate&quot; on first launch and need to paste their key once. Acceptable since the public release happens after this version.</p><h2 id="v0-25-46-—-may-5-2026" tabindex="-1">v0.25.46 — May 5, 2026 <a class="header-anchor" href="#v0-25-46-—-may-5-2026" aria-label="Permalink to &quot;v0.25.46 — May 5, 2026&quot;">​</a></h2><h3 id="security-—-server-signed-license-cache-audit-b1-b2" tabindex="-1">Security — server-signed license cache (audit B1 + B2) <a class="header-anchor" href="#security-—-server-signed-license-cache-audit-b1-b2" aria-label="Permalink to &quot;Security — server-signed license cache (audit B1 + B2)&quot;">​</a></h3><p>The local <code>.license</code> cache used to be HMAC-signed with a key derived from a hidden salt file plus <code>MachineName</code>/<code>UserName</code> — every input the user could read, so anyone with workspace access could forge <code>TierId=&quot;lab&quot;</code> with full entitlements (audit B1). Trials lived in the same file, so deleting it granted a fresh trial (audit B2).</p><ul><li><strong>Tier is now gated by a server-signed proof.</strong> A new <code>.license-proof</code> file stores an RSA-SHA256 envelope returned by <code>POST https://api.muxit.io/v1/license/sign</code>. On startup the proof is verified against an embedded RSA-4096 public key (<code>MuxitServer/Licensing/LicensePubKey.cs</code>) and, if its <code>valid_until</code> is still in the future, its <code>tier</code> overrides the local <code>.license</code>. Missing / tampered / expired → user is demoted to <code>free</code> until the next online re-validation.</li><li><strong><code>.license</code> is now plaintext JSON.</strong> It still holds local bookkeeping (<code>StartupCount</code>, <code>LastSeen</code>, <code>FingerprintComponents</code>, customer info, <code>BaseLicenseKey</code> and <code>BaseInstanceId</code> for re-validation), but editing it can no longer escalate tier — the proof is the source of truth.</li><li><strong>Trials moved to a server-side ledger.</strong> <code>LicenseManager.StartTrial</code> now calls <code>POST /v1/license/trial-start</code> with the machine fingerprint; the proxy holds an idempotent <code>(fingerprint, feature_id)</code> record. Deleting the workspace no longer grants a fresh trial. <code>StartTrial</code> is async now (<code>Task&lt;bool&gt;</code>).</li><li><strong>Soft cutover for existing installs.</strong> A read-only <code>LegacyHmacReader</code> still loads the old HMAC cache once on startup so cohort-1 customers don&#39;t see a &quot;license invalidated&quot; prompt; the next online re-validation upgrades them to the new format and deletes the legacy <code>.license-salt</code> file. Lifespan: one release.</li><li><strong>New keypair, separate from driver signing.</strong> Generated by <code>tools/generate-license-signing-key.csx</code> (PKCS#8 PEM, not .NET XML — Web Crypto in the worker imports PEM natively). Private half lives only in the Cloudflare Worker secret store; same key is never used to sign DLLs.</li><li><strong><code>MUXIT_PROXY_BASE_URL</code> env-var override</strong> added to <code>SignedLicenseClient</code> and <code>TrialClient</code> for staging / local-wrangler-dev testing without touching source.</li><li>Companion changes ship in <strong>MUXITPROXY</strong> (signing endpoint, trial-start endpoint, <code>trial_starts</code> D1 table, <code>LICENSE_SIGNING_PRIVATE_KEY</code> worker secret).</li><li>Crypto roundtrip can be verified offline via <code>dotnet script tools/verify-license-signing.csx</code>.</li></ul><h2 id="v0-25-45-—-may-4-2026" tabindex="-1">v0.25.45 — May 4, 2026 <a class="header-anchor" href="#v0-25-45-—-may-4-2026" aria-label="Permalink to &quot;v0.25.45 — May 4, 2026&quot;">​</a></h2><h3 id="connector-configs-c-parameter-replaced-by-driver-closure-variable" tabindex="-1">Connector configs: <code>c</code> parameter replaced by <code>driver</code> closure variable <a class="header-anchor" href="#connector-configs-c-parameter-replaced-by-driver-closure-variable" aria-label="Permalink to &quot;Connector configs: \`c\` parameter replaced by \`driver\` closure variable&quot;">​</a></h3><p>Custom methods, getters, and setters in connector configs no longer receive the driver proxy as a leading argument. Instead, the driver is in scope as the closure variable <code>driver</code>. Function signatures now match the script-side call exactly:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">methods</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> driver.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">startPolling</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  rampTo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">target</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { driver.voltage </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> target; } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">properties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  voltage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> driver.voltage,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">v</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { driver.voltage </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v; }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  power</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> driver.voltage </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> driver.current,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This removes the asymmetry between how a script calls a connector (<code>psu.rampTo(5)</code>) and how the connector declared the method (<code>(c, target) =&gt; …</code>), and gets rid of the cryptic <code>c</code> parameter.</p><p><strong>Breaking change.</strong> Configs still using the old <code>(c) =&gt; c.…</code> shape will fail at runtime — V8 throws because <code>c</code> is no longer bound. All bundled connectors, driver template stubs, and docs have been updated to the new form. Update any pre-release config in <code>workspace/connectors/</code>.</p><h2 id="v0-25-44-—-may-4-2026" tabindex="-1">v0.25.44 — May 4, 2026 <a class="header-anchor" href="#v0-25-44-—-may-4-2026" aria-label="Permalink to &quot;v0.25.44 — May 4, 2026&quot;">​</a></h2><h3 id="python-connector-init-runs-in-the-background-—-ui-shows-live-status" tabindex="-1">Python connector init runs in the background — UI shows live status <a class="header-anchor" href="#python-connector-init-runs-in-the-background-—-ui-shows-live-status" aria-label="Permalink to &quot;Python connector init runs in the background — UI shows live status&quot;">​</a></h3><p>A multi-minute pip-install + model-load on connector activate was blocking the synchronous <code>InitAsync</code> path. Even with last commit&#39;s 30-minute timeout, the user got a frozen UI while torch downloaded — and a single bad driver could hang the whole connectors panel for everyone else trying to enable theirs. Replaced with a fire-and-forget background init + live status broadcast.</p><p><strong>Server side</strong> (<code>MuxitServer/Drivers/PythonDriverHost.cs</code>)</p><ul><li><code>InitAsync</code> now returns immediately after the synchronous catalog / interpreter pre-checks. The actual venv setup, pip install, subprocess launch, and init RPC run on a <code>Task.Run</code> worker (<code>RunInitInBackgroundAsync</code>) with a per-instance <code>CancellationTokenSource</code>.</li><li>New <code>PythonInstanceState</code> enum: <code>Loading | Ready | Error | Cancelled</code>, plus a <code>PythonInstanceStatus</code> record stored in <code>_instanceStatus</code>. Updated at every phase boundary (<code>Preparing virtual environment...</code> → <code>Installing requirements...</code> → <code>Starting subprocess...</code> → <code>Initialising driver...</code> → <code>Ready</code>). Latest pip / venv output line is mirrored into the message field too, so the UI gets a flowing progress string.</li><li>New <code>EnsureReady(instanceId, verb)</code> helper. <code>GetPropertyAsync</code>, <code>SetPropertyAsync</code>, <code>ExecuteActionAsync</code> call it before dispatching — calls against a still-loading connector throw a clear <code>&quot;Python connector &#39;foo&#39; is not ready (loading): Installing requirements... Cannot execute action &#39;speak&#39; until initialisation completes.&quot;</code> instead of either silently waiting or timing out arbitrarily.</li><li><code>ShutdownAsync</code> now cancels the in-flight init token before tearing down. Disabling the connector mid-install kills the running pip subprocess (via <code>proc.Kill(entireProcessTree: true)</code> in <code>PythonVenvManager.RunStreamingAsync</code>&#39;s new <code>OperationCanceledException</code> branch) instead of leaving torch&#39;s wheel-builder children running. <code>ShutdownAllAsync</code> includes loading instances too.</li><li>The init RPC&#39;s own timeout is back to a sane 5 minutes — pip install (which has no timeout) is now in the background, so the RPC only covers the model-load step inside the user&#39;s <code>init()</code>.</li></ul><p><strong>Wire layer</strong> (<code>MuxitServer/WebSocket/EventBridge.cs</code>)</p><ul><li>New <code>connector.status</code> rebroadcast from the EventBus to all WS clients: <code>{ connector, driver, state, message }</code>. The existing <code>driver.python.install</code> event continues to carry per-line pip output for the server log; <code>connector.status</code> is the higher-level state machine the UI binds to.</li></ul><p><strong>UI</strong> (<code>web-ui/src/hooks/useServer.js</code>, <code>web-ui/src/components/ConnectorBrowser.jsx</code>)</p><ul><li><code>useServer</code> adds a <code>connectorStatus</code> state map keyed by connector name; subscribes to <code>connector.status</code> messages and updates the entry on every transition.</li><li><code>ConnectorCard</code> reads its entry from <code>server.connectorStatus[name]</code> and renders a one-line status badge below the header when state ≠ <code>ready</code>: a spinning gear + the latest message for <code>loading</code>, a red ✗ + error message for <code>error</code>, a muted ⊘ + reason for <code>cancelled</code>. Disappears once the connector is ready, leaving the normal card layout.</li></ul><p><strong>User-visible effect</strong></p><ul><li>Activating <code>python-chatterbox</code> now returns instantly. The connector card immediately shows <code>⚙ Installing: Creating venv at python/.venvs/chatterbox</code>, then a flow of <code>[pip] Collecting torch ...</code> lines, then <code>Loading chatterbox model on cpu (first run downloads ~2GB; subsequent runs are cached)...</code>, then disappears when the connector is ready. Other connectors can be enabled / used in parallel; nothing blocks on chatterbox&#39;s install.</li><li>Disabling the connector mid-install kills pip and the model download cleanly. Re-enabling starts fresh.</li></ul><h2 id="v0-25-43-—-may-4-2026" tabindex="-1">v0.25.43 — May 4, 2026 <a class="header-anchor" href="#v0-25-43-—-may-4-2026" aria-label="Permalink to &quot;v0.25.43 — May 4, 2026&quot;">​</a></h2><h3 id="three-fixes-that-turned-the-chatterbox-first-run-from-broken-to-working-but-slow" tabindex="-1">Three fixes that turned the chatterbox first-run from &quot;broken&quot; to &quot;working but slow&quot; <a class="header-anchor" href="#three-fixes-that-turned-the-chatterbox-first-run-from-broken-to-working-but-slow" aria-label="Permalink to &quot;Three fixes that turned the chatterbox first-run from &quot;broken&quot; to &quot;working but slow&quot;&quot;">​</a></h3><p>A user activating the <code>python-chatterbox</code> example connector hit:</p><p><code>Failed to init connector &#39;python-chatterbox&#39;: Python driver did not respond to &#39;init&#39; within 60000ms</code></p><p>…after seeing only <code>Installing requirements from chatterbox.requirements.txt (first run can take a while)</code> and then no further log output for a minute. Three independent issues were combining:</p><ul><li><strong>Init RPC timeout was 60s.</strong> Way too short for any ML driver — chatterbox&#39;s <code>ChatterboxTTS.from_pretrained()</code> downloads several GB of model weights from HuggingFace and warms them into RAM, easily 5–15 minutes on a fresh machine. Bumped to 30 minutes in <code>PythonDriverHost.InitAsync</code>. Pip install (which precedes init) already had no timeout, so this just brings init in line. Genuinely runaway processes still get caught; users who want to interrupt earlier can disable the connector.</li><li><strong>Pip&#39;s progress bar was invisible.</strong> <code>pip install</code> writes its download progress with <code>\\r</code> (no newline), and .NET&#39;s <code>Process.OutputDataReceived</code> only fires on <code>\\n</code>. So between download-step lines (which can be several minutes apart for a 2 GB wheel), the user saw absolute silence and assumed pip had hung. Added <code>--progress-bar off</code> to the pip invocation in <code>PythonVenvManager.PipInstallAsync</code> so each download step prints as a flushed line.</li><li><strong>The chatterbox example script was silent during <code>init()</code>.</strong> Even with pip output flowing, the first <code>from chatterbox.tts import ChatterboxTTS</code> plus the model load can take another minute or two. The example now prints stderr heartbeats (<code>Loading chatterbox library...</code>, <code>Loading chatterbox model on cpu (first run downloads ~2GB; subsequent runs are cached)...</code>, <code>Chatterbox ready (sample rate 24000 Hz, ...)</code>) via a small <code>_say()</code> helper that goes through <code>sys.stderr</code> so it can&#39;t collide with the JSON-RPC channel. The host&#39;s stderr pump forwards them verbatim with the <code>[connector-name] stderr:</code> prefix.</li></ul><p>Net effect for a user activating the chatterbox connector for the first time: continuous progress output throughout the multi-minute first install + model load, instead of the previous &quot;log line, silence, timeout error&quot; experience.</p><h2 id="v0-25-42-—-may-4-2026" tabindex="-1">v0.25.42 — May 4, 2026 <a class="header-anchor" href="#v0-25-42-—-may-4-2026" aria-label="Permalink to &quot;v0.25.42 — May 4, 2026&quot;">​</a></h2><h3 id="chatterbox-tts-example-for-the-generic-python-driver" tabindex="-1">Chatterbox TTS example for the generic Python driver <a class="header-anchor" href="#chatterbox-tts-example-for-the-generic-python-driver" aria-label="Permalink to &quot;Chatterbox TTS example for the generic Python driver&quot;">​</a></h3><p>Adds a fourth example to the workspace template demonstrating the heavy-dependency case for the Tier 2 generic Python driver:</p><ul><li><code>workspace/python/chatterbox.py</code> + <code>chatterbox.requirements.txt</code> — TTS via <a href="https://github.com/resemble-ai/chatterbox" target="_blank" rel="noreferrer">chatterbox-tts</a>.</li><li><code>workspace/connectors/python-chatterbox.js</code> — connector exposing <code>speak()</code>, <code>speakWithVoice()</code>, <code>speakAs()</code>, plus <code>sampleRate</code> / <code>device</code> / <code>outDir</code> properties.</li></ul><p>The flow is the same as the existing <code>http-probe</code> example, just with a much larger first-install cost: activating the connector for the first time creates <code>workspace/python/.venvs/chatterbox/</code>, pip-installs torch + transformers + chatterbox into it (a couple of GB, several minutes — output streams to the server log so you can watch progress), then loads the model. Subsequent activations skip pip and start in a few seconds; the loaded model stays in memory across <code>speak()</code> calls.</p><p>Each call writes a WAV file under <code>workspace/data/chatterbox/</code> and returns the absolute path. Pipe that path into a player, an <code>&lt;audio&gt;</code> element, or any downstream connector that handles audio playback. The companion docstrings populate the API Reference tab so users see usage hints (<code>device</code> config, voice cloning via <code>voice</code> parameter, output filename overrides) without leaving the driver page.</p><p>This commit is not smoke-tested in CI — installing torch from the chatterbox dependency tree takes too long for the test runner. The plumbing it relies on (per-script venv + Python tier dispatch) is covered by the existing <code>http-probe</code> and counter / hello tests.</p><h2 id="v0-25-41-—-may-4-2026" tabindex="-1">v0.25.41 — May 4, 2026 <a class="header-anchor" href="#v0-25-41-—-may-4-2026" aria-label="Permalink to &quot;v0.25.41 — May 4, 2026&quot;">​</a></h2><h3 id="driver-detail-page-surfaces-the-manifest-details-python-tier-label" tabindex="-1">Driver detail page surfaces the manifest details + Python tier label <a class="header-anchor" href="#driver-detail-page-surfaces-the-manifest-details-python-tier-label" aria-label="Permalink to &quot;Driver detail page surfaces the manifest details + Python tier label&quot;">​</a></h3><p>Two follow-ups to the driver page after testing the Python driver in-app:</p><ul><li><strong>Tier badge for Python drivers no longer shows &quot;Unknown&quot;.</strong> The two driver-detail components in the web UI (<code>DriverDetailPage.jsx</code> for the standalone Drivers panel, <code>DriverDocViewer.jsx</code> for the in-editor doc viewer) had their tier label maps hard-coded for tiers 0/1/3 only. Both now include <code>2: &#39;Python&#39;</code>. The &quot;trust badge&quot; ladder on <code>DriverDetailPage</code> similarly gains a <code>Community (Python)</code> arm.</li><li><strong>API Reference tab now renders manifest <code>details</code> as a &quot;Documentation&quot; section.</strong> The earlier change wired <code>details</code> through <code>driver.schema</code> for the in-editor viewer, but the user-facing Drivers panel uses <code>marketplace.detail</code> which wasn&#39;t including the field. Added <code>details = localEntry?.Details</code> to that handler&#39;s response, then rendered it as a top-of-tab Documentation section using the existing <code>DetailsMarkdown</code> component (now exported from <code>DriverDocViewer</code>). Imports the existing <code>driver-doc.css</code> so the markdown styling (paragraphs, lists, code blocks) applies in the standalone page too.</li></ul><p>Effect for the Python driver: clicking it in the Drivers panel now shows the badge <code>Community (Python)</code> instead of <code>Unknown</code>, and the API Reference tab leads with the ~4 KB markdown documenting layout, the <code>requirements.txt</code> lifecycle, examples, and conventions — instead of just the Connector Template snippet. Same plumbing benefits any driver (DLL / JS / built-in) whose manifest declares a <code>details</code> field.</p><h2 id="v0-25-40-—-may-4-2026" tabindex="-1">v0.25.40 — May 4, 2026 <a class="header-anchor" href="#v0-25-40-—-may-4-2026" aria-label="Permalink to &quot;v0.25.40 — May 4, 2026&quot;">​</a></h2><h3 id="driver-doc-viewer-surfaces-long-form-details" tabindex="-1">Driver doc viewer surfaces long-form <code>details</code> <a class="header-anchor" href="#driver-doc-viewer-surfaces-long-form-details" aria-label="Permalink to &quot;Driver doc viewer surfaces long-form \`details\`&quot;">​</a></h3><p>The driver detail page in the Drivers panel previously had room only for a one-line description and the property/action tables. Drivers like the new generic <code>Python</code> one needed more — how <code>requirements.txt</code> works, where scripts live, what conventions to follow — and there was nowhere to put it without bloating the always-visible description (which feeds into the AI system prompt).</p><p>Three coordinated changes:</p><ul><li><strong><code>manifest.json</code> gains an optional <code>details</code> field.</strong> Markdown, rendered as a collapsible &quot;Show details&quot; section in <code>DriverDocViewer.jsx</code> directly under the description. Threaded through all three tiers: <code>DriverManifest</code> → <code>DriverEntry.Details</code> → the <code>driver.schema</code> WebSocket response.</li><li><strong>Per-action and per-property <code>details</code></strong> are now sent in the schema response (the descriptor records always carried the field; the WS handler just wasn&#39;t including it). The existing <code>DetailsDisclosure</code> component already rendered them when present, so the change immediately unlocks more useful entries on every driver.</li><li><strong>The generic Python driver&#39;s introspection now splits docstrings into description + details.</strong> The first line of a function&#39;s docstring becomes its action&#39;s <code>description</code> (always-on, in the AI prompt); the rest becomes <code>details</code> (markdown, surfaced on demand). Same split for properties via the <code>get_&lt;x&gt;</code> docstring. So writing a normal Python docstring on <code>def head(url):</code> automatically populates both fields in the UI.</li></ul><p>Concrete payoff: the <code>Python</code> driver&#39;s manifest now ships a ~4 KB markdown details block covering layout, the <code>requirements.txt</code> lifecycle, examples, and limitations — visible by clicking &quot;Show details&quot; on the driver page. The shipped example scripts (<code>hello.py</code>, <code>counter.py</code>, <code>http-probe.py</code>) gain richer docstrings so each action / property has both a one-liner and a paragraph of context.</p><p>The <code>details</code> field is optional; existing drivers (built-ins, JS, DLL packages) keep rendering exactly as before until their manifests add the field.</p><h2 id="v0-25-39-—-may-4-2026" tabindex="-1">v0.25.39 — May 4, 2026 <a class="header-anchor" href="#v0-25-39-—-may-4-2026" aria-label="Permalink to &quot;v0.25.39 — May 4, 2026&quot;">​</a></h2><h3 id="fix-connector-calls-on-the-generic-python-driver-were-rejected" tabindex="-1">Fix: connector calls on the generic <code>Python</code> driver were rejected <a class="header-anchor" href="#fix-connector-calls-on-the-generic-python-driver-were-rejected" aria-label="Permalink to &quot;Fix: connector calls on the generic \`Python\` driver were rejected&quot;">​</a></h3><p><code>connector(&quot;python-hello&quot;).greet(&quot;world&quot;)</code> was throwing <code>Driver has no property or action &#39;greet&#39;</code> because the connector proxy authorises calls against the driver&#39;s catalog-time META, and the generic <code>Python</code> driver&#39;s META is intentionally empty (its actions are dynamic — they come from whichever user script the connector points at).</p><p>Fixed by mirroring the per-instance schema mechanism the DLL tier already had for <code>GenericScpi</code>: after <code>init()</code> returns, the host now sends a new <code>schema</code> RPC to the Python driver subprocess. The generic driver responds by introspecting the loaded user module:</p><ul><li>top-level callables (excluding <code>init</code>, <code>shutdown</code>, and underscore-prefixed names) → actions, with the function&#39;s first docstring line as the description</li><li>paired <code>get_&lt;x&gt;</code> / <code>set_&lt;x&gt;</code> → R/W property <code>&lt;x&gt;</code></li><li>unpaired <code>get_&lt;x&gt;</code> → read-only, unpaired <code>set_&lt;x&gt;</code> → write-only</li></ul><p>Typed <code>Driver</code> subclasses get a sensible default — <code>schema()</code> falls back to the values declared in <code>META</code> — so existing typed drivers don&#39;t change.</p><p><code>PythonDriverHost.GetDriverMetaForInstance(instanceId)</code> exposes the per-instance schema; <code>DriverRegistry.GetDriverMeta</code> consults it first for Python instances and falls back to the catalog-level meta for instances that haven&#39;t completed init yet. The schema is cleaned out on <code>ShutdownAsync</code> so a re-activate gets a fresh introspection.</p><p>After this, <code>c.greet({ name })</code> from a connector method body lands on the driver&#39;s <code>execute(&quot;greet&quot;, {...})</code> and dispatches through to <code>module.greet(name=&quot;...&quot;)</code> as intended.</p><h2 id="v0-25-38-—-may-3-2026" tabindex="-1">v0.25.38 — May 3, 2026 <a class="header-anchor" href="#v0-25-38-—-may-3-2026" aria-label="Permalink to &quot;v0.25.38 — May 3, 2026&quot;">​</a></h2><h3 id="python-tier-—-examples-by-connector-one-driver-to-install" tabindex="-1">Python tier — examples by connector, one driver to install <a class="header-anchor" href="#python-tier-—-examples-by-connector-one-driver-to-install" aria-label="Permalink to &quot;Python tier — examples by connector, one driver to install&quot;">​</a></h3><p>The two scaffolding-style Python drivers introduced alongside the tier (<code>drivers/py/example/</code> and <code>drivers/py/example-with-deps/</code>) are removed. They duplicated what the generic <code>muxit/python</code> driver now does, and shipping three Python <code>.muxdriver</code> packages where one will do made the marketplace listing confusing.</p><p>In their place, <code>workspace-template/python/</code> now ships three working user scripts and <code>workspace-template/connectors/</code> ships matching connectors so a freshly seeded workspace boots with usable Python examples:</p><ul><li><strong><code>hello.py</code></strong> + <code>python-hello.js</code> — minimal, no dependencies. Single <code>greet(name)</code> action.</li><li><strong><code>counter.py</code></strong> + <code>python-counter.js</code> — adds state; demonstrates <code>get_&lt;x&gt;()</code> / <code>set_&lt;x&gt;(value)</code> properties and <code>init(config)</code>.</li><li><strong><code>http-probe.py</code></strong> + <code>python-http-probe.js</code> — adds <code>requests</code> as a runtime dependency; the sibling <code>http-probe.requirements.txt</code> triggers the per-script venv flow on first activation.</li></ul><p><code>workspace-template/python/README.md</code> documents the conventions (one file per script, optional <code>init</code> / <code>shutdown</code>, <code>get_&lt;x&gt;</code> / <code>set_&lt;x&gt;</code> properties, <code>&lt;stem&gt;.requirements.txt</code> for deps).</p><p><code>lib/muxit-menu/index.js</code> updated so <code>python/</code> is part of the workspace seed (option 17), the empty-workspace dir creation list (option 16), and the workspace status report (option 20).</p><p>The driver-side regression test (<code>drivers/py/python-runner/</code>) and the muxit_driver SDK are unchanged — the deleted drivers were only example payloads, not part of the platform.</p><h2 id="v0-25-37-—-may-3-2026" tabindex="-1">v0.25.37 — May 3, 2026 <a class="header-anchor" href="#v0-25-37-—-may-3-2026" aria-label="Permalink to &quot;v0.25.37 — May 3, 2026&quot;">​</a></h2><h3 id="generic-python-driver-—-write-functions-skip-the-boilerplate" tabindex="-1">Generic <code>Python</code> driver — write functions, skip the boilerplate <a class="header-anchor" href="#generic-python-driver-—-write-functions-skip-the-boilerplate" aria-label="Permalink to &quot;Generic \`Python\` driver — write functions, skip the boilerplate&quot;">​</a></h3><p>A new built-and-shipped Python driver (<code>drivers/py/python-runner/</code>, manifest id <code>muxit/python</code>) lets a connector point at any <code>.py</code> file in <code>workspace/python/</code> and exposes that file&#39;s top-level functions as RPC actions / properties. Users who want to glue an existing Python library to a Muxit connector no longer have to subclass <code>Driver</code> or wire up a JSON-RPC dispatch — they drop a script with bare functions, optionally a sibling <code>&lt;name&gt;.requirements.txt</code>, and reference it from the connector&#39;s <code>config.script</code>.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># workspace/python/chatterbox.py</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(config): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> speak</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(text): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # ← connector().speak({ text }) lands here</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> get_status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # ← connector().status read</span></span></code></pre></div><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">driver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Python&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">script</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;chatterbox&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">methods</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><p>Per-script venvs at <code>workspace/python/.venvs/&lt;stem&gt;/</code> give clean dependency isolation between scripts (one with torch, one with pyserial, no collisions). First activation creates the venv and pip-installs the requirements with line-by-line output streaming to the server console. Subsequent activations check the SHA-256 of the requirements file against a stamp and skip pip when nothing changed (~150ms warm start). Migration to a typed <code>Driver</code> subclass is mechanical when a script outgrows the generic dispatch.</p><p>Plumbed through end-to-end: <code>DriverRegistry.CreateInstanceAsync</code> injects <code>_workspacePath</code> into the config dict for Tier 2 drivers (mirroring the built-in driver path), so the generic driver can resolve <code>workspace/python/&lt;script&gt;.py</code> reliably regardless of what the user wrote in the connector config.</p><p>New &quot;Two ways to use Python&quot; + &quot;Generic <code>Python</code> driver&quot; sections in <code>docs-site/reference/driver-sdk-python.md</code>.</p><h2 id="v0-25-36-—-may-3-2026" tabindex="-1">v0.25.36 — May 3, 2026 <a class="header-anchor" href="#v0-25-36-—-may-3-2026" aria-label="Permalink to &quot;v0.25.36 — May 3, 2026&quot;">​</a></h2><h3 id="python-driver-debugging-helpers" tabindex="-1">Python driver debugging helpers <a class="header-anchor" href="#python-driver-debugging-helpers" aria-label="Permalink to &quot;Python driver debugging helpers&quot;">​</a></h3><p>Two small, no-overhead additions to the Python driver SDK so a problem in a Tier 2 driver is a few minutes to track down instead of &quot;stare at stderr and guess.&quot;</p><ul><li><strong>Tracebacks in error responses.</strong> When a driver method raises, the JSON-RPC error sent to the host (and on to the script / dashboard / AI) now includes a compact location excerpt — first line is the exception, then up to 3 frames of the user&#39;s own code (SDK frames are filtered out). Looks like <code>KeyError: &#39;unknown property: nope&#39;\\n at get() in mydriver.driver.py:62</code>. The full traceback still goes to stderr / server console as before.</li><li><strong>Optional debugger attach via <code>MUXIT_PYTHON_DEBUG</code>.</strong> Setting the env var to a port number on the driver&#39;s process makes the SDK call <code>debugpy.listen((&quot;127.0.0.1&quot;, port))</code> at startup and wait for a client before <code>init</code>. Attach VS Code&#39;s &quot;Python: Attach&quot; launcher and step through your driver. No-op when the env var isn&#39;t set; degrades to a one-line stderr hint if <code>debugpy</code> isn&#39;t installed (add it to <code>requirements.txt</code> to use). <code>pdb.set_trace()</code> continues to <strong>not</strong> work — the subprocess&#39; stdin is busy with JSON-RPC.</li></ul><p>New &quot;Debugging&quot; section in <code>docs-site/reference/driver-sdk-python.md</code> covers the three escalation paths: read the error response, run the driver standalone with <code>--scan</code> / <code>--instance</code>, attach <code>debugpy</code>. Plus a &quot;Common pitfalls&quot; subsection (<code>(non-json stdout)</code>, module-level heavy imports, hangs during pip install, missing <code>python3-venv</code> on Debian).</p><h2 id="v0-25-35-—-may-3-2026" tabindex="-1">v0.25.35 — May 3, 2026 <a class="header-anchor" href="#v0-25-35-—-may-3-2026" aria-label="Permalink to &quot;v0.25.35 — May 3, 2026&quot;">​</a></h2><h3 id="features-6" tabindex="-1">Features <a class="header-anchor" href="#features-6" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Python drivers auto-install their dependencies.</strong> A driver that ships a <code>requirements.txt</code> next to its <code>.driver.py</code> now gets a per-driver virtual environment (<code>.venv/</code>) created in its package cache dir on the first connector activation. Pip runs against that venv and the driver subprocess is launched with the venv&#39;s <code>python</code>, so <code>import torch</code> (or anything else inside <code>init()</code>) resolves to a clean per-driver install. New <code>MuxitServer/Drivers/PythonVenvManager.cs</code> handles the lifecycle: hashes <code>requirements.txt</code> into <code>.venv/.muxit-req-hash</code> so subsequent activations skip the install when nothing changed; streams pip&#39;s stdout/stderr line-by-line to the server console and to a new <code>driver.python.install</code> EventBus event so the UI can show progress while the user waits. Different drivers can pin conflicting versions of the same package without interfering — each lives in its own venv. Cached venv start time is ~150ms; first install runs as fast as pip + the network can manage (~2s for <code>requests</code>, several minutes for torch).</li><li><strong>Example driver with deps.</strong> <code>drivers/py/example-with-deps/</code> (PyHttpProbe) demonstrates the flow with a small <code>requests</code> dependency. Validates + builds + runs end-to-end via the new venv plumbing.</li></ul><h3 id="known-limitations" tabindex="-1">Known limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known limitations&quot;">​</a></h3><ul><li>Native wheels (torch, opencv) sometimes need system-level build tools or libraries; pip failures surface in the driver&#39;s init error with the full transcript above. Adding a &quot;diagnose missing system deps&quot; pass is on the roadmap.</li><li>On Debian/Ubuntu the system Python may not include the <code>venv</code> module by default (<code>apt install python3-venv</code> once if you see <code>python -m venv</code> failing).</li><li>Loose <code>.driver.py</code> files dropped into <code>workspace/drivers/</code> (no <code>.muxdriver</code> packaging) don&#39;t get a managed venv — they use the system interpreter directly. This is the dev workflow; packaged drivers are the user-facing path.</li></ul><h2 id="v0-25-34-—-may-3-2026" tabindex="-1">v0.25.34 — May 3, 2026 <a class="header-anchor" href="#v0-25-34-—-may-3-2026" aria-label="Permalink to &quot;v0.25.34 — May 3, 2026&quot;">​</a></h2><h3 id="features-7" tabindex="-1">Features <a class="header-anchor" href="#features-7" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Python driver tier (Tier 2).</strong> Drivers can now be authored in Python — a new <code>.driver.py</code> extension alongside the existing <code>.driver.js</code> and <code>.dll</code> tiers. Each active connector spawns its own Python subprocess; MuxitServer talks to it over line-delimited JSON-RPC on stdin/stdout (<code>init</code> / <code>get</code> / <code>set</code> / <code>execute</code> / <code>shutdown</code>). Drivers subclass <code>muxit_driver.Driver</code> and call <code>run(YourDriverClass)</code>. Wired through end-to-end: <code>DriverTier.Python = 2</code> (<code>MuxitServer/Drivers/DriverTypes.cs</code>), <code>PythonDriverHost</code> + <code>PythonDriverInstance</code> for subprocess + IPC, <code>PythonRuntimeManager</code> to detect a usable interpreter (<code>MUXIT_PYTHON</code> env var → <code>python3</code> → <code>python</code>, requires ≥ 3.10), <code>DriverPackageStore</code> recognises <code>*.driver.py</code> packages, manifest schema accepts <code>tier: 2</code>, <code>node drivers.js build</code> packages Python drivers (auto-vendoring the <code>muxit_driver</code> SDK into every <code>.muxdriver</code> so installed drivers don&#39;t need anything on <code>PYTHONPATH</code>). Use it for Python-only ecosystems (numpy, torch, instrument SDKs); JS remains the right choice for serial/TCP instruments.</li><li><strong>Python interpreter detection is optional.</strong> If no usable Python is found at startup, the server keeps booting; Python drivers (if any are installed) surface a warning at scan time instead of failing the run. Set <code>MUXIT_PYTHON</code> to a full interpreter path to override probing. See <code>docs-site/reference/driver-sdk-python.md</code> for the SDK reference, manifest, and wire protocol.</li><li><strong>Example Python driver.</strong> <code>drivers/py/example/</code> ships a deps-free counter+greeter driver as a starting point and as the regression target for the JSON-RPC bridge.</li></ul><h3 id="known-limitations-1" tabindex="-1">Known limitations <a class="header-anchor" href="#known-limitations-1" aria-label="Permalink to &quot;Known limitations&quot;">​</a></h3><ul><li>Python drivers are free-only — premium signing isn&#39;t implemented for the Python tier yet (<code>PYTHON_PREMIUM_FORBIDDEN</code> validator rule).</li><li>Runtime dependencies declared in a driver&#39;s <code>requirements.txt</code> are bundled into the <code>.muxdriver</code>. Auto-installation into a per-driver venv landed in v0.25.35.</li></ul><h2 id="v0-25-33-—-may-4-2026" tabindex="-1">v0.25.33 — May 4, 2026 <a class="header-anchor" href="#v0-25-33-—-may-4-2026" aria-label="Permalink to &quot;v0.25.33 — May 4, 2026&quot;">​</a></h2><h3 id="csp-allow-wasm-unsafe-eval-for-the-opus-decoder-polyfill" tabindex="-1">CSP: allow <code>&#39;wasm-unsafe-eval&#39;</code> for the opus-decoder polyfill <a class="header-anchor" href="#csp-allow-wasm-unsafe-eval-for-the-opus-decoder-polyfill" aria-label="Permalink to &quot;CSP: allow \`&#39;wasm-unsafe-eval&#39;\` for the opus-decoder polyfill&quot;">​</a></h3><p>The dashboard&#39;s strict Content-Security-Policy was blocking <code>WebAssembly.compile()</code> (<code>script-src &#39;self&#39; blob:</code> doesn&#39;t permit WASM compilation), so the <code>opus-decoder</code> polyfill threw on load and the dashboard went silent on insecure-context connections.</p><p>Both the docs CSP and the SPA CSP now include <code>&#39;wasm-unsafe-eval&#39;</code> in their <code>script-src</code> directive. This is a much narrower permission than <code>&#39;unsafe-eval&#39;</code> — it allows <code>WebAssembly.compile/instantiate</code> only, not arbitrary <code>eval()</code> or string-to-code conversion. No other directive changed.</p><h2 id="v0-25-32-—-may-4-2026" tabindex="-1">v0.25.32 — May 4, 2026 <a class="header-anchor" href="#v0-25-32-—-may-4-2026" aria-label="Permalink to &quot;v0.25.32 — May 4, 2026&quot;">​</a></h2><h3 id="audio-renderer-pass-channel-count-into-the-wasm-opus-decoder" tabindex="-1">Audio renderer: pass channel count into the WASM Opus decoder <a class="header-anchor" href="#audio-renderer-pass-channel-count-into-the-wasm-opus-decoder" aria-label="Permalink to &quot;Audio renderer: pass channel count into the WASM Opus decoder&quot;">​</a></h3><p>Fix for &quot;no sound when accessing the dashboard remotely (e.g. <code>http://192.168.x.x:8765</code>)&quot;: the <code>opus-decoder</code> constructor defaults to <strong>2 channels</strong>, but the encoder produces mono. A mono Opus packet decoded by a 2-channel decoder fails silently and returns <code>samplesDecoded: 0</code>, which the renderer was discarding without any log output — so the dashboard just went quiet.</p><ul><li><strong>WASM polyfill now gets <code>{ sampleRate, channels }</code></strong> from the wire payload. WebCodecs already configured channels correctly; the WASM path was missing it.</li><li><strong>Better diagnostics.</strong> When the decoder returns 0 samples, log the configured channels / sampleRate and any decoder errors once per stream so a future regression is visible in DevTools.</li><li>The reason this only manifested on remote connections: WebCodecs is gated to <strong>secure contexts</strong> (HTTPS or <code>localhost</code>). Connecting from another machine uses <code>http://&lt;lan-ip&gt;</code> which is not a secure context, so <code>window.AudioDecoder</code> is undefined and the renderer fell through to the WASM polyfill path. Local browsers loading via <code>localhost</code> never hit it.</li></ul><h2 id="v0-25-31-—-may-4-2026" tabindex="-1">v0.25.31 — May 4, 2026 <a class="header-anchor" href="#v0-25-31-—-may-4-2026" aria-label="Permalink to &quot;v0.25.31 — May 4, 2026&quot;">​</a></h2><h3 id="audio-renderer-jitter-buffer-mobile-fallback" tabindex="-1">Audio renderer: jitter buffer + mobile fallback <a class="header-anchor" href="#audio-renderer-jitter-buffer-mobile-fallback" aria-label="Permalink to &quot;Audio renderer: jitter buffer + mobile fallback&quot;">​</a></h3><p>Two fixes for the browser-side audio path.</p><ul><li><strong>Cracking on desktop, fixed.</strong> <code>ServerAudioRenderer</code> now schedules buffers ~100 ms ahead of <code>ctx.currentTime</code> (the jitter buffer) instead of letting decode latency push <code>nextStartTime</code> into the past. The previous behaviour was the classic Web Audio streaming bug — async decode let <code>currentTime</code> overtake the cursor, the <code>Math.max(now, …)</code> clamp left a silent gap at every buffer boundary, and you&#39;d hear a click ~50 times a second. Underruns are now detected and recovered by resetting the cursor with a fresh 100 ms lookahead.</li><li><strong>48 kHz <code>AudioContext</code>.</strong> Created with <code>{ sampleRate: 48000 }</code> so decoded 48 kHz buffers don&#39;t get implicitly resampled to the device&#39;s native rate (typically 44.1 kHz on macOS/iOS). Falls back to the default-rate constructor on browsers that throw on the options arg.</li><li><strong>Opus on mobile / older browsers.</strong> The renderer now lazily imports the <a href="https://github.com/eshaz/wasm-audio-decoders" target="_blank" rel="noreferrer"><code>opus-decoder</code></a> WASM polyfill when <code>AudioDecoder</code> (WebCodecs) is missing. Covers iOS Safari &lt; 16.4, in-app WebViews, and other browsers where WebCodecs isn&#39;t shipped or doesn&#39;t support raw Opus. The polyfill bundle is only fetched on browsers that need it; WebCodecs hosts pay nothing extra.</li><li>The PCM16 fallback path goes through the same shared scheduler, so it benefits from the jitter buffer too.</li></ul><h2 id="v0-25-30-—-may-4-2026" tabindex="-1">v0.25.30 — May 4, 2026 <a class="header-anchor" href="#v0-25-30-—-may-4-2026" aria-label="Permalink to &quot;v0.25.30 — May 4, 2026&quot;">​</a></h2><h3 id="audio-encoding-moves-into-the-host" tabindex="-1">Audio encoding moves into the host <a class="header-anchor" href="#audio-encoding-moves-into-the-host" aria-label="Permalink to &quot;Audio encoding moves into the host&quot;">​</a></h3><p>The Opus encoder (Concentus) moved out of the AudioSynth driver and into a single shared <code>MuxitServer.Audio.AudioStreamEncoder</code>. Drivers that emit audio now hand the host raw float PCM through a new SDK property and the host owns codec, framing, pacing, EventBus emission, and the stop-frame. This is the right shape for the upcoming TTS connectors (chatterbox, cosyvoice, OpenAI TTS, ElevenLabs) — they can plug in without bundling their own copy of Concentus, and a future swap to binary WS frames stays a single-place change.</p><ul><li><strong>SDK addition.</strong> <code>IConnectorDriver</code> gains <code>Func&lt;float[], AudioFrameInfo, CancellationToken, Task&gt;? AudioStreamEmitter</code>, alongside the existing string <code>StreamEmitter</code>. New value type <code>Muxit.Driver.Sdk.AudioFrameInfo(int SampleRate, int Channels)</code> with a convenience <code>AudioFrameInfo.Mono48k</code>.</li><li><strong>Host addition.</strong> New <code>MuxitServer.Audio.AudioStreamEncoder</code> wrapping Concentus. <code>DriverRegistry.InitBuiltInAsync</code> and <code>DllDriverHost.InitAsync</code> wire <code>driver.AudioStreamEmitter</code> to it alongside the existing string <code>StreamEmitter</code> wiring.</li><li><strong><code>drivers/Muxit.Driver.AudioSynth</code></strong> bumped to <strong>1.4.1</strong>. Removed the Concentus PackageReference and the in-driver Opus encoding code; <code>StreamSamplesAsync</code> now just <code>await</code>s the host emitter.</li><li><strong>Wire format unchanged.</strong> Browser still sees the same JSON envelope (<code>{ op, data, sampleRate, channels, format: &quot;opus&quot;, frameSize }</code>) and <code>ServerAudioRenderer</code> is untouched.</li><li>JS drivers (Tier 1) still go through the legacy string emitter — the audio emitter is C#-only for now.</li></ul><h2 id="v0-25-29-—-may-4-2026" tabindex="-1">v0.25.29 — May 4, 2026 <a class="header-anchor" href="#v0-25-29-—-may-4-2026" aria-label="Permalink to &quot;v0.25.29 — May 4, 2026&quot;">​</a></h2><h3 id="audio-streams-are-now-opus-encoded-by-default" tabindex="-1">Audio streams are now Opus-encoded by default <a class="header-anchor" href="#audio-streams-are-now-opus-encoded-by-default" aria-label="Permalink to &quot;Audio streams are now Opus-encoded by default&quot;">​</a></h3><p>The <code>audio</code> stream wire format switches from base64 PCM16 to base64 Opus packets (32 kbit/s VBR, 20 ms frames at 48 kHz mono). The driver-side encoder is <a href="https://github.com/lostromb/concentus" target="_blank" rel="noreferrer">Concentus</a> — pure managed C# with no native deps, so the self-contained <code>muxit-win-x64.zip</code> release stays single-binary clean. On the wire, sustained audio drops from ~1 Mbit/s (base64 PCM16) to ~100–200 kbit/s.</p><ul><li><strong>AudioSynth driver</strong> bumped to <strong>1.4.0</strong>. New stream chunk shape: <code>{ &quot;op&quot;: &quot;chunk&quot;, &quot;data&quot;: &quot;&lt;base64 opus packet&gt;&quot;, &quot;sampleRate&quot;: 48000, &quot;channels&quot;: 1, &quot;format&quot;: &quot;opus&quot;, &quot;frameSize&quot;: 960 }</code>. Internal <code>ToneGenerator</code> now renders at 48 kHz so the encoder gets samples at one of Opus&#39;s native input rates without resampling. Server-side NAudio playback (<code>output: &quot;server&quot; | &quot;both&quot;</code>) is unchanged.</li><li><strong><code>web-ui/src/audio/ServerAudioRenderer.js</code></strong> decodes Opus packets via the browser-native WebCodecs <code>AudioDecoder</code> (Chrome 94+, Safari 16.4+, Firefox 130+ — universal in 2026). Decoded <code>AudioData</code> is copied into a Web Audio <code>AudioBuffer</code> and scheduled on the same per-connector <code>nextStartTime</code> cursor as before, so the rest of the pipeline is unchanged. The legacy PCM16 path (<code>format: &quot;pcm16&quot;</code>, <code>pcm</code> field) remains as a fallback for any emitter that can&#39;t link Concentus.</li><li><strong><code>docs/audio-streaming.md</code></strong> updated: the remaining migration step is now binary WS frames (when JSON+base64 envelope overhead becomes the bottleneck or when video / file-transfer streams want the same binary pipeline). Until then, JSON-wrapped Opus stays as the simpler thing that works.</li></ul><h2 id="v0-25-28-—-may-4-2026" tabindex="-1">v0.25.28 — May 4, 2026 <a class="header-anchor" href="#v0-25-28-—-may-4-2026" aria-label="Permalink to &quot;v0.25.28 — May 4, 2026&quot;">​</a></h2><h3 id="fixes" tabindex="-1">Fixes <a class="header-anchor" href="#fixes" aria-label="Permalink to &quot;Fixes&quot;">​</a></h3><ul><li><strong>Remote browser tabs hung on &quot;disconnected&quot; after a server restart.</strong> A remote client stores its login token in <code>sessionStorage</code>, but the server&#39;s session table is in-memory — restarting the server invalidates every previously-issued token. The browser can&#39;t read the 401 returned by a failed WebSocket upgrade, so <code>useServer</code> would happily keep retrying with the dead token forever and the user had to do a hard refresh to recover. The fix is two-sided: <code>/api/auth/status</code> now reports <code>sessionValid</code> when the caller presents an <code>X-Auth-Token</code>, and both <code>App.jsx</code> (on mount) and <code>useServer.connect()</code> (before each WebSocket attempt) verify a stored token via that endpoint. When the server says the token is dead, the client clears <code>sessionStorage</code>, drops the cached token in <code>api-shim.js</code>, and dispatches <code>muxit-login-required</code> so the login page shows up automatically. <code>__muxitSetAuthToken(null)</code> now also removes the sessionStorage entry, so the api-shim cache and storage stay in sync.</li></ul><h2 id="v0-25-27-—-may-3-2026" tabindex="-1">v0.25.27 — May 3, 2026 <a class="header-anchor" href="#v0-25-27-—-may-3-2026" aria-label="Permalink to &quot;v0.25.27 — May 3, 2026&quot;">​</a></h2><h3 id="audio-is-now-a-first-class-driver-stream" tabindex="-1">Audio is now a first-class driver stream <a class="header-anchor" href="#audio-is-now-a-first-class-driver-stream" aria-label="Permalink to &quot;Audio is now a first-class driver stream&quot;">​</a></h3><p>Server-side drivers can now stream audio to every connected dashboard, alongside video and other stream kinds. AudioSynth gains a multi-mode <code>output</code> config (<code>server</code> | <code>browser</code> | <code>both</code>, default <strong><code>browser</code></strong>) that picks where the synthesised audio actually plays. <strong>Cross-platform fix as a side effect:</strong> AudioSynth used to be Windows-only via NAudio; in <code>browser</code> mode it now works on Linux and macOS hosts because the audio is rendered server-side and decoded in the browser via the Web Audio API. This is also the unifying primitive for upcoming server-side TTS connectors (chatterbox, cosyvoice, OpenAI TTS, ElevenLabs) — they will plug into the same path with no new infrastructure on the client side.</p><ul><li><strong>AudioSynth <code>output</code> config.</strong> New connector field with three values: <ul><li><code>browser</code> <em>(default)</em> — every connected dashboard plays the audio. Multi-client by construction (broadcast through the existing per-client stream subscription model). Cross-platform.</li><li><code>server</code> — original v1.2 NAudio path, unchanged. Still Windows-only.</li><li><code>both</code> — runs the two paths in parallel; the local-output half fails silently on non-Windows hosts so the streaming half always works.</li></ul></li><li><strong>Wire format.</strong> Per-chunk JSON in the <code>data</code> field of <code>stream.data</code>: <code>{ &quot;op&quot;: &quot;chunk&quot;, &quot;pcm&quot;: &quot;&lt;base64-pcm16&gt;&quot;, &quot;sampleRate&quot;: 44100, &quot;channels&quot;: 1, &quot;format&quot;: &quot;pcm16&quot; }</code>. A <code>{ &quot;op&quot;: &quot;stop&quot; }</code> frame is emitted on cancellation so dashboards drop their queued audio. Real-time pacing (~50 ms chunks) so a client can schedule chunks back-to-back without running its jitter buffer dry.</li><li><strong><code>web-ui/src/audio/ServerAudioRenderer.js</code></strong> — singleton on the dashboard side. Lazily creates an <code>AudioContext</code>, primes it on the first user gesture, decodes incoming PCM16 frames to <code>AudioBuffer</code>s, and schedules them per-connector with a <code>nextStartTime</code> cursor so chunks for the same source play seamlessly. Different connectors play independently and may overlap. <strong>Not</strong> TTS-specific; any future driver that emits an <code>audio</code> stream goes through this single playback path.</li><li><strong><code>web-ui/src/hooks/useServerAudio.js</code></strong> — auto-subscribes to every connector that declares an <code>audio</code> stream (via <code>schema.streams</code>). Mounted in <code>Shell.jsx</code> and <code>MobileShell.jsx</code>.</li><li><strong><code>web-ui/src/hooks/useServer.js</code></strong> — emits a per-chunk <code>muxit-stream-data</code> window event in addition to the React-state path. Audio chunks need every frame; the existing <code>requestAnimationFrame</code> coalescing on <code>streamData</code> is right for video (only the latest matters) but wrong for continuous audio.</li><li><strong><code>script.say()</code> is unchanged</strong> — still routes through the browser&#39;s Web Speech API via the FIFO <code>TtsController</code>. For server-side voices the canonical pattern is explicit: <code>connector(&#39;chatterbox&#39;).speak(&quot;...&quot;)</code>. No implicit re-routing of <code>say()</code>, no global config field that would change its behaviour invisibly.</li><li><strong>TODO documented:</strong> raw PCM16-in-base64 trades bandwidth for simplicity (~700 kbit/s per active stream). Migration plan to Opus + binary WS frames + MSE lives in <code>docs/audio-streaming.md</code>. The JSON wrapper keeps <code>format</code> as a discriminator so adding <code>format: &quot;opus&quot;</code> later is forward-compatible.</li></ul><h2 id="v0-25-26-—-may-3-2026" tabindex="-1">v0.25.26 — May 3, 2026 <a class="header-anchor" href="#v0-25-26-—-may-3-2026" aria-label="Permalink to &quot;v0.25.26 — May 3, 2026&quot;">​</a></h2><h3 id="text-to-speech-rewrite" tabindex="-1">Text-to-speech rewrite <a class="header-anchor" href="#text-to-speech-rewrite" aria-label="Permalink to &quot;Text-to-speech rewrite&quot;">​</a></h3><p>The dashboard&#39;s TTS pipeline was rewritten around a single shared controller (<code>web-ui/src/tts/TtsController.js</code>) with a strict FIFO queue and a pluggable provider abstraction. Previous symptoms — &quot;TTS sometimes works, sometimes doesn&#39;t, and clicking the speaker button releases a buffer of accumulated speech&quot; — came from <code>useTTS</code> (chat) and <code>useScriptSpeech</code> (script <code>say()</code>) writing directly to <code>window.speechSynthesis</code> without coordination, plus a gesture gate that accumulated arbitrary callbacks until any user click drained them all at once. The new controller serialises every utterance through one worker, primes the speech engine on the first user gesture once and never queues callbacks outside the job FIFO, so a late click can never release a backlog.</p><ul><li><strong><code>web-ui/src/tts/TtsController.js</code></strong> — singleton with FIFO queue, single async worker, gesture priming, per-source filtered <code>stop()</code> (<code>stop({ source: &#39;chat&#39; })</code> cancels chat without stopping a script <code>say()</code>), and provider switching at runtime.</li><li><strong><code>web-ui/src/tts/providers/BrowserProvider.js</code></strong> — Web Speech API wrapper conforming to a small <code>speak(job) → Promise / cancel() / getVoices()</code> contract. Future providers (chatterbox, cosyvoice, OpenAI, ElevenLabs) plug in without touching consumers.</li><li><strong><code>web-ui/src/hooks/useTts.js</code></strong> replaces <code>useTTS.js</code>; <code>useScriptSpeech.js</code> now routes through the controller. <code>ttsGesture.js</code> is deleted — the gate logic lives inside the controller and no longer queues callbacks.</li><li><strong><code>SettingsEditor/VoiceSection.jsx</code> &quot;Test&quot;</strong> button now plays through the controller too, so it shares the same priming and queue with the rest of the system.</li></ul><h3 id="emotion-hint-on-say" tabindex="-1">Emotion hint on <code>say()</code> <a class="header-anchor" href="#emotion-hint-on-say" aria-label="Permalink to &quot;Emotion hint on \`say()\`&quot;">​</a></h3><p>Scripts can now pass an emotion hint: <code>say(&quot;Calibration done!&quot;, { emotion: &quot;excited&quot; })</code>. The hint flows through <code>script.say</code> WebSocket events (new <code>emotion</code> field, <code>null</code> when omitted) and is applied by the active TTS provider. The browser provider approximates with rate/pitch tweaks for a small built-in vocabulary (<code>excited</code>, <code>happy</code>, <code>sad</code>, <code>whisper</code>, <code>serious</code>); server-side providers added in a later release will honour the field natively.</p><h2 id="v0-25-25-—-may-2-2026" tabindex="-1">v0.25.25 — May 2, 2026 <a class="header-anchor" href="#v0-25-25-—-may-2-2026" aria-label="Permalink to &quot;v0.25.25 — May 2, 2026&quot;">​</a></h2><h3 id="features-8" tabindex="-1">Features <a class="header-anchor" href="#features-8" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>ONVIF driver gains software digital zoom.</strong> New writable properties <code>digitalZoom</code> (1.0–16.0), <code>zoomX</code> and <code>zoomY</code> (each <code>-1..1</code>) crop a <code>1/zoom × 1/zoom</code> window of the live frame and rescale it back to the camera&#39;s native resolution with bilinear interpolation, so the live <code>video</code> stream and any active recording keep their nominal <code>resolution</code> while showing the cropped region. Implemented as <code>OnvifDriver.ApplyDigitalZoom</code>, called per frame after <code>ApplyFlips</code> and before the recorder + JPEG encode — both the continuous capture loop and the <code>snapshot()</code> paths (live + cold-open) pick it up. The <code>(zoomX, zoomY)</code> offset is normalized against the <em>available</em> pan range at the current zoom level (<code>±1</code> puts the crop edge flush with the image edge), so the zoomed window can never extend outside the source frame regardless of zoom level — no clamping logic needed downstream. Coordinates apply on the <em>displayed</em> image, after <code>flipH</code> / <code>flipV</code>, so they always match what dashboards and AI vision pipelines see. Useful for AI tasks where you want to inspect a region without driving optical PTZ — software zoom takes effect on the next frame, not after a multi-second move-and-settle. Stored as <code>volatile float</code> so reads/writes from the capture thread and ws-handler thread don&#39;t need a lock. The starter template gains <code>zoomReset</code> / <code>zoomCenter2x</code> example methods to demonstrate the property syntax.</li></ul><h2 id="v0-25-24-—-may-2-2026" tabindex="-1">v0.25.24 — May 2, 2026 <a class="header-anchor" href="#v0-25-24-—-may-2-2026" aria-label="Permalink to &quot;v0.25.24 — May 2, 2026&quot;">​</a></h2><h3 id="startup-log-cleanup" tabindex="-1">Startup log cleanup <a class="header-anchor" href="#startup-log-cleanup" aria-label="Permalink to &quot;Startup log cleanup&quot;">​</a></h3><p>Boot output was a wall of unrelated lines (built-in driver names, half-related warnings, network diagnostics that ran before Kestrel had bound). Same content, now grouped under <code>── Built-in drivers ──</code>, <code>── Security ──</code>, <code>── Network ──</code>, <code>── Extension drivers ──</code>, <code>── Services ──</code> (and <code>── Licensing ──</code> when there&#39;s something to report). Built-in drivers collapsed to one line listing their names. The &quot;Driver registry: N drivers&quot; tail is now <code>N extension driver(s) loaded · N total in registry</code>, printed under the extension scan. Auth-token / editor-mode / HTTPS / scan results route through a single <code>StartupBanner</code> helper (<code>MuxitServer/Logging/StartupBanner.cs</code>) for consistent ✓ / ⚠ / ✗ prefixes.</p><h3 id="fixes-1" tabindex="-1">Fixes <a class="header-anchor" href="#fixes-1" aria-label="Permalink to &quot;Fixes&quot;">​</a></h3><ul><li><strong>MailKit security advisory (NU1902 / GHSA-9j88-vvj5-vhgr).</strong> Bumped <code>MailKit</code> from 4.7.1.1 to 4.8.0 in <code>MuxitServer.csproj</code>. The warning was also being re-printed at <em>every</em> <code>node start.js server</code> because <code>start.js</code> ran an explicit <code>dotnet build</code> and then <code>dotnet run</code>, which does its own implicit incremental build and re-prints the same NuGet warnings. <code>start.js</code> now passes <code>--no-build</code> to <code>dotnet run</code> whenever the explicit build succeeded.</li><li><strong>Capability scan never worked for any free driver.</strong> <code>DllDriverLoader.AuditCapabilitiesDetailed</code> built its <code>MetadataLoadContext</code> resolver from the .NET runtime dir + the driver&#39;s own dir, but <code>Muxit.Driver.Sdk.dll</code> lives in the host&#39;s <code>AppContext.BaseDirectory</code> — so every scan failed with <code>Could not find assembly &#39;Muxit.Driver.Sdk&#39;</code> and the audit reported &quot;scan failed&quot; instead of capabilities. Added <code>AppContext.BaseDirectory</code> to the resolver paths; AudioSynth / Midi / GenericScpi now report their actual capability set (FileSystem / Network / Process / …).</li><li><strong>Auth token &quot;denied&quot; then &quot;written&quot; contradiction.</strong> On Windows, <code>File.WriteAllText</code> fails with <code>Access denied</code> when overwriting an existing file that has the Hidden attribute set — which is the attribute <em>we</em> set on the previous run. The catch printed a Warning, then we unconditionally printed <code>Auth token written to: …</code>, so every restart claimed both. <code>Program.cs</code> now clears the Hidden flag before writing and only prints the success line when the write actually succeeded.</li><li><strong>False &quot;No TCP listener detected on port N&quot; warning.</strong> <code>NetworkDiagnostics.GetBannerSummary()</code> was called inline before <code>app.RunAsync()</code>, so the listener probe ran before Kestrel had bound — every boot reported a phantom &quot;Kestrel may have failed to bind&quot; hint. Moved the diagnostics + the <code>Listening:</code> / <code>WebSocket:</code> lines into <code>lifetime.ApplicationStarted.Register</code> so they print after the bind has actually happened. Same callback also kicks off Phase 2 (extension scan), so <code>── Network ──</code> appears before <code>── Extension drivers ──</code>.</li><li><strong>Cryptic &quot;Method not found&quot; for SDK-drift drivers.</strong> A driver compiled against an older <code>Muxit.Driver.Sdk</code> (e.g. before the <code>Details</code> parameter was added to <code>PropertyDescriptor</code>) failed loading with the raw <code>MissingMethodException: Void Muxit.Driver.Sdk.PropertyDescriptor..ctor(System.String, System.String, System.String, System.String, System.String)</code>. <code>DllDriverHost.ScanAsync</code> now catches that case explicitly and prints <code>✗ &lt;file&gt;.dll — incompatible Muxit SDK (rebuild and repackage with the current SDK)</code>.</li></ul><h2 id="v0-25-23-—-may-2-2026" tabindex="-1">v0.25.23 — May 2, 2026 <a class="header-anchor" href="#v0-25-23-—-may-2-2026" aria-label="Permalink to &quot;v0.25.23 — May 2, 2026&quot;">​</a></h2><h3 id="features-9" tabindex="-1">Features <a class="header-anchor" href="#features-9" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>ONVIF driver pushes a <code>snapshot</code> stream per <code>snapshot()</code> call.</strong> The driver now declares a second stream channel, <code>snapshot</code>, and emits the same base64 JPEG on it that <code>snapshot()</code> returns to the caller — both code paths (live RTSP grab and cold-open temp connection). Bind a Canvas widget to <code>&lt;connector&gt;:snapshot</code> (mode <code>image</code>) and you see exactly what the AI/vision pipeline received, with no polling overhead and no extra hardware traffic. Solves the most common &quot;the AI&#39;s description doesn&#39;t match what&#39;s on screen&quot; debugging problem: the snapshot can lag the live view by 0.5–1.5 s due to the camera&#39;s H.264 GOP / B-frame settings, and the widget now makes that lag visible. Returning the value through <code>StreamEmitter</code> was preferred over a <code>lastSnapshot</code> property because polling a 200–500 KB string at the default 1-second cadence would burn ~500 KB/s of WS traffic for no gain — streams only push when the value is actually produced.</li></ul><h2 id="v0-25-22-—-may-2-2026" tabindex="-1">v0.25.22 — May 2, 2026 <a class="header-anchor" href="#v0-25-22-—-may-2-2026" aria-label="Permalink to &quot;v0.25.22 — May 2, 2026&quot;">​</a></h2><h3 id="features-10" tabindex="-1">Features <a class="header-anchor" href="#features-10" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>ONVIF driver gains absolute PTZ + status read.</strong> Added <code>ptzMoveAbsolute({ pan, tilt, zoom })</code> for driving the camera to a specific position (vs. <code>ptzMove</code>&#39;s relative nudge), and <code>ptzStatus</code> for reading the current <code>{ pan, tilt, zoom, panTiltState, zoomState }</code> — the move-state fields are useful for waiting until an absolute move has settled before issuing the next one. Implementation lives in <code>OnvifClient.AbsoluteMoveAsync</code> / <code>GetStatusAsync</code>; both go to the existing <code>_ptzUrl</code> SOAP endpoint and reuse the same WS-Security envelope as the rest of the PTZ surface. Cameras that don&#39;t implement absolute PTZ (some lower-end Profile S devices) return a SOAP fault — script accordingly with <code>gotoPreset</code> or relative moves as fallback. Coordinate space is camera-specific; most ONVIF cameras normalize each axis to <code>[-1, 1]</code>, but a handful use degrees or raw step counts — a quick <code>ptzStatus</code> call after a known position discloses what your camera returns. The misleading &quot;continuous-move velocities&quot; wording on <code>ptzMove</code> is also corrected — <code>RelativeMove</code> is a one-shot delta, the camera stops on its own, no <code>ptzStop</code> needed.</li></ul><h3 id="dashboards" tabindex="-1">Dashboards <a class="header-anchor" href="#dashboards" aria-label="Permalink to &quot;Dashboards&quot;">​</a></h3><ul><li><p><strong>Text Display widget supports multiline values and per-widget alignment.</strong> Strings containing <code>\\n</code> now render across multiple lines (was: collapsed to a single line by the default <code>white-space: normal</code>). The widget config panel gains an <code>align</code> toggle (Left / Center / Right) so multiline values can be left-aligned for log-style output without affecting other Text Display widgets on the same dashboard. Mobile view picks up the same fix — root cause was that <code>dashboard.css</code> was only imported by the desktop entry, so the new <code>white-space: pre-wrap</code> rule was missing on mobile; <code>MobileShell</code> now imports <code>dashboard.css</code> too.</p></li><li><p><strong>Dashboard editor stops squishing widgets.</strong> Switching into edit mode used to shrink the grid canvas by ~420 px (palette + config panel widths), changing every widget&#39;s aspect ratio. The grid now keeps its live-mode width while editing — the center pane scrolls horizontally if the panels would otherwise overlap the canvas. Single change in <code>DashboardEditor.jsx</code>; the existing <code>overflow: auto</code> on the center pane handles the scrollbar.</p></li></ul><h2 id="v0-25-21-—-may-1-2026" tabindex="-1">v0.25.21 — May 1, 2026 <a class="header-anchor" href="#v0-25-21-—-may-1-2026" aria-label="Permalink to &quot;v0.25.21 — May 1, 2026&quot;">​</a></h2><h3 id="fixes-2" tabindex="-1">Fixes <a class="header-anchor" href="#fixes-2" aria-label="Permalink to &quot;Fixes&quot;">​</a></h3><ul><li><strong>AI tool flow restored after upstream strict-mode change.</strong> The Muxit cloud proxy / OpenRouter started rejecting the follow-up turn after every tool call with <code>validation_error: Invalid request: Invalid input</code> — every chat that needed a tool went unrecoverable on the second LLM call. Diagnosed via a new <code>MUXIT_AI_DEBUG=1</code>-gated dump of the full upstream error body (<a href="https://github.com/MUXIT-IO/muxit-development/pull/411" target="_blank" rel="noreferrer">#411</a>): the OpenRouter generation log showed the first call (returning <code>tool_calls</code>) succeeded with status 200, and the follow-up call was rejected by the validator before reaching generation. The only structural difference between the two calls was the assistant message we appended carrying <code>content: null</code> together with <code>tool_calls</code>. OpenAI&#39;s public spec still allows that combination, but a strict-mode rollout late April 2026 along the proxy → OpenRouter → upstream path no longer accepts it. Fix in <code>AiSession.AddAssistantMessage</code> (<code>MuxitServer/AI/AiSession.cs</code>): emit <code>content: &quot;&quot;</code> instead of <code>content: null</code> whenever <code>tool_calls</code> is present. Empty string is universally accepted, including by the local-LLM chat-template bridges (LM Studio Gemma, llama.cpp) the original <code>null</code> was already working around. (<a href="https://github.com/MUXIT-IO/muxit-development/pull/412" target="_blank" rel="noreferrer">#412</a>)</li></ul><h3 id="diagnostics" tabindex="-1">Diagnostics <a class="header-anchor" href="#diagnostics" aria-label="Permalink to &quot;Diagnostics&quot;">​</a></h3><ul><li><strong><code>AiService.Streaming</code> logs the full upstream error body</strong> when <code>MUXIT_AI_DEBUG=1</code>. One <code>_logger.Info</code> line, runs before the structured-error parsing so the full body is captured even when extraction succeeds. Zero production-log noise; saved hours on the next upstream API change. (<a href="https://github.com/MUXIT-IO/muxit-development/pull/411" target="_blank" rel="noreferrer">#411</a>)</li></ul><h2 id="v0-25-20-—-may-1-2026" tabindex="-1">v0.25.20 — May 1, 2026 <a class="header-anchor" href="#v0-25-20-—-may-1-2026" aria-label="Permalink to &quot;v0.25.20 — May 1, 2026&quot;">​</a></h2><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h3><p>Second hardening pass against the 2026-04-30 audit. Closes the three remaining sprint-sized items that round out the &quot;if anything goes wrong, it doesn&#39;t stay quiet&quot; theme.</p><ul><li><p><strong>Logger and EventBridge redaction.</strong> <code>ServerLogger.Log</code> already ran <code>SecretRedactor.RedactMessage</code> on its input, but <code>script.output</code>, <code>agent.log</code>, <code>script.say</code>, and <code>connector.error</code> events emitted straight onto the bus and bypassed it. A user script that did <code>log.error(\\</code>Bearer leaked-token-…\`)<code>or a connector load failure that quoted raw</code>apiKey: &quot;…&quot;<code>landed in the browser console verbatim.</code>EventBridge<code>now wraps each leak-prone field through the redactor at broadcast time.</code>AuditLog<code>runs</code>SecretRedactor.RedactJson<code>on the</code>details<code>tree before serializing, so both</code>workspace/logs/audit.jsonl<code>and the</code>audit.log\` WS event lose secret-named cleartext (knowing <em>&quot;user X wrote field &#39;apiKey&#39;&quot;</em> suffices for forensics). (audit U16, <a href="https://github.com/MUXIT-IO/muxit-development/pull/406" target="_blank" rel="noreferrer">#406</a>)</p></li><li><p><strong>Tamper alarm on <code>safety.audit.enabled</code> flip.</strong> A single config flag silently disabled the entire audit trail; nothing in the existing flow recorded the disable event itself. <code>AuditLog</code> now subscribes to <code>config.changed</code>, compares to a thread-safe <code>_lastKnownEnabled</code>, and on every flip writes a stderr WARNING + a structured line to a separate <strong><code>workspace/logs/audit-meta.jsonl</code></strong> that ignores the enabled flag. Startup with audit off produces an immediate <code>audit-disabled-at-startup</code> entry; runtime flips produce <code>audit-disabled</code> / <code>audit-enabled</code> entries plus an <code>audit.tamper</code> event for any future UI banner. Gated on <code>IsLicensed</code> so free-tier servers (which never had audit) stay silent. (audit U9, <a href="https://github.com/MUXIT-IO/muxit-development/pull/407" target="_blank" rel="noreferrer">#407</a>)</p></li><li><p><strong>Per-WS rate limit on hardware/AI/marketplace calls.</strong> A leaked auth token (or runaway dashboard) could fire thousands of <code>connector.call</code> / <code>scripts.start</code> / <code>ai.chat</code> / <code>marketplace.install</code> per second. <code>MessageRouter</code> now enforces a sliding 1-second window per WebSocket on the gated set; over-budget returns <code>WS_RATE_LIMITED</code> with a slow-down message. Read-only / metadata calls (<code>state.subscribe</code>, <code>connector.list</code>, <code>connector.schema</code>, <code>safety.get</code>, <code>license.get</code>, …) are intentionally <strong>not</strong> gated — legit dashboards burst through them on connect. Default 50/sec per client, override via <code>security.rateLimit.callsPerSec</code> in <code>server.json</code> (clamped to ≥ 5 so a misconfiguration can&#39;t brick the server). (audit A9, <a href="https://github.com/MUXIT-IO/muxit-development/pull/408" target="_blank" rel="noreferrer">#408</a>)</p></li></ul><h3 id="reverts" tabindex="-1">Reverts <a class="header-anchor" href="#reverts" aria-label="Permalink to &quot;Reverts&quot;">​</a></h3><ul><li><strong>U5 — AI confirmation user-turn gate (<a href="https://github.com/MUXIT-IO/muxit-development/pull/402" target="_blank" rel="noreferrer">#402</a>) reverted (<a href="https://github.com/MUXIT-IO/muxit-development/pull/410" target="_blank" rel="noreferrer">#410</a>).</strong> Reverted while diagnosing a chat-breaking <code>validation_error: Invalid input</code> after every tool call. <strong>Subsequent investigation traced the actual cause to an unrelated upstream OpenAI/OpenRouter strict-mode rollout</strong> that rejected <code>content: null</code> on assistant messages with <code>tool_calls</code> — fixed in v0.25.21 (<a href="https://github.com/MUXIT-IO/muxit-development/pull/412" target="_blank" rel="noreferrer">#412</a>). U5&#39;s logic itself was never proven faulty, only untested against the AI flow. Re-attempt tracked in the audit follow-up — needs a stub-LLM provider in the test harness before re-merging so a real regression can be told apart from another upstream coincidence.</li></ul><p>The remaining audit items (<code>B1</code>/<code>B2</code> server-side license signing, <code>U1</code> secret vault, <code>U11</code>/<code>U12</code>/<code>U13</code>/<code>U14</code>/<code>U15</code>, <code>A2</code>–<code>A8</code>, plus U5 re-attempt) are tracked in <code>internal-docs/operations/audits/2026-04-30-followup.md</code>.</p><h2 id="v0-25-19-—-may-1-2026" tabindex="-1">v0.25.19 — May 1, 2026 <a class="header-anchor" href="#v0-25-19-—-may-1-2026" aria-label="Permalink to &quot;v0.25.19 — May 1, 2026&quot;">​</a></h2><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to &quot;Security&quot;">​</a></h3><p>Hardening pass following the 2026-04-30 internal security audit (<code>internal-docs/operations/audits/2026-04-30-security-audit.md</code>). Seven coupled fixes that close the immediate &quot;trust boundaries cross the DLL layer&quot; and &quot;browser-side cross-origin / secret leakage&quot; classes.</p><ul><li><p><strong>Premium driver signature is now mandatory at load time.</strong> <code>DllDriverHost.ScanAsync</code> performs the <code>DriverSignature.IsOfficialDriver</code> check <strong>before</strong> <code>DllDriverLoader.Load</code> runs <code>Activator.CreateInstance</code>, so an unsigned DLL in a <code>premium</code>-category package never gets to execute its constructor in the host process. Skipped DLLs surface via the <code>driver.diagnostics</code> event (<code>PREMIUM_NOT_SIGNED</code>, hard severity) so the Extensions panel still shows the rejection. <code>lib/driver-manager/build.js</code> now fails the build instead of silently shipping unsigned premium packages — set <code>MUXIT_ALLOW_UNSIGNED_PREMIUM=1</code> only for local dev builds. (audit B3/B4, <a href="https://github.com/MUXIT-IO/muxit-development/pull/399" target="_blank" rel="noreferrer">#399</a>)</p></li><li><p><strong><code>RequiresSafetyGates=false</code> only honoured on signed drivers.</strong> <code>DllDriverLoader.Load</code> previously read <code>[assembly: RequiresSafetyGates(false)]</code> from any DLL and propagated it straight through <code>ConnectorInstance</code> so the safety gate became a no-op. Now the opt-out is gated on <code>DriverSignature.IsOfficialDriver</code>; an unsigned DLL that requests it gets the flag forced back to <code>true</code> plus a stderr warning. Closes the &quot;any DLL bypasses every limit/confirm/audit row&quot; path. (audit B5/U8, <a href="https://github.com/MUXIT-IO/muxit-development/pull/398" target="_blank" rel="noreferrer">#398</a>)</p></li><li><p><strong><code>connector.schema</code> no longer echoes secret-named <code>driverConfig</code> values.</strong> Fields whose key matches the existing <code>SecretRedactor.SecretKeyPattern</code> (<code>apiKey</code>, <code>password</code>, <code>token</code>, <code>bearer</code>, <code>credential</code>, <code>auth_key</code>, <code>private_key</code>, <code>secret</code>) come back as <code>{ value: null, secret: true, hasValue: bool }</code>. Editing still works through the per-key <code>connector.driverConfig.set</code> endpoint, which only takes the single key being changed. UI work to render the new shape as &quot;Set / Replace&quot; is a follow-up. (audit U3, <a href="https://github.com/MUXIT-IO/muxit-development/pull/396" target="_blank" rel="noreferrer">#396</a>)</p></li><li><p><strong>WebSocket upgrade rejects cross-origin requests.</strong> Browsers will happily open a WS from any page to <code>ws://127.0.0.1:8765</code> as long as the auth token is present. <code>WebSocketHandler.HandleAsync</code> now compares <code>Origin</code> against <code>http(s)://&lt;Request.Host.Value&gt;</code>; non-browser clients (CLI, MCP, Python) don&#39;t send Origin and stay gated by the auth-token check alone. Closes the realistic DNS-rebinding / token-exfil-via-XSS attack vectors. (audit U6, <a href="https://github.com/MUXIT-IO/muxit-development/pull/397" target="_blank" rel="noreferrer">#397</a>)</p></li><li><p><strong>Editor <code>open-with</code> extension whitelist.</strong> <code>/api/editor/open-with</code> previously called <code>Process.Start</code> with <code>UseShellExecute=true</code> on Windows and no extension check, so a workspace script that dropped a <code>.bat</code>/<code>.cmd</code>/<code>.vbs</code>/<code>.lnk</code> could trigger arbitrary code execution as the server user once the user clicked &quot;open&quot;. The handler now whitelists document / image / video / audio / plain-text extensions only; everything else returns 415. macOS / Linux paths switch from string interpolation to <code>ProcessStartInfo.ArgumentList</code> (defence in depth). <code>RevealInExplorer</code> got the same <code>ArgumentList</code> treatment. Editor mode remains opt-in (<code>security.editorMode=true</code>). (audit U7, <a href="https://github.com/MUXIT-IO/muxit-development/pull/400" target="_blank" rel="noreferrer">#400</a>)</p></li><li><p><strong><code>AiDebugDump</code> output is redacted.</strong> <code>MUXIT_AI_DEBUG=1</code> writes the assembled chat-request body (including connector instructions, <code>Authorization: Bearer …</code> headers in the messages array, and any <code>sk-…</code> tokens the LLM reflected back) to <code>workspace/logs/ai-last-request.json</code> — exactly the file users grab when reporting bugs. The dump now deep-clones via parse, runs a new <code>SecretRedactor.RedactJson</code> walker on the clone, and stamps <code>&quot;redacted&quot;: true</code> so it&#39;s obvious the file has been sanitised. <code>RedactJson</code> is reusable for the upcoming logger / event-bridge pass (audit U16). (audit U2, <a href="https://github.com/MUXIT-IO/muxit-development/pull/401" target="_blank" rel="noreferrer">#401</a>)</p></li><li><p><strong>AI confirmation key requires a fresh user turn.</strong> ⚠ <strong>Reverted in <a href="https://github.com/MUXIT-IO/muxit-development/pull/410" target="_blank" rel="noreferrer">#410</a></strong> during a chat-breaking outage. Subsequently traced (v0.25.21) to an upstream strict-mode rollout, <strong>not to U5 itself</strong>; U5&#39;s logic was never proven faulty, just never tested end-to-end. Original intent was to tag each pending entry with <code>_userMessageCount</code> at record time so AI can&#39;t auto-confirm its own retries within a single turn. Re-attempt blocked on stub-LLM test harness. (audit U5, <a href="https://github.com/MUXIT-IO/muxit-development/pull/402" target="_blank" rel="noreferrer">#402</a>)</p></li><li><p><strong>Default-deny cross-connector access for connector configs.</strong> A connector config running in V8 could call <code>connector(&quot;any-name&quot;)</code> and reach into any other loaded connector — read state, invoke methods, exfiltrate via <code>emit()</code>. Pre-deploy this is fine because the user installs every connector themselves; once a marketplace exists it becomes a supply-chain vector. The opt-in is now explicit:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  driver: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  allowedConnectors: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mqtt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;psu&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Self-reference is always allowed (composite/wrapper convention). Scripts under <code>workspace/scripts/</code> go through <code>ScriptHost</code> / <code>ConnectorBridge</code> directly and are unaffected — every demo / template script that calls <code>connector(&quot;test-device&quot;)</code> keeps working without any opt-in. Workspace-template connector configs don&#39;t cross-call today, so no migration. (audit U4, <a href="https://github.com/MUXIT-IO/muxit-development/pull/403" target="_blank" rel="noreferrer">#403</a>)</p></li></ul><p>The remaining audit items (<code>B1</code>/<code>B2</code> server-side license signing, <code>U1</code> secret vault, <code>U16</code> logger redaction, <code>A4</code> driver process isolation, et al.) are tracked in <code>internal-docs/operations/audits/2026-04-30-followup.md</code>.</p><h2 id="v0-25-18-—-april-30-2026" tabindex="-1">v0.25.18 — April 30, 2026 <a class="header-anchor" href="#v0-25-18-—-april-30-2026" aria-label="Permalink to &quot;v0.25.18 — April 30, 2026&quot;">​</a></h2><h3 id="features-11" tabindex="-1">Features <a class="header-anchor" href="#features-11" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><p><strong>WebhookNotifier <code>notify()</code> now attaches images.</strong> Pass <code>image</code> as either a base64 string (with or without a <code>data:</code> prefix) or an absolute file path — the camera drivers&#39; <code>snapshot()</code> output (base64 JPEG) flows through unchanged. For ntfy.sh the driver switches to PUT-binary mode (image bytes as the request body, message text moves to the <code>Message</code> header) so the push notification renders the snapshot inline. For Discord the driver sends <code>multipart/form-data</code> with <code>payload_json</code> + <code>files[0]</code> and the embed references the file via <code>attachment://&lt;filename&gt;</code> so it shows inside the embed. Generic flavor base64-encodes the image into the JSON body. Slack incoming webhooks can&#39;t upload files — the driver drops the image and emits a warn <code>delivery</code> event. Pattern (<code>MuxitServer/Drivers/BuiltIn/WebhookNotifierDriver.cs:144-200</code>):</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">connector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;webhook-ntfy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Robot status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Voor inspectie&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;info&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    image: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">connector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;webcam&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">snapshot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// base64 JPEG</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div></li><li><p><strong>Three more <code>notify()</code> args:</strong> <code>click</code> (URL — ntfy/Discord make the notification clickable), <code>tags</code> (ntfy emoji shortcodes like <code>&quot;warning,robot&quot;</code>), and a new config-level <code>markdown: true</code> flag for ntfy that adds the <code>Markdown: yes</code> header so the body renders as markdown in the ntfy app. The flag is ignored when an image is attached because ntfy doesn&#39;t render markdown alongside attachments.</p></li><li><p><strong>WebhookNotifier and EmailNotifier surfaced on the public marketplace page</strong> (<code>website/src/pages/marketplace.astro</code>) under built-ins. One-liner each, naming the concrete destinations (ntfy / Discord / Slack for the webhook; Gmail App Password / Resend / M365 / local relay for e-mail) so the marketing surface matches what ships out of the box.</p></li></ul><h3 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><strong>E-mail without typing your password.</strong> The &quot;raw password in config&quot; footgun was the most-cited friction point on the EmailNotifier — users (rightly) push back on dropping their Google password into a workspace file. The connector template (<code>workspace-template/connectors/email.js</code>) is now a four-path pick-list with all blocks commented except Gmail App Password: alongside Gmail it shows Microsoft 365, Resend (API key as <code>password</code>), and a no-auth local relay. Header and <code>ai.instructions</code> make explicit that <code>password</code> is never the user&#39;s real e-mail password — it&#39;s an App Password, an API key, or no credential at all. The Notifications guide (<a href="./../guides/notifications.html">guides/notifications.md</a>) gains a &quot;E-mail without typing your password&quot; table covering Gmail / M365 / Resend / SendGrid / Mailgun / Postmark / Brevo / SES / local relay, and explains why a transactional API service is usually the right call for any lab sending more than a handful of mails a day.</li><li><strong>ntfy &quot;raw JSON&quot; pitfall</strong> documented in both the <a href="./drivers/webhook-notifier.html#ntfy">WebhookNotifier reference</a> and the Notifications guide. The fix is almost always one of three things: calling <code>post()</code> instead of <code>notify()</code> (post bypasses flavor reshaping), <code>flavor: &quot;generic&quot;</code> against an ntfy URL, or expecting markdown without the <code>markdown: true</code> config flag. The webhook-ntfy connector template gains a header note steering users at <code>notify()</code> and an example block that shows tags, click URL, and image attachment in their idiomatic forms.</li></ul><h2 id="v0-25-17-—-april-30-2026" tabindex="-1">v0.25.17 — April 30, 2026 <a class="header-anchor" href="#v0-25-17-—-april-30-2026" aria-label="Permalink to &quot;v0.25.17 — April 30, 2026&quot;">​</a></h2><h3 id="features-12" tabindex="-1">Features <a class="header-anchor" href="#features-12" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Two new built-in drivers ship outbound notifications.</strong> Long-running scripts and threshold breaches can now reach the user out-of-band — the existing real-time WebSocket plumbing only helps when the browser tab is open. Both drivers live in the <code>Communication</code> group alongside <code>MqttBridge</code> and follow the same registration shape (<code>MuxitServer/Core/MuxitHost.cs:69-70</code>). <ul><li><strong><code>EmailNotifier</code></strong> (<code>MuxitServer/Drivers/BuiltIn/EmailNotifierDriver.cs</code>) sends SMTP via <a href="https://github.com/jstedfast/MailKit" target="_blank" rel="noreferrer">MailKit</a> — handles STARTTLS, implicit-SSL, plain transports, and SASL auth out of the box, so Gmail App Passwords, Microsoft 365, and local relays all work without per-provider plumbing. The <code>send</code> action takes <code>{ to, subject, body, html?, cc?, bcc?, from?, attachments? }</code>; when both <code>body</code> and <code>html</code> are supplied the message goes out as <code>multipart/alternative</code> with the plain version as fallback. Connection is opened, used, and closed per send — fine for the typical &quot;few mails per run&quot; rate. <code>RequiresSafetyGates = true</code>, so the first send to a new recipient triggers a confirmation prompt; subsequent sends to the same address pass through.</li><li><strong><code>WebhookNotifier</code></strong> (<code>MuxitServer/Drivers/BuiltIn/WebhookNotifierDriver.cs</code>) POSTs to a fixed URL. A <code>flavor</code> config key reshapes the body for popular targets so scripts call a uniform <code>notify(title, body, level)</code> regardless of where the message lands: <code>ntfy</code> sends <code>text/plain</code> + <code>Title</code>/<code>Priority</code> headers (level → <code>default</code>/<code>low</code>/<code>high</code>/<code>urgent</code>); <code>discord</code> builds a webhook embed with color per level (info=blue, warn=orange, error=red, success=green); <code>slack</code> builds a <code>text</code> + colored attachment; <code>generic</code> posts JSON <code>{ title, body, level, timestamp }</code> for n8n / Zapier / Make / custom backends. <code>post(payload)</code> bypasses flavor reshaping for full control. <code>RequiresSafetyGates = false</code> because the URL is fixed at config time — there&#39;s no runtime target choice to gate.</li></ul></li><li><strong>Four workspace-template connector starters</strong> ship with the new drivers (<code>workspace-template/connectors/email.js</code>, <code>webhook.js</code>, <code>webhook-ntfy.js</code>, <code>webhook-discord.js</code>), each preloaded with the right <code>flavor</code> and a placeholder URL or credential block. Drop in your value and the connector is live on next workspace reload.</li><li><strong>Two demo scripts</strong> (<code>workspace-template/scripts/demo/15-notifications.js</code>, <code>16-threshold-alert.js</code>) show the canonical patterns: the first runs a fake long operation, sends a short webhook push, then mails an AI-generated summary; the second polls a property with hysteresis-based debounce so a sustained high reading produces one alert, not one per sample (re-arms after the value drops 5 units below the threshold).</li><li><strong>New documentation:</strong> <a href="./../guides/notifications.html">Notifications &amp; Alerts guide</a> walks through channel selection, the three core use cases (run finished, threshold breach, hardware fault), severity-level mapping per flavor, and safety-gate behavior. Per-driver references at <a href="./drivers/email-notifier.html">drivers/email-notifier.md</a> and <a href="./drivers/webhook-notifier.html">drivers/webhook-notifier.md</a> cover properties, actions, streams, and config options. Sidebar entries added under Guides and Built-in Drivers (<code>docs-site/.vitepress/config.mts</code>).</li></ul><h2 id="v0-25-16-—-april-30-2026" tabindex="-1">v0.25.16 — April 30, 2026 <a class="header-anchor" href="#v0-25-16-—-april-30-2026" aria-label="Permalink to &quot;v0.25.16 — April 30, 2026&quot;">​</a></h2><h3 id="fixes-3" tabindex="-1">Fixes <a class="header-anchor" href="#fixes-3" aria-label="Permalink to &quot;Fixes&quot;">​</a></h3><ul><li><strong>Editor running-line highlight now actually moves with the script.</strong> The Phase-2 execution-line tracer was effectively dead end-to-end — three independent bugs lined up to make it never produce a visible highlight: <strong>(1)</strong> <code>__traceCallSite()</code> in <code>SandboxScript.cs</code> walked <code>new Error().stack</code> looking for <code>&lt;anonymous&gt;:LINE:COL</code> and picked the first frame whose adjusted line was ≥ 1, but <code>SandboxScript.InitCode</code> is loaded via a <em>separate</em> <code>engine.Execute(...)</code> call so its frames also rendered as <code>&lt;anonymous&gt;</code> — the regex matched the helper&#39;s own <code>new Error()</code> line (~line 31 of InitCode) before ever reaching the user frame. Fix: evaluate the wrapped user script with <code>DocumentInfo(&quot;muxit:user&quot;)</code> (<code>MuxitServer/Scripts/ScriptEngine.cs</code>) so user frames render as <code>muxit:user:LINE:COL</code>, and key <code>__traceCallSite</code> and <code>StackLineRegex</code> / <code>AdjustStackLines</code> off that source name. The regex tolerates a <code> [temp-N]</code> suffix that ClearScript appends for transient docs in some versions. <strong>(2)</strong> Both effects in <code>EditorGroup.jsx</code> (the Monaco decoration and the <code>subscribeTrace</code> opt-in) gated on a strict <code>^scripts/.+\\.js$</code> regex, but the workspace explorer hands the editor paths like <code>workspace/scripts/foo.js</code>. Neither effect fired, so the trace subscriber count on the server stayed at zero, <code>__host.TraceEnabled</code> returned false, and <code>__traceCallSite</code> short-circuited before reporting a single line. Both effects now use <code>isScript()</code> (the project&#39;s own predicate, accepting any <code>*/scripts/*.js</code>), and the highlight key derives from <code>activeFile.name</code> to match the name the Run button passes to <code>server.runScript</code>. <strong>(3)</strong> <code>subscribeTrace()</code> in <code>useServer.js</code> was a no-op when issued before the WebSocket finished opening (<code>wsRef.current</code> was still null), and it never re-sent on reconnect. <code>ws.onopen</code> now resends the subscribe whenever <code>traceSubRef.current &gt; 0</code>. <code>docs/clearscript-quirks.md</code> quirk #9 updated to record the InitCode-vs-user-script frame disambiguation gotcha.</li></ul><h3 id="documentation-1" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-1" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><p><strong>MCP setup instructions corrected for end users.</strong> The previous AI guide (<a href="./../guides/ai.html">guides/ai.md</a>) and the in-app <strong>Settings → MCP</strong> panel (<code>web-ui/src/components/SettingsEditor/McpSection.jsx</code>) shipped three misleading snippets: (1) the <strong>Claude Code</strong> block claimed to be &quot;already configured in <code>.claude/.mcp.json</code>&quot; — wrong path (the actual project file is <code>.mcp.json</code> at the repo root) and wrong scope (it only applies when Claude Code is launched from inside this source checkout, not from a user&#39;s lab workspace); (2) the <strong>Claude Desktop</strong> snippet used <code>dotnet run --project /path/to/MuxitServer</code> which is a from-source invocation an end user with the published <code>muxit</code> binary can&#39;t run; (3) the <strong>ChatGPT</strong> block told users to paste <code>http://127.0.0.1:8765/mcp</code> directly into the connector dialog, but ChatGPT&#39;s connector UI rejects loopback / private-IP addresses as <em>&quot;unsafe url&quot;</em> and requires a publicly reachable HTTPS endpoint with a valid TLS cert — pasting the local URL just produces an <code>Error creating connector. unsafe url</code> toast every time. All three blocks now distinguish <em>installed Muxit</em> from <em>source checkout</em>: Claude Code shows a <code>claude mcp add muxit -- /path/to/muxit --mcp --workspace …</code> command for installed builds and keeps the existing <code>dotnet run</code> snippet as the source-tree variant; Claude Desktop&#39;s snippet now points at the absolute path of the installed <code>muxit</code> binary; ChatGPT documents the public-HTTPS-tunnel workaround (Cloudflare Tunnel one-liner) and labels it as a constraint of ChatGPT, not Muxit. Both the docs page and the Settings UI panel were updated to stay in sync.</p></li><li><p><strong>ChatGPT-via-tunnel security warning.</strong> Because Muxit&#39;s HTTP middleware (<code>MuxitServer/Program.cs:786-865</code>) treats every loopback request as authenticated and a tunnel daemon (cloudflared / ngrok) running on the same host reaches Muxit <em>over loopback</em>, naively exposing the local <code>/mcp</code> endpoint over a public tunnel publishes the full hardware surface — read sensors, write properties, run scripts — with no credentials. The optional <code>security.remoteAccess</code> password gate is bypassed for loopback callers and uses a custom <code>X-Auth-Token</code> header that ChatGPT&#39;s connector can&#39;t send anyway, so it doesn&#39;t close the gap. Both the AI guide&#39;s ChatGPT section and the Settings UI&#39;s ChatGPT panel now lead with a stark warning and recommend acceptable mitigations: <strong>Tailscale Funnel</strong> restricted to your tailnet, <strong>Cloudflare Access</strong> in front of cloudflared, or short-lived per-session tunnels — and steer users with no tunnel infrastructure toward Claude Desktop / Claude Code, which connect locally over stdio with no exposure.</p></li><li><p><strong>AI features tier table footnote.</strong> The capability matrix at the top of <a href="./../guides/ai.html">guides/ai.md</a> advertised &quot;Claude Desktop / Claude Code / ChatGPT&quot; as the three supported MCP clients with no caveat; the ChatGPT-only public-HTTPS limitation is now flagged with an asterisk and a small footnote pointing at the new MCP Server section, and the <em>What you get on Free</em> paragraph now reads &quot;ChatGPT (with a tunnel — see below)&quot; so the marketing copy doesn&#39;t oversell the out-of-the-box ChatGPT experience.</p></li></ul><h2 id="v0-25-15-—-april-29-2026" tabindex="-1">v0.25.15 — April 29, 2026 <a class="header-anchor" href="#v0-25-15-—-april-29-2026" aria-label="Permalink to &quot;v0.25.15 — April 29, 2026&quot;">​</a></h2><h3 id="features-13" tabindex="-1">Features <a class="header-anchor" href="#features-13" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>ONVIF driver gains <code>flipH</code> / <code>flipV</code> orientation toggles.</strong> Both are new config options <em>and</em> live R/W properties on the driver, so cameras mounted upside-down or facing a mirror can be corrected without re-opening the RTSP stream. Flips are applied in <code>ApplyFlips()</code> (<code>MuxitServer/Drivers/BuiltIn/OnvifDriver.cs</code>) immediately after decode and <em>before</em> recorder feed + JPEG encode, so both the live <code>video</code> stream and any active <code>.mp4</code> recording reflect the flip. Implementation uses OpenCvSharp&#39;s <code>Cv2.Flip</code> with <code>FlipMode.Y</code> (horizontal mirror), <code>FlipMode.X</code> (vertical), or <code>FlipMode.XY</code> (180° rotation when both are set). No restart needed when toggling at runtime — the capture loop reads the volatile flags each iteration. Documented in <code>docs-site/reference/drivers/onvif.md</code>.</li></ul><h3 id="changes" tabindex="-1">Changes <a class="header-anchor" href="#changes" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>ONVIF capture pipeline tightened for lower latency.</strong> Two driver-side changes for the cameras whose 1–2 s end-to-end delay was bottlenecking the dashboard view: (1) <code>OPENCV_FFMPEG_CAPTURE_OPTIONS</code> now also sets <code>max_delay=0</code> and <code>reorder_queue_size=0</code>, disabling FFmpeg&#39;s frame-reordering and demuxer-delay buffers (the rest — <code>nobuffer</code>, <code>low_delay</code>, <code>framedrop=1</code>, <code>analyzeduration=0</code>, <code>probesize=32768</code> — was already there). (2) After opening <code>VideoCapture</code>, the driver now calls <code>_capture.Set(VideoCaptureProperties.BufferSize, 1)</code> to ask the FFmpeg backend for a single-frame internal buffer; backends that don&#39;t honor the request silently ignore it. The remaining 0.5–1.5 s of glass-to-screen delay is dominated by the camera&#39;s own H.264 encoder (GOP length, B-frames) and the browser&#39;s MJPEG decode pipeline — the <a href="./drivers/onvif.html#latency">ONVIF driver reference</a> now has a &quot;Latency&quot; section documenting the camera-side knobs (substream profile, shorter GOP, disable B-frames, wired LAN) that move the needle further.</li></ul><h2 id="v0-25-14-—-april-29-2026" tabindex="-1">v0.25.14 — April 29, 2026 <a class="header-anchor" href="#v0-25-14-—-april-29-2026" aria-label="Permalink to &quot;v0.25.14 — April 29, 2026&quot;">​</a></h2><h3 id="features-14" tabindex="-1">Features <a class="header-anchor" href="#features-14" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><p><strong>Dashboard knob widget now handles fractional values and direct entry.</strong> Previously the knob always rounded display to one decimal (<code>current.toFixed(1)</code>) and used a step-rounding pass that left float noise (<code>Math.round(0.15 / 0.05) * 0.05</code> = <code>0.15000000000000002</code>), so a knob configured <code>min:0, max:2.6, step:0.05</code> looked broken. The widget now derives display precision from <code>step</code> (e.g. <code>step:0.05</code> → 2 decimals, <code>step:1</code> → 0 decimals), strips post-rounding float noise via <code>toFixed(decimals)</code>, and renders the value as an editable input — click it to type a specific value and press Enter (or click out) to commit; Escape cancels. Out-of-range or off-step entries are clamped/quantized on commit. Implemented in <code>web-ui/src/components/dashboard/widgets/KnobWidget.jsx</code> with new <code>decimalsFromStep()</code> and <code>quantize()</code> helpers and matching styles in <code>web-ui/src/styles/dashboard.css</code>.</p></li><li><p><strong>Numeric config fields (<code>min</code>, <code>max</code>, <code>step</code>, <code>duration</code>, <code>yMin</code>, <code>yMax</code>, <code>sendValue</code>, <code>lineWidth</code>, <code>maxLines</code>) now accept decimal entry.</strong> The widget config panel previously parsed every keystroke through <code>Number(val)</code>, which meant typing <code>0.05</code> was eaten alive: after <code>0.</code> the parser collapsed it to <code>0</code> and re-rendered the input, swallowing the in-progress decimal point. A new <code>NumericField</code> component (<code>web-ui/src/components/dashboard/WidgetConfigPanel.jsx</code>) keeps the in-progress text in local state and only commits a parsed number on blur or Enter. Escape reverts; invalid input falls back to the last good value. The fix unblocks fractional knob configuration end-to-end (the widget supports it; the config panel was the bottleneck).</p></li><li><p><strong>Dashboard editor exposes grid columns, row height, and layout-compaction mode in the right panel.</strong> A new &quot;Dashboard&quot; section (<code>web-ui/src/components/dashboard/WidgetConfigPanel.jsx</code> <code>DashboardSection</code>) sits at the top of the right-side editor panel and is visible whether or not a widget is selected. <strong>Columns</strong> is a 1–24 slider (default 12), <strong>Row height</strong> is a number input (px), and <strong>Layout</strong> toggles between <em>Auto-stack (compact)</em> (default — items pull up to fill whitespace, the previous behavior) and <em>Free placement</em> (preserves whitespace above widgets so users can leave intentional gaps). Free-placement mode swaps in <code>noCompactor</code> from react-grid-layout; new widgets dropped while in this mode get placed at the bottom of the existing layout (otherwise <code>y:Infinity</code> wouldn&#39;t resolve under noCompactor). All three settings persist in the dashboard JSON.</p></li><li><p><strong>Subtle gridlines while the dashboard is in edit mode.</strong> A new <code>.dashboard-grid.show-gridlines</code> style (<code>web-ui/src/styles/dashboard.css</code>) paints faint 1px lines at every column and row boundary using two <code>repeating-linear-gradient</code> backgrounds. The <code>--grid-col-step</code> and <code>--grid-row-step</code> CSS variables are computed in JS from the current <code>containerWidth</code>, <code>gridCols</code>, <code>rowHeight</code>, and the grid&#39;s actual margin/padding, so the lines align with cell boundaries exactly and shift live as the columns slider changes or the editor pane resizes. Lines are off in live (non-edit) mode.</p></li></ul><h2 id="v0-25-13-—-april-29-2026" tabindex="-1">v0.25.13 — April 29, 2026 <a class="header-anchor" href="#v0-25-13-—-april-29-2026" aria-label="Permalink to &quot;v0.25.13 — April 29, 2026&quot;">​</a></h2><h3 id="features-15" tabindex="-1">Features <a class="header-anchor" href="#features-15" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Editor word wrap is now toggleable from the View menu.</strong> Previously the Monaco editor was hard-coded to <code>wordWrap: &#39;off&#39;</code>, so long lines (CSV rows, log dumps, single-line connector configs) ran off-screen and required horizontal scrolling. The View menu now has a <strong>Word Wrap</strong> entry (also bound to <code>Alt+Z</code>) that flips the editor between <code>&#39;on&#39;</code> and <code>&#39;off&#39;</code>. The setting persists in the <code>muxit-layout-state</code> localStorage key alongside the other layout preferences, applies live across all editor groups, and is reflected with a checkmark in the View dropdown. Documented in <a href="./../guides/keyboard-shortcuts.html">guides/keyboard-shortcuts.md</a>.</li></ul><h3 id="changes-1" tabindex="-1">Changes <a class="header-anchor" href="#changes-1" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>The free tier is now limited to one active SCPI (<code>GenericScpi</code>) connector.</strong> The overall connector budget is unchanged (free=5, maker=10, pro+=unlimited), but a new <code>scpi-connectors</code> feature in the license registry sub-limits how many of those slots may be SCPI instruments — free=1, maker and above unlimited. Enforced in three places in <code>MuxitServer/Connectors/ConnectorManager.cs</code>: <code>ResolveEnabledSet</code> skips additional SCPI configs at startup (the first SCPI connector wins; the rest land in the disabled list with a <code>Not enabled (SCPI limit: 1 on Free tier)</code> reason), <code>AddConnector</code> rejects programmatic creation past the limit with <code>CONN_LIMIT_REACHED</code>, and <code>SetEnabledConnectorsAsync</code> validates the count when the user changes the enabled set from the UI. Match is by driver name (<code>GenericScpi</code>, case-insensitive) — connectors built on the JS <code>serial-monitor</code> driver or any TCP/serial transport that happens to send SCPI bytes are not affected, by design (the limit is on the convenience driver, not on raw byte traffic). Existing paid tiers are unchanged.</li></ul><h3 id="documentation-2" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-2" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><strong>Free vs Maker AI capabilities are now spelled out.</strong> The AI guide (<a href="./../guides/ai.html">guides/ai.md</a>) leads with a new <strong>AI features by tier</strong> section: a side-by-side comparison table covering MCP, Chat Panel, voice, <code>ai()</code>, AI-assisted SCPI authoring, Vision AI, Local LLM, and Autonomous Agents, followed by three short narrative blocks — <em>What you get on Free</em> (MCP plus your own AI client is a complete workflow for a one-person lab), <em>What Maker unlocks</em> (the things that live inside the Muxit window — chat, voice, <code>ai()</code> in scripts, AI-assisted SCPI, 500 credits/month), and <em>Beyond Maker</em> (Vision, Local LLM, Agents at Pro). The Script API reference (<a href="./script-api.html">reference/script-api.md</a>) had <code>ai()</code> mis-listed as Pro-tier — corrected to Maker, with a vision caveat for the <code>ai(prompt, image)</code> form (still Pro) and a pointer to MCP for Free users. The AI-assisted SCPI guide (<a href="./../guides/ai-scpi-authoring.html">guides/ai-scpi-authoring.md</a>) now opens with a tier note that points Free users to two equally good paths: hand-authoring with the declarative <code>GenericScpi</code> schema, or driving the same <code>probe_scpi_device</code> / <code>validate_connector_config</code> / <code>write_connector_config</code> tools from their own AI client over MCP. Marketing-side, the maker page restructures <em>AI you actually use</em> into a clear Free → Maker progression and reframes Local LLM as a Pro-tier feature, and the pricing page (<code>website/src/pages/pricing.astro</code>) now sells MCP on the Free card as a real workflow — <em>&quot;point Claude Desktop, Claude Code, or ChatGPT at Muxit and your AI client drives your hardware, runs scripts, and drafts connectors. Your provider, no Muxit credits.&quot;</em> The Maker card emphasises that built-in chat and voice live <em>in the Muxit window</em> (no external client to juggle), and the <em>AI credits — how they work</em> info block was rewritten as <strong>AI on Free vs paid plans</strong>, presenting MCP and Muxit AI as two first-class paths instead of burying MCP as a footer afterthought.</li></ul><h2 id="v0-25-11-—-april-28-2026" tabindex="-1">v0.25.11 — April 28, 2026 <a class="header-anchor" href="#v0-25-11-—-april-28-2026" aria-label="Permalink to &quot;v0.25.11 — April 28, 2026&quot;">​</a></h2><h3 id="documentation-3" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-3" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><strong>Script API docs and examples now lead with the <code>.file(path)</code> handle pattern.</strong> The Script Guide (<a href="./../guides/scripts.html">guides/scripts.md</a>), Script API Reference (<a href="./script-api.html">reference/script-api.md</a>), the FileAccess driver reference&#39;s &quot;Example Connector&quot; / &quot;Example Script&quot; sections (<a href="./drivers/file-access.html">reference/drivers/file-access.md</a>), the UI tour&#39;s CSV-charts blurb (<a href="./../getting-started/ui-tour.html">getting-started/ui-tour.md</a>), and the AI script-authoring system prompt (<code>MuxitServer/AI/Prompts/script-api.md</code>) were all written before v0.25.10 added handles, so every example still showed the path-on-every-call form (<code>files.writeText({ path: &#39;temps.csv&#39;, content: ... })</code>, <code>files.appendText({ path: &#39;temps.csv&#39;, content: ... })</code>). They now demonstrate the handle pattern as the recommended path: <code>const csv = files.file(&#39;temps.csv&#39;); csv.write(header); csv.append(row);</code>. Standalone actions are still documented in the FileAccess action table (and still work — positional args, object-arg, both unchanged) but the worked examples no longer teach the noisier form. The default workspace connector&#39;s <code>writeArray</code> helper (<code>workspace-template/connectors/fileaccess.js</code>) was also rewritten on the handle (<code>c.file(args.path).write(...)</code>) so the example custom method matches the docs.</li></ul><h2 id="v0-25-10-—-april-27-2026" tabindex="-1">v0.25.10 — April 27, 2026 <a class="header-anchor" href="#v0-25-10-—-april-27-2026" aria-label="Permalink to &quot;v0.25.10 — April 27, 2026&quot;">​</a></h2><h3 id="features-16" tabindex="-1">Features <a class="header-anchor" href="#features-16" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>FileAccess driver gains a path-bound <code>file(path)</code> handle for compact write/read syntax.</strong> Repeating the path on every call (<code>fa.writeText({ path: &quot;runs/log.csv&quot;, content: line })</code>) was the most-cited papercut in the script API, especially in append loops. The new <code>file(path)</code> action returns a <code>FileHandle</code> host object whose methods bind the path once: <code>const f = fa.file(&quot;runs/log.csv&quot;); f.write(header); f.append(row); f.read(); f.exists; f.size; f.info(); f.delete(); f.rename(&quot;runs/done.csv&quot;)</code>. The handle is a CLR object surfaced via ClearScript <code>[ScriptMember]</code> attributes, so the path-binding closures live on the .NET side and survive the script ↔ connector V8 boundary cleanly — pure-JS handles would have been stripped by <code>V8Interop.NormalizeJsValue</code>&#39;s function-discarding traversal. All operations delegate to the parent driver&#39;s existing path-resolution, size-limit, and <code>workspace.file.changed</code> plumbing, so handle calls obey the same sandbox rules as the standalone actions. Methods are synchronous (<code>f.write(...)</code> returns <code>&quot;OK&quot;</code>, no <code>await</code>) to match the rest of the connector proxy. The handle&#39;s <code>rename</code> updates itself in-place so subsequent calls operate on the moved file. Default connector config (<code>workspace-template/connectors/fileaccess.js</code>) and the FileAccess template advertise the new pattern in <code>ai.instructions</code> so the AI prefers it. Standalone actions (<code>writeText</code>, <code>readText</code>, <code>appendText</code>, …) are unchanged and still accept positional args via <code>MergePositionalArgs</code>. Reference: <code>docs-site/reference/drivers/file-access.md</code> &quot;File Handles&quot; section.</li></ul><h3 id="features-17" tabindex="-1">Features <a class="header-anchor" href="#features-17" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><p><strong>System prompts now have a <code>minimal</code> profile for local LLMs with 4–8K context windows.</strong> A new <code>ai.promptProfile</code> config key (<code>&quot;standard&quot;</code> default, <code>&quot;minimal&quot;</code> opt-in) controls how much of the platform context ships in the system message and tool-definition list. In <code>minimal</code> mode:</p><ul><li><strong>Connected Devices</strong> collapses to one line per connector (<code>- name (driver) — instructions</code>) plus a single hint to call <code>get_connector_schema(name)</code> when properties/actions are needed.</li><li><strong>Decision-tree fragment</strong> (<code>decision-tree.md</code> / <code>decision-simple.md</code>, ~1.5 KB) is skipped — short tool-description summaries already steer tool choice for the small models that need this profile.</li><li><strong>Safety-confirmation fragment</strong> swaps from the 3.3 KB worked-examples version to a 0.6 KB rule sheet (<code>safety-confirmation-minimal.md</code>). The chat gate still parks numeric writes and script-authoring; the model just learns the protocol from the short version. Skipped entirely when the active safety mode is <code>off</code>.</li><li><strong>Tool descriptions</strong> in the OpenAI function-calling list collapse to the first sentence of each tool&#39;s entry in <code>tool-descriptions.md</code>. <code>PromptRegistry.GetSectionShort</code> cuts at the first <code>.</code>/<code>!</code>/<code>?</code> that&#39;s followed by either end-of-string or whitespace + an uppercase letter, so decimals (<code>1.5</code>) and abbreviations (<code>e.g.</code>) don&#39;t trip it. <code>ToolRegistry.GetDefinitions(groups, minimal: true)</code> flips the lookup; <code>AiService</code> reads the profile from config and passes the flag through. Saves several KB across the ~50 registered tools — for the worst offenders (<code>run_script</code>, <code>run_code</code>, <code>probe_scpi_device</code>, <code>write_dashboard</code>, <code>read_manual_pages</code>) the first sentence is what the model needs to call them; recovery / argument detail is something it can rediscover from failures or the JSON schema.</li></ul><p>On a typical 5-connector lab the system message drops from ~13 KB to ~1.5 KB and the tool-definitions list drops from ~14 KB to ~3 KB. The model trades occasional tool round-trips for ~7× total shrink — right tradeoff for Ollama / LM Studio backends, wrong tradeoff for cloud models, hence opt-in. Documented in <code>docs-site/reference/configuration.md</code> and <code>docs-site/guides/ai.md</code>.</p></li></ul><h3 id="changes-2" tabindex="-1">Changes <a class="header-anchor" href="#changes-2" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>MCP tool-result JSON keeps non-ASCII characters literal.</strong> <code>MuxitServer/Mcp/McpJsonOptions.{Default,Indented}</code> now set <code>Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping</code>, so em-dashes (<code>—</code>), units (<code>°</code>, <code>µ</code>, <code>Ω</code>), <code>+</code>, <code>&lt;</code>, and similar safe punctuation appear as one literal byte instead of expanding to a six-byte <code>\\uXXXX</code> escape. Affects every MCP tool result the AI sees (notably <code>list_connectors</code>, <code>get_connector_schema</code>, <code>read_property</code> results that quote device strings) and the device-schema JSON the agent-mode prompt embeds via <code>SystemPromptBuilder.BuildForAgent</code>. The &quot;unsafe&quot; name refers to embedding JSON in HTML/JS, which we never do for tool results — only the LLM consumes them. Saves a few percent on text-heavy results and several KB on schemas with many <code>—</code> separators.</li></ul><h2 id="v0-25-8-—-april-25-2026" tabindex="-1">v0.25.8 — April 25, 2026 <a class="header-anchor" href="#v0-25-8-—-april-25-2026" aria-label="Permalink to &quot;v0.25.8 — April 25, 2026&quot;">​</a></h2><h3 id="bug-fixes-4" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-4" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Hitting the model&#39;s context window now shows a friendly chat message with a &quot;Start new chat&quot; button instead of a raw HTTP error.</strong> The streaming SSE error parser in <code>AiService.Streaming.cs</code> now classifies upstream errors against a marker list (<code>context_length_exceeded</code>, <code>prompt_too_long</code>, <code>request too large</code>, <code>context window</code>, <code>exceeds the context</code>, …) and throws a <code>MuxitException</code> with the new <code>AI_CONTEXT_LENGTH_EXCEEDED</code> error code. The web client&#39;s <code>useChat</code> reads <code>err.code</code> (set by <code>muxit-client</code> from the server error envelope) and tags the message with <code>errorCode: &#39;AI_CONTEXT_LENGTH_EXCEEDED&#39;</code>; <code>ChatPanel</code> renders an inline <strong>Start new chat</strong> button next to the error so the user can clear the bloated session in one click. Detection covers OpenAI&#39;s <code>error.code = &quot;context_length_exceeded&quot;</code>, Anthropic&#39;s prose <code>message</code>, OpenRouter&#39;s <code>upstream_status</code> body, plus the plain-text 4xx that older Ollama builds return without a JSON envelope. Previously this surfaced as <code>Error: AI service error (invalid_request_error) (upstream 400): ...</code> and broke TTS, scrolling, and the loading indicator in confusing ways.</li><li><strong>Muxit AI test connection no longer 502s with &quot;anthropic/claude-haiku-4 is not a valid model ID&quot;.</strong> OpenRouter retired the bare 4.0 Anthropic slugs in favour of the dated 4.5 IDs, so the proxy was rejecting every request that used the shipped default. <code>AiService.DefaultModelId</code>, the <code>FALLBACK_MODELS</code> / <code>DEFAULT_FAVORITE_MODELS</code> / <code>DEFAULT_MODEL</code> constants in the web UI, the <code>AI_PROVIDERS[muxit].defaultModel</code>, the <code>ModelCache</code> fallback list, and the doc references all move to <code>anthropic/claude-haiku-4-5</code> and <code>anthropic/claude-sonnet-4-5</code>. Test config in <code>tests/fixtures/test-server.json</code> updated to match.</li><li><strong>Switching AI providers now refreshes the model list and picker.</strong> <code>useModels</code> was only fetching once per mount, so flipping <code>ai.provider</code> from Muxit to Ollama / LM Studio left the chat header dropdown showing OpenRouter cloud slugs that the local backend rejects. The hook now takes a <code>providerKey</code> (the active provider id) and re-fetches whenever it changes; <code>SettingsEditor</code> and <code>ChatPanel</code> pass <code>config.ai.provider</code> through. The chat-header <code>ModelDropdown</code> also receives <code>activeProviderId</code>: on a local provider it lists everything from <code>/models</code> whose <code>provider</code> matches (Ollama / LM Studio / openai-compatible), so the picker only ever offers models the active backend can actually serve. Stale cloud favourites still live in <code>ai.favoriteModels</code> for when the user swings back to Muxit, but they no longer surface against a local endpoint.</li><li><strong>Local LLMs that emit tool calls as JSON in <code>content</code> now actually trigger the tool.</strong> Qwen3, Hermes-3, and gemma chat-template bridges sometimes ignore the OpenAI <code>tool_calls</code> field and return the call as JSON in the assistant message body — <code>{&quot;tool&quot;: &quot;read_property&quot;, &quot;arguments&quot;: {...}}</code> (or wrapped in <code>&lt;tool_call&gt;...&lt;/tool_call&gt;</code>, or fenced in a <code></code>json \`\` block). Previously this rendered as raw JSON in the chat and the user saw nothing happen. <code>AiService.Streaming.ExtractEmbeddedToolCalls</code> is a post-stream fallback (only runs when no structured tool call landed) that recognises all three shapes, accepts <code>name</code>/<code>tool</code> and <code>arguments</code>/<code>parameters</code>/<code>input</code> field aliases, and promotes the payload to a real tool-call entry the loop executes. Streaming text deltas whose first non-whitespace char is <code>{</code> or <code>&lt;tool_call&gt;</code> are also held server-side until the extractor decides — if it isn&#39;t a tool call after all, the buffer flushes through <code>ai.delta</code> so prose still streams; if it is, the JSON never reaches the chat.</li></ul><h2 id="v0-25-7-—-april-25-2026" tabindex="-1">v0.25.7 — April 25, 2026 <a class="header-anchor" href="#v0-25-7-—-april-25-2026" aria-label="Permalink to &quot;v0.25.7 — April 25, 2026&quot;">​</a></h2><h3 id="bug-fixes-5" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-5" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Multi-positional args from scripts no longer create empty files named <code>Microsoft.ClearScript.V8.V8ScriptItem+V8Array</code>.</strong> A script call like <code>connector(&quot;fileaccess&quot;).writeText(path, content)</code> was forwarded across the V8 bridge as a single ClearScript <code>ScriptObject</code> (a V8 array, not a CLR <code>object?[]</code>) — <code>ConnectorManager.MergePositionalArgs</code> only checks for <code>object?[]</code>, so it fell through to the scalar-wrap branch and produced <code>{path: &lt;V8Array&gt;}</code>. The driver&#39;s <code>ArgString(args, &quot;path&quot;, &quot;&quot;)</code> then <code>.ToString()</code>&#39;d the V8Array, yielding the type-name string, and <code>WriteText</code> happily wrote an empty file at that bogus path. The single-arg object form <code>writeText({ path, content })</code> worked because of a downstream normalization, but the same fragility was waiting on every multi-positional call — <code>rename(from, to)</code>, <code>appendText(path, content)</code>, <code>writeBinary(path, content)</code>, and any extension driver action with two or more <code>ArgDescriptor</code> entries. The v0.25.2 positional-args fix only covered the WebSocket path (where JSON deserialization already produces CLR types); the V8 path was still broken. Three layered fixes: (1) <code>ConnectorBridge.CallAsync</code> now runs <code>V8Interop.NormalizeJsValue</code> on the incoming args, recursively converting V8 arrays → <code>object?[]</code> and V8 objects → <code>Dictionary&lt;string, object?&gt;</code> before they reach <code>MergePositionalArgs</code>. (2) <code>MergePositionalArgs</code> throws <code>VALIDATION_INVALID_VALUE</code> when more positional arguments are passed than the action declares, naming the action&#39;s expected signature instead of silently dropping the extras. (3) <code>Muxit.Driver.Sdk.DriverConfig.ArgString</code>/<code>ArgInt</code>/<code>ArgDouble</code>/<code>ArgBool</code> reject dictionary, collection, and ClearScript <code>ScriptObject</code> values up-front with <code>&quot;Argument &#39;X&#39; expected Y, got Z&quot;</code>, so even if marshalling regresses the driver fails loudly instead of writing files named after CLR class names. Numeric-to-string coercion (<code>writeText({ path: 42, ... })</code> → <code>&quot;42&quot;</code>) is preserved.</li></ul><h2 id="v0-25-6-—-april-25-2026" tabindex="-1">v0.25.6 — April 25, 2026 <a class="header-anchor" href="#v0-25-6-—-april-25-2026" aria-label="Permalink to &quot;v0.25.6 — April 25, 2026&quot;">​</a></h2><h3 id="features-18" tabindex="-1">Features <a class="header-anchor" href="#features-18" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Script and connector errors now point at the right line.</strong> <code>script.done</code> carries a structured <code>error</code> object — <code>{ message, line, column, stack, details }</code> — instead of a flat string. Line numbers refer to the user&#39;s source: the wrapper preamble that <code>ScriptEngine.cs</code> injects to make top-level <code>await</code> and main()-style scripts both work prepended 3 lines, and every <code>&lt;anonymous&gt;:N:M</code> reference is now adjusted by <code>ScriptEngine.WrapperPreambleLines</code> before it leaves the server (<code>ScriptEngine.AdjustStackLines</code>). <code>ConnectorLoader.TransformImports</code> keeps line counts stable too — stripped import statements are replaced with newline-equal padding instead of being collapsed, so V8&#39;s reported lines line up with the user&#39;s <code>.js</code>. Connector load failures broadcast a new <code>connector.error</code> message with the same <code>line</code>/<code>column</code> shape.</li><li><strong>The web UI marks the offending row.</strong> <code>EditorGroup.jsx</code> calls <code>monaco.editor.setModelMarkers</code> against the active script or connector file when its name appears in <code>useServer</code>&#39;s new <code>scriptErrors</code> / <code>connectorErrors</code> maps. The squiggly carries the V8 stack as the hover message; markers clear automatically on the next run start. <code>BottomPanel</code> renders an <code>Lnn</code> chip next to error log lines that have line info — clicking it opens the file (<code>muxit-open-file</code> event → <code>Shell.handleFileOpen</code>) and reveals the line via <code>editor.revealLineInCenter</code> / <code>editor.setPosition</code>.</li><li><strong>The editor highlights what a running script is parked on.</strong> A new <code>script.line</code> broadcast (with paired <code>script.line.subscribe</code> / <code>script.line.unsubscribe</code> ref-counted on the server) reports the current user-line whenever a script blocks on <code>delay</code>, <code>await connector.x</code>, <code>ai</code>, <code>ask.*</code>, <code>say</code>, or <code>stream</code>. The trace is gated by <code>ScriptHost.TraceEnabled</code> so the in-script <code>__traceCallSite()</code> helper short-circuits when no dashboard is watching; throttled to one event per 50ms per script and de-duplicated on the prior line. EditorGroup paints a Monaco line decoration (soft-amber background plus a ▶ glyph) for the active file&#39;s running line. Useful for lab automation where a script can sit on <code>await delay(30000)</code> or a slow <code>await psu.voltage = 12</code> for a long time.</li></ul><h2 id="v0-25-5-—-april-25-2026" tabindex="-1">v0.25.5 — April 25, 2026 <a class="header-anchor" href="#v0-25-5-—-april-25-2026" aria-label="Permalink to &quot;v0.25.5 — April 25, 2026&quot;">​</a></h2><h3 id="features-19" tabindex="-1">Features <a class="header-anchor" href="#features-19" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Muxit AI can now talk to a local LLM instead of the Muxit cloud proxy.</strong> A new <code>ai.provider</code> config selects between <code>muxit</code> (the existing managed proxy, default), <code>ollama</code> (local Ollama daemon, default <code>http://localhost:11434/v1</code>), <code>lmstudio</code> (LM Studio&#39;s local server, default <code>http://localhost:1234/v1</code>), and <code>openai-compatible</code> (any other server exposing <code>/chat/completions</code> — vLLM, llama-server, …). Per-provider settings live under <code>ai.providers.&lt;id&gt;.{baseUrl,apiKey,model}</code> so switching back doesn&#39;t lose the previous endpoint. <strong>Settings → AI Services</strong> now shows a provider picker with a per-provider Test connection button (powered by a new <code>config.test_provider</code> WebSocket message) that probes the endpoint, validates auth, and reports how many models the server has loaded. The chat UI, the <code>ai()</code> script global, the agent inference loop, and the AI vision object detector all transparently route through the active provider, so the same dashboards / scripts / agents work whether you&#39;re on the cloud or fully offline. Vision uses the OpenAI <code>image_url</code> content block, which works against multimodal Ollama models (llava, qwen2-vl, llama3.2-vision) without changes. Local providers gate behind a new <strong>Local LLM</strong> Pro feature (<code>local-llm</code> in the registry); the Muxit cloud path still rides on the existing <code>ai</code> feature, so Free/Maker tiers behave exactly as before.</li></ul><h3 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h3><ul><li><strong>AI/LLM call sites consolidated behind <code>ILlmProvider</code>.</strong> <code>MuxitServer/AI/AiService.cs</code>, <code>MuxitServer/AI/AiService.Streaming.cs</code>, <code>MuxitServer/Scripts/ScriptHost.Ai.cs</code>, <code>MuxitServer/Agents/AgentInstance.cs</code>, and <code>MuxitServer/AI/ObjectDetector.cs</code> previously each built their own <code>HttpRequestMessage</code> against the hardcoded Muxit proxy URL with the license key as bearer. They now all go through <code>MuxitServer/AI/Providers/LlmProviderFactory</code>, which returns a cached <code>MuxitProvider</code> (cloud) or <code>OpenAiCompatibleProvider</code> (Ollama / LM Studio / openai-compatible) based on <code>ai.provider</code>. The streaming SSE parser, tool executor, safety gate, session store, and WebSocket message shapes are unchanged — adding a new provider only requires implementing <code>ILlmProvider</code>. License gates moved into the providers themselves: <code>MuxitProvider</code> enforces the <code>ai</code> feature, <code>OpenAiCompatibleProvider</code> enforces <code>local-llm</code>. <code>ModelCache</code> now merges the active local provider&#39;s <code>/models</code> response with the OpenRouter cloud list so the model picker shows what&#39;s actually loaded on Ollama / LM Studio.</li></ul><h2 id="v0-25-4-—-april-24-2026" tabindex="-1">v0.25.4 — April 24, 2026 <a class="header-anchor" href="#v0-25-4-—-april-24-2026" aria-label="Permalink to &quot;v0.25.4 — April 24, 2026&quot;">​</a></h2><h3 id="features-20" tabindex="-1">Features <a class="header-anchor" href="#features-20" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>The AI always confirms numeric and scripted actions before running them.</strong> A new <code>AI confirmation</code> column was added to the safety matrix (<code>SafetyPolicy.LevelBehavior.AiConfirm</code>, the SafetyChip dialog, and the <code>safety.custom.aiConfirm</code> field in <code>server.json</code>). Every built-in level — Observe, Assisted, Active, Unrestricted — ships with <code>Ask every time</code>; only a <strong>Custom</strong> row may set it to <code>Allow</code>. While on, the AI is blocked from executing two classes of tool call without the user first approving them in plain text: <code>write_property</code> / <code>call_action</code> whose arguments contain any number (the AI restates each value with its unit — &quot;setting voltage to 15 V, current limit to 0.35 A — confirm?&quot;), and <code>write_script</code> / <code>run_code</code> (the AI summarizes the script&#39;s plan — connectors, ranges, step sizes, expected duration — before authoring or executing). Numbers the user just spoke are still echoed back, because voice input drops units and &quot;15&quot; on its own is ambiguous. Simple parameterless or boolean-only calls like <code>power_on</code> or <code>reset</code> continue to execute immediately — the gate only catches writes that carry numbers and script-authoring tools. The legacy per-level <code>confirm_unknown_values</code> / <code>confirm_all</code> / <code>off</code> mapping is gone; the column now drives every AI tool-call prompt. See the <a href="/muxit/guides/safety.html#ai-confirmation">Safety Guide</a> for the new flow.</li></ul><h3 id="changes-3" tabindex="-1">Changes <a class="header-anchor" href="#changes-3" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>The AI stops walking loops in chat.</strong> The <code>decision-tree</code>, <code>decision-simple</code>, and <code>safety-confirmation</code> prompt fragments now carry a mandatory &quot;never walk a loop in chat&quot; rule: anything that implies three or more device writes of the same property (a sweep, a range, &quot;for each&quot;, &quot;every N ms&quot;, &quot;measure at X values&quot;, &quot;log every step&quot;) must be authored with <code>write_script</code> or <code>run_code</code> and executed — not stepped through with repeated <code>write_property</code> calls. Before this, a 35-step current sweep produced 35 per-step confirmation round-trips; now the same request produces one <code>kind: &quot;script&quot;</code> plan confirmation and the script runs with per-step writes going through the hardware safety gate directly. The <code>ClassifyIntentByKeywords</code> classifier and <code>MentionsScriptAuthoring</code> gate were both extended so sweep/step/range phrasing activates the Scripts toolgroup and loads the <code>script-api</code> fragment even when the user doesn&#39;t say &quot;script&quot; or &quot;loop&quot; outright.</li><li><strong>Identical tool-call retries no longer re-arm the confirmation gate.</strong> <code>BuildConfirmationKey</code> now canonicalizes the args JSON before hashing — object keys are sorted and numeric leaves are round-trip-formatted, so <code>{&quot;value&quot;:0}</code> and <code>{&quot;value&quot;:0.0}</code> collide to the same pending-confirmation marker. The old raw-string comparison let the LLM trip the gate a second time just by re-emitting the same call with different number formatting, producing &quot;Setting current_set to 0 A (repeat) — confirm?&quot; loops.</li></ul><h2 id="v0-25-3-—-april-24-2026" tabindex="-1">v0.25.3 — April 24, 2026 <a class="header-anchor" href="#v0-25-3-—-april-24-2026" aria-label="Permalink to &quot;v0.25.3 — April 24, 2026&quot;">​</a></h2><h3 id="bug-fixes-6" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-6" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Typos in connector/driver property names now throw instead of silently returning a function stub.</strong> Previously, a script line like <code>log.info(psu.voltage)</code> — where the real property is <code>measured_voltage</code> — evaluated to a ghost function (the Proxy&#39;s generic dispatch closure) and string-interpolated into logs as a garbage function body, hiding the typo. The V8 <code>Proxy</code> <code>get</code> traps for (a) the script-sandbox <code>connector()</code>, (b) cross-connector access inside connector configs, and (c) the per-config <code>__driverProxy</code> used as <code>c</code> in custom methods, now branch three ways: known property → read value, known action → callable function, unknown → <code>throw new ReferenceError(&quot;Connector &#39;X&#39; has no property or method &#39;Y&#39;&quot;)</code> (or <code>&quot;Driver has no property or action &#39;Y&#39;&quot;</code> for the driver proxy). The script-sandbox Proxy now also pulls <code>bridge.ActionNames</code> alongside <code>PropertyNames</code> to tell valid actions apart from typos; the other two already tracked both sets. <code>set</code> traps are unchanged — writes to unknown names already throw via <code>ConnectorManager.CallAsync</code>&#39;s <code>ConnMethodNotFound</code> path.</li></ul><h2 id="v0-25-2-—-april-24-2026" tabindex="-1">v0.25.2 — April 24, 2026 <a class="header-anchor" href="#v0-25-2-—-april-24-2026" aria-label="Permalink to &quot;v0.25.2 — April 24, 2026&quot;">​</a></h2><h3 id="bug-fixes-7" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-7" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Positional args now work when calling driver actions from scripts.</strong> Previously a script like <code>connector(&#39;fileaccess&#39;).writeText(&#39;data.csv&#39;, &#39;hello&#39;)</code> failed with <code>Error: Path cannot be empty</code> — the script-bridge proxy forwards the call as <code>{method: &#39;writeText&#39;, args: [&#39;data.csv&#39;, &#39;hello&#39;]}</code>, and <code>ConnectorManager.MergePositionalArgs</code> only converted an array to a named-arg dict when its last element was already a trailing options dict. Pure positional calls (and single non-dict scalars like <code>readText(&#39;data.csv&#39;)</code>) fell through unchanged, so <code>FileAccessDriver.WriteText</code> read <code>ArgString(args, &quot;path&quot;, &quot;&quot;)</code> off a bare array/string and got the empty default. <code>MergePositionalArgs</code> now also handles the pure-positional and single-scalar cases by mapping them onto the action&#39;s declared <code>ArgDescriptor</code> names, so <code>writeText(path, content)</code> → <code>{path, content}</code> and <code>readText(&quot;x.csv&quot;)</code> → <code>{path: &quot;x.csv&quot;}</code>. The object-literal form (<code>writeText({ path, content })</code>) keeps working exactly as before.</li></ul><h2 id="v0-25-1-—-april-24-2026" tabindex="-1">v0.25.1 — April 24, 2026 <a class="header-anchor" href="#v0-25-1-—-april-24-2026" aria-label="Permalink to &quot;v0.25.1 — April 24, 2026&quot;">​</a></h2><h3 id="bug-fixes-8" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-8" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>TTS no longer replays the last reply on page refresh, and survives the browser&#39;s autoplay block.</strong> Browsers reject <code>speechSynthesis.speak()</code> with <code>error: &quot;not-allowed&quot;</code> until the tab receives a user gesture (autoplay policy) — previously this silently tore the speech chain down, stranded queued progressive chunks, and on a subsequent gesture suddenly flushed the whole backlog, which looked like &quot;TTS was buffering and now it&#39;s firing everything at once.&quot; Both AI chat TTS (<code>useTTS</code>) and script-<code>say()</code> TTS (<code>useScriptSpeech</code>) now route every <code>synth.speak()</code> through a shared priming gate (<code>ttsGesture.whenReady</code>) that defers the call until the next <code>click</code>/<code>keydown</code>/<code>touchstart</code>; a <code>not-allowed</code> error re-arms the gate and retries the same chunk on the next gesture instead of terminating the chain. On page refresh, the chat auto-speak cursor is seeded from restored history so the last assistant reply isn&#39;t re-narrated — toggling TTS off then on still replays it explicitly, switching sessions via the chats panel resets the cursor, and new sessions / cleared chats drop it back so the next reply speaks. Two extra footguns were closed along the way: <code>finish()</code> now also drops the progressive backlog (so a hard error can&#39;t leave chunks that a later call would drain), and <code>speakNext</code>/<code>speak</code>/<code>speakProgressive</code> read voice/rate/pitch from refs so their <code>useCallback</code> identities are stable — otherwise the async <code>voiceschanged</code> event was re-identifying them, which re-fired the ChatPanel auto-speak effect and occasionally kicked a duplicate speak.</li></ul><h2 id="v0-25-0-—-april-23-2026" tabindex="-1">v0.25.0 — April 23, 2026 <a class="header-anchor" href="#v0-25-0-—-april-23-2026" aria-label="Permalink to &quot;v0.25.0 — April 23, 2026&quot;">​</a></h2><h3 id="features-21" tabindex="-1">Features <a class="header-anchor" href="#features-21" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Drivers can opt out of the safety gate.</strong> Drivers with no path to physical hardware and no destructive actions now declare <code>requiresSafetyGates: false</code> — connectors of that driver bypass the <a href="./../guides/safety.html">safety gate</a> entirely (no limit checks, no confirmations, no rate caps, <strong>no audit rows</strong>). C# DLLs declare it at the assembly level via <code>[assembly: RequiresSafetyGates(false)]</code>; JS drivers set <code>meta.requiresSafetyGates: false</code>; built-ins override <code>bool RequiresSafetyGates =&gt; false;</code> on the driver class. Default is <code>true</code>, so every existing driver keeps its current behaviour — this is a pure opt-out. Built-in <strong>Webcam</strong>, <strong>FileAccess</strong>, and <strong>Onvif</strong> ship opted out. <strong>TestDevice</strong> keeps the flag on so it can serve as the canonical example for exploring the safety system (limits, confirms, simulate, audit) against a no-hardware target. The flag is surfaced in <code>drivers.list</code> and <code>driver.schema</code> WebSocket responses as <code>requiresSafetyGates</code>. Orthogonal to per-connector <code>safety.overrideLevel</code> — <code>overrideLevel</code> pins a gated connector to a specific level; <code>requiresSafetyGates: false</code> turns the gate off for the whole driver.</li></ul><h2 id="v0-24-5-—-april-23-2026" tabindex="-1">v0.24.5 — April 23, 2026 <a class="header-anchor" href="#v0-24-5-—-april-23-2026" aria-label="Permalink to &quot;v0.24.5 — April 23, 2026&quot;">​</a></h2><h3 id="changes-4" tabindex="-1">Changes <a class="header-anchor" href="#changes-4" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>Serial connectors are three lines shorter.</strong> The built-in serial JS drivers (<code>Gcode</code>, <code>SerialMonitor</code>, <code>Plotter</code>, <code>SerialProbe</code>) now accept plain <code>port</code> and <code>baudRate</code> fields in their connector config and build the serial transport themselves. The old pattern — a bogus <code>import { createSerialTransport } from &quot;../../shell/src/transports/serial.js&quot;</code> (the path never existed; the loader stripped it with a regex), a pair of top-level <code>const port = …; const baudRate = …;</code> lines, and a hand-rolled <code>_transport: createSerialTransport(port, { baudRate })</code> entry — is replaced by a two-field config block (<code>{ port: &quot;COM3&quot;, baudRate: 115200 }</code>). Optional serial knobs (<code>delimiter</code>, <code>timeout</code>, <code>dataBits</code>, <code>parity</code>, <code>stopBits</code>) sit alongside. SerialProbe&#39;s <code>exportConnector</code> emits the new shape. The <code>config._transport</code> field still works as an undocumented escape hatch for non-serial transports (e.g. redirecting the Plotter driver at a TCP simulator via <code>createTcpTransport</code>) and for back-compat with existing workspaces — no C# host changes were needed; the cleanup is entirely in the four driver JS files, their templates, and the in-tree workspace-template connectors.</li></ul><h2 id="v0-24-4-—-april-23-2026" tabindex="-1">v0.24.4 — April 23, 2026 <a class="header-anchor" href="#v0-24-4-—-april-23-2026" aria-label="Permalink to &quot;v0.24.4 — April 23, 2026&quot;">​</a></h2><h3 id="changes-5" tabindex="-1">Changes <a class="header-anchor" href="#changes-5" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>The legacy per-service AI safety mode is gone.</strong> Settings → AI Services no longer shows a <em>Safety Mode</em> radio (Confirm / Trust), and <code>ai.safetyMode</code> has been removed from the <code>server.json</code> schema and docs. The setting was already dead code — <code>AiService.GetSafetyMode()</code> derived tool-call prompt behaviour from the global <code>SafetyGate.CurrentLevel</code> (see v0.23.0), so the per-service config was accepted and silently ignored. Unknown config keys are preserved on disk, so any <code>ai.safetyMode</code> still in an existing <code>server.json</code> is harmless; removing it keeps the file tidy. Tool-call approvals are now controlled exclusively by the safety level picker in the StatusStrip SafetyChip — see the <a href="/muxit/guides/safety.html#ai-user-value-confirmation">Safety Guide</a> for the level-to-behaviour mapping.</li></ul><h2 id="v0-24-3-—-april-23-2026" tabindex="-1">v0.24.3 — April 23, 2026 <a class="header-anchor" href="#v0-24-3-—-april-23-2026" aria-label="Permalink to &quot;v0.24.3 — April 23, 2026&quot;">​</a></h2><h3 id="changes-6" tabindex="-1">Changes <a class="header-anchor" href="#changes-6" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>The <code>&quot;auto&quot;</code> AI model is gone.</strong> It was confusing — users assumed it triaged simple requests to a cheap model and escalated only when needed. In reality the proxy did a static tier-based pick (Haiku for Free, Sonnet for Pro), which silently raised cost on Pro accounts. <code>ai.model</code> now defaults to <code>anthropic/claude-haiku-4-5</code> (a single concrete ID for every tier) and the model picker in the Chat header and Settings → AI no longer offers an <em>Auto</em> option. Existing configs with <code>&quot;model&quot;: &quot;auto&quot;</code> keep working: <code>AiService.NormalizeModelId</code> maps the legacy sentinel to the new default, and the UI&#39;s <code>currentModel</code> fallback now mirrors <code>AiService.DefaultModelId</code> so the picker always shows the model the server will actually call. Pin a different model by setting <code>ai.model</code> to any OpenRouter ID (e.g. <code>anthropic/claude-sonnet-4-5</code>).</li></ul><h2 id="v0-24-2-—-april-23-2026" tabindex="-1">v0.24.2 — April 23, 2026 <a class="header-anchor" href="#v0-24-2-—-april-23-2026" aria-label="Permalink to &quot;v0.24.2 — April 23, 2026&quot;">​</a></h2><h3 id="features-22" tabindex="-1">Features <a class="header-anchor" href="#features-22" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Restart Server is reachable without digging into Settings.</strong> The system-tray icon menu gained a <em>Restart Server</em> entry between <em>Open GUI</em> and <em>Quit</em>, and the Titlebar&#39;s <em>File</em> menu now has a <em>Restart Server...</em> item below <em>Settings</em>. Both use the same mechanism as the existing Settings → Server button (<code>/api/server/restart</code> for the UI, <code>WorkspaceManager.WriteRestartFile()</code> + <code>IHostApplicationLifetime.StopApplication()</code> for the tray), so the launcher&#39;s restart marker auto-relaunches the process. The File-menu action asks for confirmation before firing since menu items are easier to misclick than the Settings button.</li></ul><h3 id="bug-fixes-9" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-9" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Restart now takes ~2s instead of 20–30s.</strong> The generic host&#39;s <code>ShutdownTimeout</code> was defaulting to 30s, so every restart path (tray, Settings, <code>/api/server/restart</code>) had to wait for Kestrel to drain idle WebSocket subscribers before the launcher&#39;s restart marker could fire. <code>HostOptions.ShutdownTimeout</code> is now capped at 2s — long enough for in-flight HTTP requests to finish, short enough that stale WS connections don&#39;t hold up a relaunch. Clients reconnect on their own once the new process is listening.</li></ul><h2 id="v0-24-1-—-april-23-2026" tabindex="-1">v0.24.1 — April 23, 2026 <a class="header-anchor" href="#v0-24-1-—-april-23-2026" aria-label="Permalink to &quot;v0.24.1 — April 23, 2026&quot;">​</a></h2><h3 id="bug-fixes-10" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-10" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Settings → Server tab no longer crashes on open.</strong> The Remote Access subsection referenced a closure variable (<code>licenseHook</code>) that wasn&#39;t in its scope, so every attempt to open the tab threw a <code>ReferenceError</code> at render time and was silently swallowed by React&#39;s ErrorBoundary, leaving the tab non-functional. The <code>license</code> prop now threads through <code>SettingsEditor</code> → <code>ServerSection</code> → <code>RemoteAccessSubSection</code>, and the remote-access tier gate renders correctly.</li></ul><h2 id="v0-24-0-—-april-22-2026" tabindex="-1">v0.24.0 — April 22, 2026 <a class="header-anchor" href="#v0-24-0-—-april-22-2026" aria-label="Permalink to &quot;v0.24.0 — April 22, 2026&quot;">​</a></h2><h3 id="features-23" tabindex="-1">Features <a class="header-anchor" href="#features-23" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Five-tier licensing lands in the server.</strong> The compile-time tier table in <code>AppSettings.cs</code> now matches the published pricing: Free → Maker → Pro → Lab → Enterprise, with ranks 0–4 and Lemon Squeezy product IDs wired for the four paid tiers. Count limits are raised in line with the marketing page (Free now gets 5 connectors / 3 scripts / 5 extension drivers; Maker gets 10 connectors and unlimited scripts and drivers; Pro gets unlimited connectors). AI, MQTT broker, remote access, startup scripts, and video recording move from Pro down to <strong>Maker</strong>; Vision AI and autonomous agents stay at <strong>Pro</strong>; <code>SafetyLevel.Custom</code> and the new <strong>Safety Policy Engine</strong> live on <strong>Lab</strong>. Dashboards are unlimited on every tier. The Settings → Subscription <em>Plan Comparison</em> table now renders all five tiers dynamically from the server&#39;s feature registry (boolean features show a tick from each feature&#39;s <code>requiredTierId</code> upward; count-limited features inherit through the tier rank), and the &quot;Pro&quot; badge / upgrade copy across the UI is driven by the blocked feature&#39;s <code>blockedReason</code> rather than hard-coded strings, so future tier moves don&#39;t need a UI patch.</li><li><strong>Safety Policy Engine is a Lab-tier feature.</strong> The per-connector <code>safety: { limits, confirm, rate, interlock, safeState, overrideLevel }</code> block is now gated by the new <code>safety.policy-engine</code> license feature. <code>SafetyGate</code> keeps every connector&#39;s block parsed as before, but wraps lookups in a tier check: on Maker/Pro the block is ignored (with a one-time console warning per connector) and the global <code>SafetyLevel</code> policy table applies unchanged, so downgrades never silently remove safety — they fall back to a coarser but still-enforced policy. On Lab and Enterprise the block is fully honoured.</li></ul><h2 id="v0-23-0-—-april-22-2026" tabindex="-1">v0.23.0 — April 22, 2026 <a class="header-anchor" href="#v0-23-0-—-april-22-2026" aria-label="Permalink to &quot;v0.23.0 — April 22, 2026&quot;">​</a></h2><h3 id="features-24" tabindex="-1">Features <a class="header-anchor" href="#features-24" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Graduated safety levels with an audit trail.</strong> Every write and action across the server now flows through a new <code>SafetyGate</code> that resolves to a single <strong>safety level</strong> — <strong>Observe</strong> (simulate every write/action so nothing reaches hardware), <strong>Assisted</strong> (real writes; ask once on large changes, ask every destructive action), <strong>Active</strong> (real writes; only destructive actions ask once), <strong>Unrestricted</strong> (everything passes; typed phrase to enable), plus a <strong>Custom</strong> row where the user picks an outcome per column. The level picker lives in the StatusStrip <code>SafetyChip</code>; applying a change is a single click in the dialog (no second confirmation — the dialog itself is the confirmation surface), with <strong>Unrestricted</strong> additionally requiring typing <code>I UNDERSTAND</code> inside the dialog. Once granted, a level stays active until the user picks a different one. Connector configs can add an optional <code>safety: { limits, confirm, rate, interlock, overrideLevel }</code> block — <code>limits.min/max</code> hard-block out-of-range writes at every level except Unrestricted, <code>largeChangeFraction</code> (default 25% of range) catches fat-finger deltas, <code>confirm: { reset: &quot;always&quot; | &quot;typed&quot; }</code> flags an action destructive so the level&#39;s destructive-action column applies, and <code>overrideLevel</code> pins a single connector to a specific level independent of the global. Blocked, simulated, and confirmed operations are appended to <code>workspace/logs/audit.jsonl</code> as flat <code>{ts, event, caller, details}</code> records — the record shape is deliberately extensible so a future phase can log every write without schema migration. Every origin (<code>script:&lt;name&gt;</code>, <code>ai:&lt;sessionId&gt;</code>, <code>dashboard:&lt;clientId&gt;</code>, <code>agent:&lt;name&gt;</code>, <code>mcp:&lt;id&gt;</code>, <code>system:&lt;subsystem&gt;</code>) is stamped onto the audit line. <strong>License gating is per-level:</strong> each level is a separate license feature (<code>safety.level.observe</code>, <code>safety.level.assisted</code>, <code>safety.level.active</code>, <code>safety.level.unrestricted</code>, <code>safety.level.custom</code>), and the audit trail is its own feature (<code>safety.audit</code>). Default ship config gives the two low-risk levels (Observe, Unrestricted) to <code>free</code> and the three lab-safety levels (Assisted, Active, Custom) plus the audit trail to <code>pro</code>, but every tier&#39;s allow-list is config-driven — future tiers (Maker, Lab, Enterprise, …) can mix and match any subset by bumping <code>RequiredTierId</code> on the relevant features in <code>AppSettings.cs</code>, no code changes in the SafetyGate. The chip and dialog are visible on every tier; locked levels appear in the dialog with a badge and disabled radio. A workspace moved to a tier that doesn&#39;t expose the persisted level is clamped to the lowest-risk allowed level on startup (falling back to Unrestricted only if the tier exposes no levels at all — a pathological config that keeps writes working until the config is fixed). The AI user-value confirmation is <strong>not</strong> gated by any of this: <code>ai.safetyMode</code> gains a third level — <code>confirm_unknown_values</code> (the new default) — which only prompts when a tool-call argument carries a numeric value the user did not actually say; existing <code>confirm</code> configs migrate to <code>confirm_all</code> automatically so no one loses existing prompts. The Chat Panel&#39;s pending-approval card now labels each argument as user-spoken (green) or AI-chosen (orange) so it is obvious why the confirmation is being asked.</li></ul><h2 id="v0-22-1-—-april-21-2026" tabindex="-1">v0.22.1 — April 21, 2026 <a class="header-anchor" href="#v0-22-1-—-april-21-2026" aria-label="Permalink to &quot;v0.22.1 — April 21, 2026&quot;">​</a></h2><h3 id="features-25" tabindex="-1">Features <a class="header-anchor" href="#features-25" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>AI asks before it acts on complex requests</strong> — the default chat behavior no longer rushes to call tools on the first sentence of a multi-step request. For script authoring, agent setup, and other automation that touches real hardware, the AI now runs a short clarification exchange first (which device, what range, what step size, what timing, where to save data, what safety limits), offers sensible options inline, restates the plan in one sentence, then acts. Simple reads, setting changes, and questions still resolve directly without a round-trip. The change lives in <code>MuxitServer/AI/Prompts/identity-fallback.md</code> and a new &quot;Before authoring&quot; section in <code>script-api.md</code>, and the matching guidance was mirrored into the seeded <code>workspace-template/config/instructions.md</code> so fresh workspaces get the new defaults too.</li></ul><h2 id="v0-22-0-—-april-21-2026" tabindex="-1">v0.22.0 — April 21, 2026 <a class="header-anchor" href="#v0-22-0-—-april-21-2026" aria-label="Permalink to &quot;v0.22.0 — April 21, 2026&quot;">​</a></h2><h3 id="features-26" tabindex="-1">Features <a class="header-anchor" href="#features-26" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Workspace EULA acknowledgement + Sandbox mode</strong> — every workspace must now have an accepted EULA on file before Muxit will let any driver perform a hardware write. On startup the server checks <code>{workspace}/.muxit/EULA-ACCEPTED.md</code> against the canonical EULA bundled with the server (SHA256 of the body above the footer separator, LF line endings, UTF-8 no BOM). If the file is missing, corrupt, tampered, or outdated the workspace enters <strong>Sandbox mode</strong>: reads and polling keep working, but every property write and action call is refused at <code>DriverRegistry</code> with error code <code>EULA_NOT_ACCEPTED</code>, and <code>script.run()</code> refuses to launch. The web UI shows a blocking acceptance modal that renders the full EULA, asks for an identifier (name or email), and requires an explicit checkbox; accepting writes the acceptance file atomically and appends an entry to <code>.muxit/eula-history.jsonl</code> for the audit trail. Headless installs (<code>--cli</code>, <code>--mcp</code>, CI) accept via the new <code>muxit eula accept --user &lt;id&gt;</code> subcommand or the <code>MUXIT_EULA_ACCEPT=1</code> / <code>MUXIT_EULA_USER=…</code> env-var pair. Minor/major EULA bumps require re-acceptance; patch bumps do not. See <code>docs-site/guides/workspaces.md</code> for the full flow.</li></ul><h2 id="v0-21-0-—-april-21-2026" tabindex="-1">v0.21.0 — April 21, 2026 <a class="header-anchor" href="#v0-21-0-—-april-21-2026" aria-label="Permalink to &quot;v0.21.0 — April 21, 2026&quot;">​</a></h2><h3 id="features-27" tabindex="-1">Features <a class="header-anchor" href="#features-27" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>AI can now generate and edit dashboards.</strong> Three new tools — <code>list_dashboards</code>, <code>read_dashboard</code>, <code>write_dashboard</code> — let the Chat Panel AI (and any MCP client such as Claude Desktop / Claude Code) create <code>.dashboard.json</code> files directly in the active workspace. Ask &quot;build me a dashboard for the PSU and the robot&quot; and the AI calls <code>list_connectors</code> + <code>get_connector_schema</code> to discover valid bindings, then writes a complete JSON layout with gauges, sliders, charts, and action buttons wired to real device properties. Follow-ups like &quot;add a chart of <code>voltage</code>&quot; round-trip through <code>read_dashboard</code> → <code>write_dashboard</code> so the existing layout is preserved. Nested sub-folders (e.g. <code>robots/arm-1.dashboard.json</code>) work for AI and MCP just like the HTTP API. File operations share the same validation rules as the HTTP dashboard API (filename must end with <code>.dashboard.json</code>, content must parse as JSON, no path traversal, sub-folders must already exist), so hand-edited and AI-edited dashboards stay interchangeable. Intent routing picks up the keywords <code>dashboard</code>, <code>widget</code>, <code>gauge</code>, <code>panel</code>, <code>layout</code> so the group only loads when relevant.</li></ul><h2 id="v0-20-0-—-april-20-2026" tabindex="-1">v0.20.0 — April 20, 2026 <a class="header-anchor" href="#v0-20-0-—-april-20-2026" aria-label="Permalink to &quot;v0.20.0 — April 20, 2026&quot;">​</a></h2><h3 id="features-28" tabindex="-1">Features <a class="header-anchor" href="#features-28" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>SCPI driver speaks USBTMC</strong> — the <code>GenericScpi</code> driver now supports a third transport alongside TCP/LXI and Serial: USBTMC (USB Test &amp; Measurement Class), the USB-native protocol used by modern bench instruments with a front-panel USB-B port (Rigol DS1000Z/MSO5000, Keysight DSO-X, Siglent SDG/SSA/SDS, Owon, most R&amp;S gear). Set <code>transport: &quot;usbtmc&quot;</code> in the connector config and provide <code>vendorId</code> / <code>productId</code> (hex or decimal) — an optional <code>serial</code> filter disambiguates multiple of the same model. The driver wraps every SCPI line in a USBTMC <code>DEV_DEP_MSG_OUT</code> BBB header, implements the two-step <code>REQUEST_DEV_DEP_MSG_IN</code> + multi-transfer reassembly for reads, issues <code>INITIATE_CLEAR</code> on connect and <code>INITIATE_ABORT_BULK_IN</code> on drain recovery — enough of the spec for reliable SCPI round-trips against every mainstream instrument. Uses <code>libusb-1.0</code> via <code>LibUsbDotNet</code> (native binaries auto-bundled into the <code>.muxdriver</code> package for Windows). Requires WinUSB bound to the device via <a href="https://zadig.akeo.ie/" target="_blank" rel="noreferrer">Zadig</a>. See <code>docs-site/reference/drivers/scpi.md</code> for setup details.</li></ul><h2 id="v0-19-0-—-april-20-2026" tabindex="-1">v0.19.0 — April 20, 2026 <a class="header-anchor" href="#v0-19-0-—-april-20-2026" aria-label="Permalink to &quot;v0.19.0 — April 20, 2026&quot;">​</a></h2><h3 id="features-29" tabindex="-1">Features <a class="header-anchor" href="#features-29" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Built-in AI is now a Pro feature.</strong> The Chat Panel, the script <code>ai()</code> global, vision AI (<code>identify_objects</code> / <code>locate_object</code>), <code>ai.credits</code>, and the &quot;test AI&quot; button in Settings all now require a Pro subscription (or active Pro trial). Free-tier users who try these get a clean <code>AI_REQUIRES_PRO</code> error instead of consuming credits against the Muxit AI proxy. The Chat Panel renders an upgrade CTA in place of the input box when AI is blocked. <strong>What stays free:</strong> MCP (bring your own AI client — Claude Desktop, Claude Code, ChatGPT), <code>say()</code> (browser TTS), <code>ask.*</code> dashboard prompts, and every non-AI feature. Agents were already Pro-gated; this just closes the remaining entry points to Muxit-hosted AI.</li></ul><h2 id="v0-18-1-—-april-19-2026" tabindex="-1">v0.18.1 — April 19, 2026 <a class="header-anchor" href="#v0-18-1-—-april-19-2026" aria-label="Permalink to &quot;v0.18.1 — April 19, 2026&quot;">​</a></h2><h3 id="bug-fixes-11" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-11" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Default script template and docs no longer show trailing cleanup after <code>while (script.running)</code> loops.</strong> Pressing <strong>Stop</strong> cancels <code>delay()</code> <em>and</em> calls V8 <code>Interrupt()</code>, which terminates execution synchronously — so statements after the main loop (and <code>finally</code> blocks) were never reliably running, despite examples like <code>log.info(&quot;script finished&quot;)</code> suggesting otherwise. The built-in &quot;new script&quot; template, the sinewave demo, the scripts guide, the first-script walkthrough, the script API reference, and the AI script-writing prompt have all been updated to drop the misleading trailing cleanup and to spell out that Stop is a hard-kill. The stop behavior itself is unchanged — this is a pure docs/template alignment so scripts are written to be safe-to-abort at every iteration rather than depending on cleanup code that never runs.</li></ul><h2 id="v0-15-1-—-april-19-2026" tabindex="-1">v0.15.1 — April 19, 2026 <a class="header-anchor" href="#v0-15-1-—-april-19-2026" aria-label="Permalink to &quot;v0.15.1 — April 19, 2026&quot;">​</a></h2><h3 id="bug-fixes-12" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-12" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Network diagnostics no longer leak LAN topology to remote callers</strong> — <code>GET /api/diagnostics/network</code> now returns the full report (interface IPs, Windows Firewall rule list, PowerShell fix commands, elevation flag) only to loopback callers. Authenticated remote clients — including a client using a leaked session token — get a redacted summary: interface counts, firewall profile booleans, and mDNS counters, but no addresses or rule names. The Settings → Server → Network Diagnostics panel now loads via HTTP (instead of WebSocket) so the loopback check governs the UI too; the WebSocket <code>diagnostics.network</code> handler always returns the redacted view because WebSocket handlers don&#39;t currently see peer IP. The startup console banner likewise drops the LAN-address list and Administrator flag, keeping only counts and firewall findings — safer when server logs are collected to shared files.</li></ul><h2 id="v0-18-0-—-april-18-2026" tabindex="-1">v0.18.0 — April 18, 2026 <a class="header-anchor" href="#v0-18-0-—-april-18-2026" aria-label="Permalink to &quot;v0.18.0 — April 18, 2026&quot;">​</a></h2><h3 id="features-30" tabindex="-1">Features <a class="header-anchor" href="#features-30" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Smaller drivers fill in the on-demand <code>details</code> layer</strong> — the <code>Gcode</code>, <code>Plotter</code>, <code>AudioSynth</code>, and <code>SerialMonitor</code> drivers now populate <code>Details</code> on the props/actions whose one-liners were hiding non-obvious behavior. Highlights: <code>Gcode</code> documents the GRBL status enum values, the <code>send</code> / <code>home</code> / <code>reset</code> / <code>hold</code> distinction around buffer flushing and step loss, and <code>streamFile</code>&#39;s line-count-based progress (which doesn&#39;t wait for motion completion); <code>Plotter</code> documents the canvas-API angle convention (0 = +x, CCW), <code>lineTo</code> chaining polylines until <code>penUp</code> breaks them, and the <code>draw</code> batch&#39;s non-atomic error behavior; <code>AudioSynth</code> documents <code>volume</code>/<code>waveform</code> applying to the <em>next</em> playback (not in-progress), the <code>playMelody</code> shorthand grammar with number-as-rest, and that overlapping playback calls replace rather than layer; <code>SerialMonitor</code> documents that <code>port</code>/<code>baudRate</code> are transport-owned sentinel values and that fast bursts need the <code>output</code> stream rather than <code>lastLine</code> polling. This completes the first pass of the <code>details</code> rollout — every shipped driver (except the intentionally minimal <code>Template</code> and the not-yet-stable <code>Avantes</code>) now carries extended markdown on its subtle props and actions.</li></ul><h2 id="v0-17-0-—-april-18-2026" tabindex="-1">v0.17.0 — April 18, 2026 <a class="header-anchor" href="#v0-17-0-—-april-18-2026" aria-label="Permalink to &quot;v0.17.0 — April 18, 2026&quot;">​</a></h2><h3 id="features-31" tabindex="-1">Features <a class="header-anchor" href="#features-31" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Extension drivers fill in the on-demand <code>details</code> layer</strong> — the premium <code>Midi</code>, <code>Scpi</code>, <code>Vision</code>, and <code>Fairino</code> extension drivers now populate <code>Details</code> on their most semantically dense props/actions. Highlights: <code>Midi</code> documents channel 1-16 vs wire-level 0-15, velocity-0 = Note Off, <code>sendSysex</code> auto-framing of F0/F7, and <code>learn</code> timeout behavior; <code>GenericScpi</code> documents query retry-once + <code>timeout</code> scope, <code>settleMs</code> applied to writes only, and <code>readErrors</code> iterating until the SCPI <code>&quot;No error&quot;</code> sentinel; <code>Vision</code> documents the <code>color</code>/<code>contour</code> tracker param schemas and <code>calibrateColor</code> HSV margins (±10 hue, ±40 sat/val); <code>Fairino</code> documents the 6-element joint/TCP layout with mixed mm/deg, null-vs-zero semantics, <code>moveJ</code> arc vs <code>moveL</code> straight-line paths, blending with <code>blendT</code> / <code>blendR</code>, singularity risk, <code>stop</code> flushing the queue vs <code>pause</code> preserving it, and <code>disable</code> letting the robot fall under gravity. As before, <code>Details</code> only flows through on-demand surfaces (doc disclosure, editor hover, <code>get_*_schema</code>) so the upfront AI prompt cost is unchanged.</li></ul><h2 id="v0-16-0-—-april-18-2026" tabindex="-1">v0.16.0 — April 18, 2026 <a class="header-anchor" href="#v0-16-0-—-april-18-2026" aria-label="Permalink to &quot;v0.16.0 — April 18, 2026&quot;">​</a></h2><h3 id="features-32" tabindex="-1">Features <a class="header-anchor" href="#features-32" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Built-in drivers fill in the on-demand <code>details</code> layer</strong> — the new <code>Details</code> field added in v0.15.0 is now populated across every built-in driver: <code>Webcam</code>, <code>Onvif</code>, <code>MqttBridge</code>, <code>FileAccess</code>, and <code>TestDevice</code>. The one-line descriptions stay the same (and the AI system prompt summary stays the same size), but opening a driver&#39;s doc page or hovering a method in the script editor now surfaces parameter enums, clamp ranges, side effects, and failure modes that the one-liners hid — e.g. Onvif&#39;s PTZ speed sign convention, FileAccess&#39;s write-size enforcement timing, MqttBridge&#39;s QoS levels and retain-flag semantics, Webcam&#39;s silent backend-dependent brightness/contrast normalization. The extra markdown only travels through on-demand surfaces (driver doc disclosure, editor hover popup, <code>get_connector_schema</code> / <code>get_driver_schema</code>) so the upfront token cost is unchanged.</li></ul><h2 id="v0-15-0-—-april-18-2026" tabindex="-1">v0.15.0 — April 18, 2026 <a class="header-anchor" href="#v0-15-0-—-april-18-2026" aria-label="Permalink to &quot;v0.15.0 — April 18, 2026&quot;">​</a></h2><h3 id="features-33" tabindex="-1">Features <a class="header-anchor" href="#features-33" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Driver descriptors gain an on-demand <code>details</code> layer</strong> — <code>PropertyDescriptor</code> and <code>ActionDescriptor</code> now carry an optional markdown <code>Details</code> field alongside the existing one-line <code>Description</code>. The short description stays in the upfront AI system prompt and IntelliSense summary line (so per-request token cost is unchanged); the extended markdown travels only through on-demand surfaces: the driver doc page&#39;s new &quot;Show details&quot; disclosure, the script editor&#39;s hover/autocomplete popup, and the full-schema payloads of <code>get_connector_schema</code> / <code>get_driver_schema</code>. Authors put parameter enums, side effects, truncation caps, and failure modes in <code>Details</code> — everything a user or AI agent would otherwise have to discover through trial and error. The <code>SerialProbe</code> driver now demonstrates the pattern with extended docs on its five subtle actions (<code>scanBaud</code>, <code>hypothesize</code>, <code>analyzeFrames</code>, <code>stimulus</code>, <code>exportConnector</code>); other drivers fill in <code>Details</code> opportunistically.</li><li><strong>Network diagnostics panel for &quot;my device can&#39;t connect&quot; triage</strong> — Settings → Server → Network Diagnostics now renders a live report: bind address/port, Kestrel listener state, network interfaces with per-interface mDNS advertise/skip reasons, mDNS query/answer counters, a UDP 5353 conflict probe (warns when Apple Bonjour / Windows mDNSResponder / Avahi is already bound), and — on Windows — the active Windows Firewall profile, whether any enabled rule covers the listening port, and any stale <code>muxit</code> Block rules overriding an Allow. The same report prints as a startup console banner and is exposed over HTTP (<code>GET /api/diagnostics/network</code>) and WebSocket (<code>diagnostics.network</code>), so a phone or laptop that <em>can&#39;t</em> reach the server can still curl it. The UI shows copy-paste PowerShell fix commands (add allow rule, remove stale block rules, set profile to Private); the diagnostics layer is strictly read-only and never touches firewall state. Backed by <code>MuxitServer/Core/NetworkDiagnostics.cs</code>, <code>Http/DiagnosticsApi.cs</code>, and <code>WebSocket/Handlers/DiagnosticsHandler.cs</code>.</li><li><strong>SerialProbe port / baud configurable at runtime</strong> — the driver now configures its serial port and link-layer settings at session time via actions (<code>listPorts</code>, <code>openPort</code>, <code>closePort</code>, <code>setBaudRate</code>, <code>setPort</code>), so the AI can try different configurations without editing the connector config. <code>scanBaud</code> accepts an explicit <code>portPath</code> + <code>reopenAt</code> policy, <code>exportConnector</code> refuses to run without an active port (so the generated config captures the confirmed <code>portPath</code> + <code>baudRate</code>), and the starter template + example connector no longer pin a port — the AI picks one via <code>listPorts</code> / <code>openPort</code> at session start. The driver now also exposes five terminal-friendly streams — <code>traffic</code> (unified RX/TX with arrow prefix), <code>rxHex</code>, <code>rxText</code>, <code>txHex</code>, <code>txText</code> — alongside the existing <code>frames</code> / <code>events</code>; raw-hex output previously went through an un-wired emitter and never reached terminal dashboards. The legacy preconfigured <code>config._transport</code> path still works for backward compat.</li><li><strong>Dev Console: driver registry publication status</strong> — the Dev Console Drivers tab now shows, per driver, whether the latest built <code>.muxdriver</code> matches what&#39;s published to the public <code>muxit-drivers</code> registry, with a direct action to publish or re-publish from the button row. Release routing in <code>lib/driver-manager/publish.js</code> targets the public <code>muxit-io/muxit-drivers</code> repo instead of the development repo.</li></ul><h3 id="changes-7" tabindex="-1">Changes <a class="header-anchor" href="#changes-7" aria-label="Permalink to &quot;Changes&quot;">​</a></h3><ul><li><strong>Verbose AI request/response logging is now opt-in</strong> — the AI service no longer floods the server log with full proxy request bodies, SSE event dumps, or tool-call summaries by default. Set <code>MUXIT_AI_DEBUG=1</code> before starting the server to re-enable the full firehose while diagnosing prompt or tool-format issues.</li></ul><h2 id="v0-14-0-—-april-18-2026" tabindex="-1">v0.14.0 — April 18, 2026 <a class="header-anchor" href="#v0-14-0-—-april-18-2026" aria-label="Permalink to &quot;v0.14.0 — April 18, 2026&quot;">​</a></h2><h3 id="features-34" tabindex="-1">Features <a class="header-anchor" href="#features-34" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Declarative SCPI schema keeps connector configs light</strong> — the GenericScpi driver now reads an optional <code>config.scpi: { properties: {...}, methods: {...} }</code> block and auto-synthesizes the SCPI boilerplate. An entry like <code>voltage: { cmd: &quot;VOLT&quot;, type: &quot;float&quot;, unit: &quot;V&quot; }</code> becomes a full read/write property (get sends <code>VOLT?</code>, set sends <code>VOLT 5</code>), and <code>save: { cmd: &quot;*SAV&quot;, arg: &quot;int&quot; }</code> becomes <code>c.save(3)</code> → sends <code>*SAV 3</code>. A trailing <code>?</code> on the command marks the property read-only. Top-level <code>properties</code>/<code>methods</code> with the same name still win, so vendor quirks can stay hand-written while the regular 80% shrinks to a single-line declaration. The <code>bk-psu.js</code> template loses ~40 lines of repeated <code>parseFloat</code>/<code>\${Number(v)}</code>/<code>.trim()</code> boilerplate. Under the hood, <code>DllDriverHost.GetDriverMetaForInstance</code> now returns live per-instance schema to <code>ConnectorInstance</code> so declared properties reach the connector pipeline (DLL drivers that want per-config schema in the future get this for free). The SCPI driver is bumped to v1.2.0.</li></ul><h2 id="v0-13-0-—-april-17-2026" tabindex="-1">v0.13.0 — April 17, 2026 <a class="header-anchor" href="#v0-13-0-—-april-17-2026" aria-label="Permalink to &quot;v0.13.0 — April 17, 2026&quot;">​</a></h2><h3 id="features-35" tabindex="-1">Features <a class="header-anchor" href="#features-35" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Open file tabs refresh automatically on FileAccess writes</strong> — a <code>.csv</code> log being appended to from a running script now updates its chart (or text) view live, no close-and-reopen needed. <code>FileAccess</code> actions (<code>writeText</code>, <code>appendText</code>, <code>writeBinary</code>, <code>deleteFile</code>, <code>rename</code>) emit a new <code>workspace.file.changed</code> WebSocket event with the workspace-relative path; the editor debounces rapid events (~150 ms) and reloads any open tab pointing at that path. Tabs with unsaved edits are skipped so in-progress work isn&#39;t clobbered.</li><li><strong>Script API prompt and docs steer the AI away from <code>require(&#39;fs&#39;)</code></strong> — the AI script-sandbox prompt, the Script Guide, and the Script API Reference now include a dedicated &quot;Working with Files&quot; section that shows the FileAccess connector pattern (<code>connector(&#39;files&#39;).writeText({ path, content })</code>, <code>appendText</code>, <code>readText</code>, …) and explicitly calls out that <code>require</code>, <code>import</code>, and Node.js filesystem APIs are not available. Cuts down on AI-generated scripts that try <code>require(&#39;fs&#39;)</code> and fail at runtime.</li></ul><h2 id="v0-12-1-—-april-17-2026" tabindex="-1">v0.12.1 — April 17, 2026 <a class="header-anchor" href="#v0-12-1-—-april-17-2026" aria-label="Permalink to &quot;v0.12.1 — April 17, 2026&quot;">​</a></h2><h3 id="bug-fixes-13" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-13" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>AI chat TTS no longer gets stuck behind script speech</strong> — after the v0.12.0 split, the chat&#39;s progressive TTS only kick-started when the global <code>speechSynthesis</code> queue was idle, so any in-flight (or just-finished) script <code>say()</code> utterance left the chat chunks queued forever and the toggle felt broken. The chain now starts whenever its own iterator isn&#39;t running, regardless of what <code>useScriptSpeech</code> is doing on the shared queue — the browser still plays the utterances in order. Re-enabling the chat speaker after a response has already arrived now also speaks that last response, instead of silently waiting for the next one.</li></ul><h2 id="v0-12-0-—-april-17-2026" tabindex="-1">v0.12.0 — April 17, 2026 <a class="header-anchor" href="#v0-12-0-—-april-17-2026" aria-label="Permalink to &quot;v0.12.0 — April 17, 2026&quot;">​</a></h2><h3 id="features-36" tabindex="-1">Features <a class="header-anchor" href="#features-36" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Independent script speech toggle</strong> — script <code>say()</code> output now has its own speaker button (🔊) in the top status strip, decoupled from the AI chat TTS toggle. Mute the AI without silencing your scripts (or vice versa). The new toggle also makes <code>say()</code> audible while the chat panel is closed — previously the auto-speak only ran when the chat sidebar was the active panel. Persisted as <code>voice.tts.scriptEnabled</code> (default on).</li></ul><h2 id="v0-11-0-—-april-17-2026" tabindex="-1">v0.11.0 — April 17, 2026 <a class="header-anchor" href="#v0-11-0-—-april-17-2026" aria-label="Permalink to &quot;v0.11.0 — April 17, 2026&quot;">​</a></h2><h3 id="features-37" tabindex="-1">Features <a class="header-anchor" href="#features-37" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>CSV files open as charts</strong> — opening a <code>.csv</code> or <code>.tsv</code> file from the File Explorer now renders it as an interactive line chart (powered by the existing uPlot dependency). The parser auto-detects the delimiter (<code>,</code>, <code>;</code>, or tab), whether the first row is a header, and each column&#39;s type (timestamp, number, or text). By default the first timestamp column becomes the X-axis and every numeric column becomes a Y-series; both can be changed from the controls bar. A toggle in the tab&#39;s control strip switches between chart view and the raw Monaco text view, so the files produced by <code>FileAccess.appendText(...)</code> in measurement scripts are now immediately inspectable without wiring up a dashboard widget. Files over 100 000 rows are capped on render (with a banner) to keep the chart responsive. No new dependencies.</li></ul><h2 id="v0-10-0-—-april-17-2026" tabindex="-1">v0.10.0 — April 17, 2026 <a class="header-anchor" href="#v0-10-0-—-april-17-2026" aria-label="Permalink to &quot;v0.10.0 — April 17, 2026&quot;">​</a></h2><h3 id="features-38" tabindex="-1">Features <a class="header-anchor" href="#features-38" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>AI no longer hangs on long-running scripts</strong> — <code>run_script</code> and <code>run_code</code> now accept <code>wait_seconds</code> (default 15, max 120) and return <code>status: &quot;running&quot;</code> with a <code>run_id</code>, recent output, and a <code>next_seq</code> cursor if the script doesn&#39;t finish within the window. The script keeps running; the assistant stays responsive. Three new tools — <code>get_script_status</code>, <code>get_script_output</code> (with <code>since_seq</code> cursor), and <code>wait_for_script</code> — let the AI poll progress, fetch incremental log output, or block again with a bounded wait. Pass <code>wait_seconds: 0</code> for known background scripts (monitors, watchers) to kick off and return immediately. Per-script output is retained for about 5 minutes after completion so polls that arrive just after the script exits still see the final result and log tail.</li></ul><h2 id="v0-9-1-—-april-17-2026" tabindex="-1">v0.9.1 — April 17, 2026 <a class="header-anchor" href="#v0-9-1-—-april-17-2026" aria-label="Permalink to &quot;v0.9.1 — April 17, 2026&quot;">​</a></h2><h3 id="bug-fixes-14" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-14" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Midi driver release-ready</strong> — shipped with its own first-class <code>template.js</code> (the old <code>MidiDriver.Template.cs</code> C# string has been removed), registered in the documentation sidebar and the Built-in Drivers index, and given accurate manifest tags (<code>[&quot;midi&quot;, &quot;audio&quot;]</code> — a new <code>midi</code> tag was added to the controlled vocabulary so it&#39;s discoverable in the Extensions panel). The driver source and behaviour are unchanged.</li></ul><h3 id="features-39" tabindex="-1">Features <a class="header-anchor" href="#features-39" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Generic SCPI driver 1.1.1</strong> — consolidates the earlier 1.1.0/1.1.2 pair into a single coherent release: <ul><li><strong>Stale-response framing</strong> — every transaction drains inbound bytes before sending, auto-resyncs on query timeouts or empty responses, and retries once with a known-clean RX buffer. Fixes the &quot;values off by one&quot; / <code>NaN</code> flicker on slow instruments.</li><li><strong><code>settleMs</code> option</strong> — optional post-write pause held inside the transport lock, so a set-then-query sequence reads back the freshly applied setpoint on instruments that need a few ms to commit.</li><li><strong><code>debug</code> TX/RX stream</strong> — when <code>debug: true</code>, every transaction&#39;s command and response is emitted on the <code>scpi</code> stream (for Terminal-widget live tails) and written to server stdout.</li><li><strong>Cleaner defaults &amp; internals</strong> — TCP host default is now the RFC 5737 placeholder <code>192.0.2.100</code>, the unused <code>IScpiTransport</code> interface was removed in favour of the existing abstract base, and the <code>*IDN?</code> fallback now only swallows <code>TimeoutException</code> / <code>InvalidOperationException</code> instead of every <code>Exception</code>.</li><li><strong>New reference page</strong> — <a href="./drivers/scpi.html">Scpi Driver</a> documents every property, action, config option, the framing/recovery model, and a BK Precision example connector.</li></ul></li></ul><h2 id="v0-9-0-—-april-16-2026" tabindex="-1">v0.9.0 — April 16, 2026 <a class="header-anchor" href="#v0-9-0-—-april-16-2026" aria-label="Permalink to &quot;v0.9.0 — April 16, 2026&quot;">​</a></h2><h3 id="features-40" tabindex="-1">Features <a class="header-anchor" href="#features-40" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Guided tour via the AI chat</strong> — new <strong>Help → Guided Tour</strong> menu in the title bar with one-click starter prompts (&quot;Install a Driver&quot;, &quot;Create a Connector&quot;, &quot;Drag Properties into a Script&quot;, &quot;Build a Dashboard&quot;, plus a general overview). Each entry pins the AI chat open and sends a pre-written question so the assistant walks you through the task. Starter prompts are not a gate — you can ask for a tour in your own words and get the same behaviour.</li><li><strong>AI docs-reading tools</strong> — three new tools (<code>list_docs</code>, <code>read_doc</code>, <code>search_docs</code>) give the AI direct access to every page of this documentation site. Pages are embedded into the server binary at build time so they&#39;re available offline and always match the running version. The chat picks them up automatically when your question includes phrases like <em>how do I</em>, <em>walk me through</em>, <em>tour</em>, <em>tutorial</em>, <em>getting started</em>, or <em>what is</em> — expect the assistant to quote the relevant guide back instead of guessing. Dev-mode reload is available via <code>MUXIT_DOCS_DEV=1</code>, mirroring the prompt fragment hot-reload. Backed by a new <code>DocsRegistry</code> and <code>DocsToolHandlers</code>.</li></ul><h2 id="v0-8-0-—-april-16-2026" tabindex="-1">v0.8.0 — April 16, 2026 <a class="header-anchor" href="#v0-8-0-—-april-16-2026" aria-label="Permalink to &quot;v0.8.0 — April 16, 2026&quot;">​</a></h2><h3 id="features-41" tabindex="-1">Features <a class="header-anchor" href="#features-41" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>New driver: SerialProbe</strong> — AI-assisted serial protocol reverse-engineering. A Tier 1 bundled JavaScript driver that captures raw bytes in labelled windows, auto-detects framing (<code>lines-lf</code> / <code>lines-crlf</code> / <code>stx-etx</code> / <code>fixed:N</code>), runs local CRC-16/Modbus and XOR-trailing checks, correlates stimulus/response pairs, and exports a working connector config when a hypothesis is confirmed. The AI never sees raw traffic — only summaries returned by local analysis — so a full session stays cheap. See <a href="./drivers/serial-probe.html"><code>reference/drivers/serial-probe</code></a>.</li><li><strong>Raw-byte streaming on SerialTransport</strong> — <code>setRawMode(true)</code> + <code>onRawData(hex, timestampMs)</code> bypass line buffering and deliver per-chunk hex with timestamps. <code>portPath</code> and <code>baudRate</code> are now accessible from JS, enabling drivers like SerialProbe to spawn temporary transports for baud-rate scans.</li></ul><h2 id="v0-7-0-—-april-16-2026" tabindex="-1">v0.7.0 — April 16, 2026 <a class="header-anchor" href="#v0-7-0-—-april-16-2026" aria-label="Permalink to &quot;v0.7.0 — April 16, 2026&quot;">​</a></h2><h3 id="features-42" tabindex="-1">Features <a class="header-anchor" href="#features-42" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>New MIDI extension driver</strong> — live input/output for any USB or virtual MIDI device, per-channel accept-lists, input→output pass-through, internal 24-PPQN clock with <code>start</code>/<code>stop</code>/<code>continue</code> transport, Standard MIDI File playback and recording (sandboxed to <code>workspace/midi/</code>), <code>panic</code> (All Notes Off + All Sound Off + Reset All Controllers on every channel, with local <code>activeNotes</code> bookkeeping cleared), and a <code>learn</code> action that resolves the next incoming message for binding physical knobs/pads. Incoming messages are normalized to <code>{ type, channel, note?/controller?/…, tsMs }</code> and published on three streams: <code>events</code> (everything), <code>note</code> (just Note On/Off), and <code>clock</code> (24-PPQN ticks, opt-in via <code>emitClockStream: true</code>). Built on <a href="https://github.com/melanchall/drywetmidi" target="_blank" rel="noreferrer">DryWetMIDI</a> and ships as <code>muxit-midi-v0.1.0.muxdriver</code> — install via the Extensions panel. Full reference: <a href="./drivers/midi.html">Midi Driver</a>.</li><li><strong>AudioSynth extension driver</strong> — new free tier-3 driver that plays tones, musical notes, and short melodies on the server host&#39;s default audio output via NAudio. Four waveforms (sine / square / triangle / sawtooth), volume control, click-free attack/release envelopes, and replace-not-queue playback semantics so a new sound always cleanly replaces the previous one. Actions: <code>playTone(frequency, duration)</code>, <code>playNote(note, duration)</code>, <code>playMelody(notes)</code> (shorthand <code>&quot;C4:200,E4:200&quot;</code> or <code>[{note,duration}, …]</code>), <code>beep()</code>, <code>stop()</code>. Properties: <code>volume</code>, <code>waveform</code>, <code>playing</code>. Ships with a <code>workspace-template/connectors/audio.js</code> example wiring up <code>chime()</code>, <code>alert()</code>, and <code>fanfare()</code> helpers. See <a href="./../reference/drivers/audio-synth.html">AudioSynth reference</a> — useful for lab-side &quot;experiment complete&quot; cues, CNC/robot feedback tones, and safety alerts.</li><li><strong>New driver tags</strong> — <code>audio</code>, <code>synth</code>, and <code>notification</code> added to the <a href="./../reference/driver-tags.html">controlled tag vocabulary</a> (and <a href="https://github.com/muxit-io/muxit-development/blob/main/lib/driver-manager/schema.js" target="_blank" rel="noreferrer"><code>lib/driver-manager/schema.js</code></a>) so audio-class drivers are discoverable in the Extensions panel.</li></ul><h2 id="v0-6-0-—-april-16-2026" tabindex="-1">v0.6.0 — April 16, 2026 <a class="header-anchor" href="#v0-6-0-—-april-16-2026" aria-label="Permalink to &quot;v0.6.0 — April 16, 2026&quot;">​</a></h2><h3 id="features-43" tabindex="-1">Features <a class="header-anchor" href="#features-43" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Centralised AI instructions and tool registry</strong> — every AI-facing prompt fragment (script API guide, decision tree, agent identity, per-autonomy blurbs, TTS rules, tool descriptions) now lives as markdown under <code>MuxitServer/AI/Prompts/</code> and is loaded at runtime by a new <code>PromptRegistry</code>. A new <code>ToolRegistry</code> owns the authoritative tool list and dispatch; per-group handler classes under <code>MuxitServer/AI/ToolHandlers/</code> replace the 1,040-line monolithic <code>AiToolExecutor</code>. Chat and agent system prompts are composed by a shared <code>SystemPromptBuilder</code> from these fragments, eliminating the parallel implementations in <code>AiService.BuildSystemPrompt</code> and <code>AgentInstance.BuildAgentSystemPrompt</code>.</li><li><strong>Dev-mode prompt hot-reload</strong> — setting <code>MUXIT_PROMPT_DEV=1</code> makes <code>PromptRegistry</code> read fragments from the source tree on every lookup, so prompt edits are visible without rebuilding the server.</li></ul><h3 id="bug-fixes-15" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-15" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Consistent script-writing guidance</strong> — resolved contradictions between <code>write_script</code>/<code>run_code</code> tool descriptions (which showed <code>await dev.prop()</code>) and the Script API guide (which said &quot;Do NOT use await&quot;). Canonical text now lives once in <code>Prompts/script-api.md</code> and is referenced from everywhere else.</li><li><strong>Robot-specific rules scoped to the robot section</strong> — the <code>workspace-template/config/instructions.md</code> rule &quot;do not add delays in the script&quot; previously appeared to apply to all scripts; it is now inside the <code>### robot</code> device subsection where it belongs, and the generic Script API guide keeps <code>delay()</code> as the recommended loop pacing tool for non-robot devices.</li><li><strong>Edit connector visibility from the Connectors panel</strong> — expanded connector cards have a pencil (✎) icon that enters an edit mode. In that mode every property, action, and stream gets an eye-icon toggle and each section header gets <strong>hide all</strong> / <strong>show all</strong> shortcuts; hidden items appear greyed-out so they can be toggled back on. Both driver-sourced items and custom methods / properties defined in the connector&#39;s own <code>methods</code> / <code>properties</code> blocks can be hidden or exposed (previously only driver items). Changes are written back to the connector&#39;s <code>.js</code> source (the top-level <code>hide: […]</code> array, clearing any <code>expose:</code> whitelist to keep the file consistent) and the connector is hot-reloaded — the AI picks up the new surface on the next turn, no server restart required.</li><li><strong>Edit primitive driver-config from the Connectors panel</strong> — edit mode also reveals a <strong>Config</strong> section that lists each driver-config key with an inline editor (text / number / checkbox). Committing a value surgically rewrites the matching literal in the <code>.js</code> source (either at the top level or inside the nested <code>config: { … }</code> block, whichever already holds the key) and hot-reloads only that connector. Non-primitive entries (expressions, transports, objects) are shown read-only with a tooltip explaining that the file must be edited directly. Framework-injected internals (anything whose key starts with <code>_</code>, e.g. <code>_workspacePath</code>) are never exposed.</li><li><strong>Auto-open Config on load error</strong> — when a connector fails to initialize (wrong serial port, unreachable host, ...) the card automatically expands into edit mode with the Config section visible, so the problem can be fixed without opening the file. A failed reload keeps the connector in the enabled list with its <code>InitError</code> set, so the Config panel stays reachable for the retry.</li><li><strong>Compact pill switch for enable/disable</strong> — the per-connector checkbox is replaced with a small toggle-pill to avoid the visual collision with the pencil/checkmark icons elsewhere in the header. Property rows show the <code>R/W</code> access label inline again (previously only in the tooltip).</li><li><strong>New WebSocket endpoints</strong> — <a href="./../reference/websocket-api.html#connector-visibility-set"><code>connector.visibility.set</code></a> and <a href="./../reference/websocket-api.html#connector-driverconfig-set"><code>connector.driverConfig.set</code></a> drive the new UI. <a href="./../reference/websocket-api.html#connector-schema"><code>connector.schema</code></a> gains an optional <code>includeHidden: true</code> flag and its response now includes <code>driverConfig</code>, <code>hiddenItems</code>, and <code>visibilityMode</code>; streams are now returned as <code>[{name, hidden}]</code> objects (the UI already handled both forms).</li></ul><h2 id="v0-5-1-—-april-15-2026" tabindex="-1">v0.5.1 — April 15, 2026 <a class="header-anchor" href="#v0-5-1-—-april-15-2026" aria-label="Permalink to &quot;v0.5.1 — April 15, 2026&quot;">​</a></h2><h3 id="features-44" tabindex="-1">Features <a class="header-anchor" href="#features-44" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>License status on the premium driver detail page</strong> — when a premium driver (e.g. Fairino) is installed, the driver detail page now shows a clear license banner: &quot;License active&quot;, &quot;Trial — N day(s) left&quot;, or &quot;License required&quot; with an actionable message. <code>marketplace.detail</code> carries new <code>licensed</code>, <code>licenseStatus</code>, <code>licenseMessage</code>, and <code>trialDaysRemaining</code> fields for locally-loaded premium drivers.</li></ul><h2 id="v0-5-0-—-april-15-2026" tabindex="-1">v0.5.0 — April 15, 2026 <a class="header-anchor" href="#v0-5-0-—-april-15-2026" aria-label="Permalink to &quot;v0.5.0 — April 15, 2026&quot;">​</a></h2><h3 id="features-45" tabindex="-1">Features <a class="header-anchor" href="#features-45" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Premium driver management in the Dev Console</strong> — new <strong>Premium</strong> tab provides a single source of truth for premium drivers (currently Fairino, Vision, Avantes). Each row joins the driver&#39;s <code>manifest.json</code> with its build / sign / package state and its Lemon Squeezy product binding. Inline editor for the <code>commerce</code> block writes back to <code>drivers/&lt;name&gt;/manifest.json</code> so the binding ships inside the signed <code>.muxdriver</code>. Optional <strong>Push to LS</strong> mirrors manifest storefront copy to the Lemon Squeezy product via the v1 REST API (requires <code>LEMON_SQUEEZY_API_KEY</code> env var on the dev console).</li><li><strong>Manifest <code>commerce</code> block</strong> — driver manifests now carry an optional <code>commerce</code> block (<code>lemonSqueezyProductId</code>, <code>lemonSqueezyVariantId</code>, <code>price</code>, <code>storefront.{tagline,longDescription,images}</code>) for premium drivers. At server boot, <code>DllDriverHost</code> reads the product ID and feeds it into <code>LicenseManager.RegisterDynamicFeature(...)</code> so a customer&#39;s Lemon Squeezy activation can be matched back to this driver. Validator emits new rules (<code>COMMERCE_MISSING_FOR_PREMIUM</code>, <code>COMMERCE_LS_PRODUCT_MISSING</code>, <code>COMMERCE_LS_PRODUCT_FORMAT</code>, <code>COMMERCE_PRICE_INVALID</code>, <code>COMMERCE_FREE_FORBIDDEN</code>).</li><li><strong><code>node drivers.js premium-status</code> CLI</strong> — new headless command reports the build / sign / commerce readiness of every premium driver. <code>--strict</code> exits non-zero on any outstanding issue, suitable as a release gate.</li></ul><h2 id="v0-4-2-—-april-15-2026" tabindex="-1">v0.4.2 — April 15, 2026 <a class="header-anchor" href="#v0-4-2-—-april-15-2026" aria-label="Permalink to &quot;v0.4.2 — April 15, 2026&quot;">​</a></h2><h3 id="bug-fixes-16" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-16" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Installed drivers appear immediately after install</strong> — the server now broadcasts <code>drivers.changed</code> over WebSocket after a marketplace install or uninstall, so the Extensions panel&#39;s Installed tab refreshes without needing a reconnect.</li><li><strong>Premium badge in Available tab</strong> — the <code>marketplace.search</code> response now includes the <code>category</code> field, and the Extensions panel renders a Premium tag next to premium drivers on the Available tab (not just the Installed tab).</li></ul><h3 id="features-46" tabindex="-1">Features <a class="header-anchor" href="#features-46" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Version selector for drivers with multiple registry entries</strong> — when the registry exposes several versions of the same driver id, the Available tab now renders a single card with a version dropdown instead of one card per version. <code>marketplace.install</code> accepts an optional <code>version</code> field so clients can pin a specific version; omitted, the server installs the highest-versioned entry.</li></ul><h2 id="v0-4-1-—-april-14-2026" tabindex="-1">v0.4.1 — April 14, 2026 <a class="header-anchor" href="#v0-4-1-—-april-14-2026" aria-label="Permalink to &quot;v0.4.1 — April 14, 2026&quot;">​</a></h2><h3 id="bug-fixes-17" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-17" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Wake word changes apply instantly</strong> — updating the wake word phrase in Settings → Voice now restarts the background listener immediately instead of requiring a page refresh.</li><li><strong>Microphone selection applies instantly</strong> — picking a different input device while hands-free listening is active now reopens the capture stream with the new device, no refresh needed.</li></ul><h3 id="features-47" tabindex="-1">Features <a class="header-anchor" href="#features-47" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>&quot;Saved — applied immediately&quot; indicator in Settings</strong> — every change to Voice, AI Services, and AI Behavior settings now shows a transient confirmation badge so users can see the change took effect without refreshing the page.</li></ul><h2 id="v0-4-0-—-april-14-2026" tabindex="-1">v0.4.0 — April 14, 2026 <a class="header-anchor" href="#v0-4-0-—-april-14-2026" aria-label="Permalink to &quot;v0.4.0 — April 14, 2026&quot;">​</a></h2><h3 id="breaking-changes" tabindex="-1">Breaking Changes <a class="header-anchor" href="#breaking-changes" aria-label="Permalink to &quot;Breaking Changes&quot;">​</a></h3><ul><li><strong>Removed DEBUG-only license bypass</strong> — <code>AppSettings.DevMode</code>, the surrounding <code>#if DEBUG</code> compile-time switch, and the <code>license.dev_simulate</code> WebSocket endpoint have all been deleted. License enforcement now applies uniformly to every build configuration, closing the risk that a mis-shipped Debug binary would silently disable tier / count / entitlement gates and expose a backdoor that could forge any subscription tier. Side effect: 8 of the 9 build warnings emitted by <code>dotnet build MuxitServer -c Release</code> (all <code>CS0162 Unreachable code detected</code>) are gone, plus a related <code>CS8602</code> nullable-reference warning in <code>Program.cs</code>.</li></ul><h2 id="v0-3-2-—-april-14-2026" tabindex="-1">v0.3.2 — April 14, 2026 <a class="header-anchor" href="#v0-3-2-—-april-14-2026" aria-label="Permalink to &quot;v0.3.2 — April 14, 2026&quot;">​</a></h2><h3 id="bug-fixes-18" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-18" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li>Fix AI script prompt to teach canonical property syntax (db55b4e)</li><li>Fix Hardware pane polling pile-up on state updates (303b1a1)</li><li>Fix Hardware pane writing to devices on mount (49b3e2e)</li><li>Fix SCPI driver stale-response framing + add settleMs / resync (31e2b14)</li><li>Fix GUI page flickering on driver info / marketplace panes (7919baa)</li><li>Fix Scpi manifest entryPoint drift + add validator rule to catch class (52c31bc)</li><li>Fix marketplace install on Windows + route premium drivers correctly (7e23aa8)</li><li>Fix dev console crash on boot — stale MUXIT_SERVER_URL reference (faf845e)</li><li>Fix Overview/Diagnostics tabs not refreshing when MuxitServer comes up (804ae1a)</li><li>Fix dashboard Knob widget first-quarter unresponsiveness (f28d33d)</li><li>Fix outdated await/function-call patterns in docs and examples (25b3589)</li><li>Fix DeepEquals numeric type coercion causing phantom serial writes (ae33d8d)</li><li>Fix serial overload from knob and improve SCPI log accuracy (b48f460)</li><li>Fix BK-PSU serial overload, property types, and store updates (cf39a4f)</li><li>Fix PSU polling timeout blocking other connectors (d0e67b6)</li><li>Fix dashboard Knob widget first-quarter unresponsiveness (93709bb)</li><li>Fix outdated await/function-call patterns in docs and examples (90ae0de)</li><li>Fix DeepEquals numeric type coercion causing phantom serial writes (63b11f5)</li><li>Fix serial overload from knob and improve SCPI log accuracy (de753ac)</li><li>Fix BK-PSU serial overload, property types, and store updates (9db1d0f)</li><li>Fix PSU polling timeout blocking other connectors (776a8c2)</li><li>Fix docs site CSP errors by using path-conditional security policy (0cfe29e)</li><li>Fix ai() script command returning empty response (6eabb38)</li><li>Fix log copy formatting: use tab-separated fields instead of line breaks (e3b0403)</li><li>fix: preserve InitializeAsync wrapper for MCP stdio mode (5addd16)</li></ul><h3 id="features-48" tabindex="-1">Features <a class="header-anchor" href="#features-48" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li>Self-pace Hardware pane polling with setTimeout chain (c67fd82)</li><li>SCPI driver 1.1.2: also write TX/RX to stdout when debug=true (1cdfb28)</li><li>Rename sidebar tabs, add create shortcuts, fix Connectors show-all crash (e3baab9)</li><li>Bump SCPI driver to 1.1.1 for version verification (5d8c590)</li><li>debugging SCPI (8cd8894)</li><li>Flatten workspace/drivers and keep .muxdriver packages intact (95aa67c)</li><li>fixed docs build error; fix PSU connector (2765ef1)</li><li>Extract Vision into a premium extension; drop muxit-vision static feature (8c7fc96)</li><li>Expose EventBus to drivers via an SDK-level IDriverHost (67cbc84)</li><li>Dev console: rename pass + clickable sources + promote/extract refactor (7178573)</li><li>Dev console UI cleanup: collapsible driver cards + Overview clarity (1618e7f)</li><li>Phase 6c: deployment simulation — local mocks for GitHub + driver registry (5718ad5)</li><li>Phase 6b: driver authoring — per-driver manifest editor in dev console (270a629)</li><li>Phase 6a: expanded per-tab docs + new Release pipeline tab (2b2ce8a)</li><li>Proxy MuxitServer HTTP + WS through dev console to bypass CORS (8da35cf)</li><li>Auto-discover MuxitServer host:port instead of hard-coding 127.0.0.1:8765 (ed73a02)</li><li>Add Driver/Feature Overview tab + DEBUG tier simulator to Dev Console (0ff217f)</li><li>Replace in-server Dev Tools panel with a standalone Dev Console (Phase 4) (eada670)</li><li>Revert &quot;Add Dev Tools panel skeleton with Workspace + Diagnostics tabs (Phase 4)&quot; (bb3aeb3)</li><li>Add Dev Tools panel skeleton with Workspace + Diagnostics tabs (Phase 4) (c36710a)</li><li>Add server-side driver validator and surface diagnostics in UI (Phase 3) (5a6b1bd)</li><li>Reorganize muxit.js menu and split driver build out of build.js (Phase 2) (985ec91)</li><li>Codify driver manifest schema and structured validator (Phase 1) (f06fd90)</li><li>Gate SCPI TX/RX stream behind debug config option (7adf534)</li><li>Throttle property writes to prevent knob/slider flooding serial devices (cdc6b0a)</li><li>Add SCPI command/response stream to PSU dashboard for debugging (395d24a)</li><li>updated manifests (7ddea2a)</li><li>added manifests for avantes, fairino, scpi drivers (dd9b3a7)</li><li>Gate SCPI TX/RX stream behind debug config option (5fae791)</li><li>Decouple workspace from git — use extensions for driver development (b760d92)</li><li>Throttle property writes to prevent knob/slider flooding serial devices (0b131e2)</li><li>Add SCPI command/response stream to PSU dashboard for debugging (b634499)</li><li>Add clean URL rewriting for docs so help tab links work (b814c86)</li><li>Add AI Chat toggle to View menu, sort sidebar tabs to match icon order (a873bc5)</li><li>Add mobile/desktop view switcher, pin AI chat to secondary sidebar (9af64d2)</li><li>fixed robot enable state (befc1f8)</li><li>robot dashboard (016547a)</li><li>test script (e354dd2)</li></ul><h3 id="other" tabindex="-1">Other <a class="header-anchor" href="#other" aria-label="Permalink to &quot;Other&quot;">​</a></h3><ul><li>refactor: split startup into core + background phases for instant GUI loading (cae93d6)</li></ul><h2 id="v0-3-2-—-april-14-2026-1" tabindex="-1">v0.3.2 — April 14, 2026 <a class="header-anchor" href="#v0-3-2-—-april-14-2026-1" aria-label="Permalink to &quot;v0.3.2 — April 14, 2026&quot;">​</a></h2><h3 id="bug-fixes-19" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-19" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Hardware pane no longer writes to the device on mount</strong> — the desktop Hardware pane (<code>ConnectorBrowser</code>) polled writable properties by sending <code>args: []</code> in its <code>connector.call</code> WebSocket message. An empty array is not <code>null</code>, so the server routed the call as a SET with an empty-array payload, which JS coerces to <code>0</code> for numbers and <code>true</code> for booleans — silently reprogramming instruments the moment you expanded a connector card (e.g. writing <code>VOLT 0 / CURR 0 / OUTP ON</code> to a power supply). Fixed on both sides: the UI now omits the <code>args</code> field for reads, and <code>ConnectorManager.CallAsync</code> normalises an empty args array to <code>null</code> so no future caller can hit this trap.</li></ul><h2 id="v0-3-1-—-april-14-2026" tabindex="-1">v0.3.1 — April 14, 2026 <a class="header-anchor" href="#v0-3-1-—-april-14-2026" aria-label="Permalink to &quot;v0.3.1 — April 14, 2026&quot;">​</a></h2><h3 id="bug-fixes-20" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-20" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>SCPI driver: stale-response framing fix</strong> — <code>GenericScpi</code> over TCP and Serial no longer mis-attributes a late reply from a prior (timed-out) transaction to the next query. The transport now drains inbound bytes at the top of every transaction, owns its own line buffer on TCP (removing the hidden <code>StreamReader</code> buffer), and enters a self-healing &quot;resync&quot; state on timeout / empty-response so the cascade that made voltage reads collapse to <code>0</code> after a few iterations is closed out. The connector-level <code>parseFloat</code> hazard that rendered <code>NaN</code> / <code>object [0]</code> in the Hardware pane is addressed by a new <code>num()</code> guard in <code>bk-psu.js</code> and by the driver rejecting empty SCPI responses outright.</li><li><strong>SCPI driver: echo stripping</strong> — instruments with local-echo enabled are handled automatically (the driver peels one echo layer before returning the response).</li></ul><h3 id="features-49" tabindex="-1">Features <a class="header-anchor" href="#features-49" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>SCPI driver <code>settleMs</code> option</strong> — hold the transport lock for N ms after every non-query write, so a <code>VOLT 5</code> / <code>VOLT?</code> round-trip reads back the freshly applied setpoint without needing <code>delay()</code> in the connector.</li><li><strong>SCPI driver: shared transport base</strong> — TCP and Serial transports now share <code>ScpiTransportBase</code>, collapsing ~130 lines of duplicated locking / pacing / framing into one place.</li></ul><h2 id="v0-3-0-—-april-14-2026" tabindex="-1">v0.3.0 — April 14, 2026 <a class="header-anchor" href="#v0-3-0-—-april-14-2026" aria-label="Permalink to &quot;v0.3.0 — April 14, 2026&quot;">​</a></h2><h3 id="features-50" tabindex="-1">Features <a class="header-anchor" href="#features-50" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Flat <code>workspace/drivers/</code> layout</strong> — the <code>free/</code> and <code>premium/</code> subdirectories are gone. Installed drivers now sit as single <code>.muxdriver</code> package files directly under <code>workspace/drivers/</code>; the server extracts each package transparently into a hidden <code>workspace/drivers/.cache/&lt;id&gt;@&lt;version&gt;/</code> on load. The <code>category</code> in each package&#39;s <code>manifest.json</code> is the sole source of truth for premium enforcement — a DLL&#39;s location on disk no longer matters.</li><li><strong>Package-native install/uninstall</strong> — installing a driver now copies the <code>.muxdriver</code> file into <code>workspace/drivers/</code> untouched; uninstalling deletes it. Stale cache entries are garbage-collected automatically on the next scan.</li></ul><h3 id="breaking-changes-1" tabindex="-1">Breaking Changes <a class="header-anchor" href="#breaking-changes-1" aria-label="Permalink to &quot;Breaking Changes&quot;">​</a></h3><ul><li>Any existing <code>workspace/drivers/free/</code> or <code>workspace/drivers/premium/</code> contents are no longer scanned. Reinstall drivers via the Extensions panel.</li><li>Editor API now blocks all writes under <code>drivers/</code> (previously only blocked <code>drivers/free/</code> and <code>drivers/premium/</code>). Installs must go through the Extensions panel.</li><li><code>node drivers.js sign</code> now signs each premium driver&#39;s <code>bin/publish/</code> DLL (pre-package). Re-run <code>node drivers.js build</code> afterwards to repackage.</li></ul><h2 id="v0-2-0-—-april-6-2026" tabindex="-1">v0.2.0 — April 6, 2026 <a class="header-anchor" href="#v0-2-0-—-april-6-2026" aria-label="Permalink to &quot;v0.2.0 — April 6, 2026&quot;">​</a></h2><ul><li><strong>Add revision history page</strong> accessible from Help menu and documentation sidebar</li><li><strong>Bump version</strong> from 0.1.0 to 0.2.0</li></ul><h2 id="v0-1-0-—-april-2-2026" tabindex="-1">v0.1.0 — April 2, 2026 <a class="header-anchor" href="#v0-1-0-—-april-2-2026" aria-label="Permalink to &quot;v0.1.0 — April 2, 2026&quot;">​</a></h2><p>Initial development release.</p><h3 id="bug-fixes-21" tabindex="-1">Bug Fixes <a class="header-anchor" href="#bug-fixes-21" aria-label="Permalink to &quot;Bug Fixes&quot;">​</a></h3><ul><li><strong>Fix V8 OOM crash</strong> from sinewave script tight loop (<a href="https://github.com/muxit-io/muxit-development/pull/188" target="_blank" rel="noreferrer">#188</a>)</li><li><strong>Fix AI Service section hidden</strong> when credits fetch fails (<a href="https://github.com/muxit-io/muxit-development/pull/187" target="_blank" rel="noreferrer">#187</a>)</li><li><strong>Fix Muxit AI proxy integration</strong> — system prompt format, tools format, and error messages (<a href="https://github.com/muxit-io/muxit-development/pull/183" target="_blank" rel="noreferrer">#183</a>)</li><li><strong>Fix <code>await delay()</code> in connector configs</strong> resolving instantly (<a href="https://github.com/muxit-io/muxit-development/pull/182" target="_blank" rel="noreferrer">#182</a>)</li><li><strong>Fix scripts stuck in <code>_running</code> after stop</strong>, blocking new script starts (<a href="https://github.com/muxit-io/muxit-development/pull/179" target="_blank" rel="noreferrer">#179</a>)</li></ul><h3 id="features-51" tabindex="-1">Features <a class="header-anchor" href="#features-51" aria-label="Permalink to &quot;Features&quot;">​</a></h3><ul><li><strong>Align docs styling</strong> with muxit.io and fix MQTT docs (<a href="https://github.com/muxit-io/muxit-development/pull/186" target="_blank" rel="noreferrer">#186</a>)</li><li><strong>Improve license key masking</strong> to show first 8 + last 4 chars (<a href="https://github.com/muxit-io/muxit-development/pull/185" target="_blank" rel="noreferrer">#185</a>)</li><li><strong>Add oscilloscope widget</strong> with phosphor glow and persistence effects (<a href="https://github.com/muxit-io/muxit-development/pull/184" target="_blank" rel="noreferrer">#184</a>)</li><li><strong>Add AI service card</strong> to license UI with keys, credits, and test button (<a href="https://github.com/muxit-io/muxit-development/pull/181" target="_blank" rel="noreferrer">#181</a>)</li><li><strong>Add smart tool filtering</strong> to reduce AI costs and improve cheap model reliability (<a href="https://github.com/muxit-io/muxit-development/pull/180" target="_blank" rel="noreferrer">#180</a>)</li><li><strong>Redesign license plan comparison</strong> to dynamic column grid (<a href="https://github.com/muxit-io/muxit-development/pull/178" target="_blank" rel="noreferrer">#178</a>)</li></ul>`,396)])])}const g=o(i,[["render",s]]);export{p as __pageData,g as default};

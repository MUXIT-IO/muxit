import{_ as t,o as a,c as s,ag as o}from"./chunks/framework.DkuLC1cV.js";const p=JSON.parse('{"title":"AI-assisted SCPI setup","description":"","frontmatter":{},"headers":[],"relativePath":"guides/ai-scpi-authoring.md","filePath":"guides/ai-scpi-authoring.md"}'),i={name:"guides/ai-scpi-authoring.md"};function r(n,e,l,c,d,u){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="ai-assisted-scpi-setup" tabindex="-1">AI-assisted SCPI setup <a class="header-anchor" href="#ai-assisted-scpi-setup" aria-label="Permalink to &quot;AI-assisted SCPI setup&quot;">​</a></h1><p>Muxit&#39;s AI chat can add a new SCPI instrument (oscilloscope, multimeter, power supply, signal generator, …) end-to-end — probe the device, look up its command set online, write the connector config, and bring it online without a restart. All three <code>GenericScpi</code> transports are supported: TCP/IP (LAN), Serial, and USBTMC (direct USB-B).</p><div class="tip custom-block"><p class="custom-block-title">Subscription required</p><p>AI-assisted SCPI authoring uses Muxit&#39;s managed AI — <strong>Maker tier or higher</strong>. On Free, you have two equally good paths:</p><ul><li><strong>Add SCPI instruments by hand</strong> using the <a href="./../reference/drivers/scpi.html"><code>GenericScpi</code> driver</a> — the declarative <code>scpi: { properties, methods }</code> schema means most instruments are 20–40 lines of config. No code, no AI required.</li><li><strong>Drive the same flow from your own AI client</strong> by pointing Claude Desktop, Claude Code, or ChatGPT at Muxit&#39;s MCP server. The <code>probe_scpi_device</code>, <code>validate_connector_config</code>, and <code>write_connector_config</code> tools are exposed to MCP on all tiers, including Free — your AI client uses your own provider.</li></ul><p>See <a href="./ai.html#ai-features-by-tier">AI features by tier</a> for the full breakdown.</p></div><h2 id="two-ways-to-start" tabindex="-1">Two ways to start <a class="header-anchor" href="#two-ways-to-start" aria-label="Permalink to &quot;Two ways to start&quot;">​</a></h2><ul><li><strong>From the driver page.</strong> Open the Add Connector dialog, select <strong>GenericScpi</strong>, and click <strong>Set up with AI</strong>. The chat opens with a pre-filled prompt — edit the device details, hit enter, and follow along.</li><li><strong>From chat.</strong> Say something like <em>&quot;Add my Rigol DP832 at 192.168.1.50&quot;</em>, <em>&quot;Set up my Siglent SDG on COM5&quot;</em>, or <em>&quot;My Rigol DS1054Z is plugged in via USB&quot;</em>. Any mention of SCPI, a common instrument class, a well-known vendor, or USBTMC routes the turn through the SCPI-authoring flow.</li></ul><h2 id="what-happens-under-the-hood" tabindex="-1">What happens under the hood <a class="header-anchor" href="#what-happens-under-the-hood" aria-label="Permalink to &quot;What happens under the hood&quot;">​</a></h2><p>When the AI detects SCPI-setup intent it activates the <code>scpi-authoring</code> tool group and enables OpenRouter&#39;s web-search plugin for that turn. You&#39;ll see the assistant use these tools in order:</p><ol><li><code>list_serial_ports</code> / <code>list_usbtmc_devices</code> — if you didn&#39;t specify a port or a USB VID/PID, the assistant suggests candidates. <code>list_usbtmc_devices</code> enumerates every USB device libusb can see; the assistant picks the one whose manufacturer/product matches your instrument.</li><li><code>list_manuals</code> — scans <code>workspace/manuals/</code> for any user-supplied programming guide matching the device. If one is there, the assistant prefers it over web search (see <em>Upload a programming manual</em> below).</li><li><code>probe_scpi_device</code> — opens a one-shot TCP, serial, or USBTMC connection, sends <code>*IDN?</code>, and returns <code>{vendor, model, serial, firmware}</code>. Failures (timeout, refused, empty response, libusb permission) come back as <code>errors[]</code> so the assistant can tell you what went wrong instead of silently writing a broken config.</li><li><code>search_manual</code> / <code>read_manual_pages</code> <strong>or</strong> web search — the assistant confirms exact command syntax against your uploaded manual (citing page numbers) or, if no manual is provided, looks up the programming manual on the web and cites URLs inline.</li><li><code>validate_connector_config</code> — parses the drafted config through the same loader the server uses at startup. Catches syntax errors, disallowed globals, missing <code>driver</code> field, malformed <code>scpi</code> schema.</li><li><code>write_connector_config</code> — saves the file to <code>workspace/connectors/</code>.</li><li><code>reload_connectors</code> — tears down the connector registry and re-inits the catalog so the new device appears without a server restart.</li></ol><h2 id="usbtmc-direct-usb-instruments" tabindex="-1">USBTMC (direct USB) instruments <a class="header-anchor" href="#usbtmc-direct-usb-instruments" aria-label="Permalink to &quot;USBTMC (direct USB) instruments&quot;">​</a></h2><p>Many benchtop instruments (Rigol DS1000Z, Keysight DSO-X, Siglent SDG/SSA, …) plug in via USB-B and talk SCPI over the USB Test &amp; Measurement Class protocol. The AI handles these the same way as TCP / serial — tell it <em>&quot;my scope is plugged in via USB&quot;</em> and it will:</p><ol><li>Call <code>list_usbtmc_devices</code> to see every USB device libusb can enumerate.</li><li>Pick the entry whose <code>manufacturer</code>/<code>product</code> matches your instrument (or ask you to confirm if several look plausible).</li><li>Call <code>probe_scpi_device { transport: &quot;usbtmc&quot;, vendorId, productId }</code>.</li></ol><h3 id="platform-prerequisites" tabindex="-1">Platform prerequisites <a class="header-anchor" href="#platform-prerequisites" aria-label="Permalink to &quot;Platform prerequisites&quot;">​</a></h3><p>USBTMC needs libusb to be able to open the device. On Windows, install <a href="https://zadig.akeo.ie" target="_blank" rel="noreferrer">Zadig</a>, select your instrument from its list, and switch its driver to <strong>WinUSB</strong>. This replaces whatever NI-VISA or Keysight IO Libraries installed — if you want to use those tools again later, Zadig can switch back.</p><p>If the probe fails the assistant surfaces the relevant fix inline.</p><h2 id="upload-a-programming-manual" tabindex="-1">Upload a programming manual <a class="header-anchor" href="#upload-a-programming-manual" aria-label="Permalink to &quot;Upload a programming manual&quot;">​</a></h2><p>For niche instruments, older models, or anything with a vendor manual behind a login, dropping the PDF into <code>workspace/manuals/</code> gives the AI the exact source of truth — more reliable and much cheaper than web search.</p><ul><li><p><strong>Where:</strong> <code>workspace/manuals/</code> (create it if it doesn&#39;t exist — it&#39;s a standard workspace subdirectory). Per-device subfolders are optional but tidy for multi-instrument labs:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>workspace/manuals/</span></span>
<span class="line"><span>  rigol-ds1054z/DS1000Z_ProgrammingGuide.pdf</span></span>
<span class="line"><span>  siglent-sdg1032x/sdg_programming_manual.pdf</span></span>
<span class="line"><span>  notes.md</span></span></code></pre></div></li><li><p><strong>Formats:</strong> text-based PDFs, plain <code>.txt</code>, and <code>.md</code> work. Scanned/image-only PDFs can&#39;t be read — the assistant will warn you and fall back to web search.</p></li><li><p><strong>How the AI uses it:</strong> <code>search_manual</code> runs a substring query and returns excerpts with page numbers (e.g. &quot;p. 84: <code>:TIM:SCAL &lt;scale_val&gt;</code> — sets the horizontal time base scale&quot;). <code>read_manual_pages</code> pulls a specific page range when the excerpt looked promising and the assistant wants more context.</p></li><li><p><strong>Privacy:</strong> manuals stay on your machine. Only the extracted excerpts the assistant decides to quote are sent to the LLM, same as any other tool result.</p></li><li><p><strong>Cache:</strong> the first <code>search_manual</code> call extracts the PDF to a sibling <code>&lt;file&gt;.extracted.txt</code> so subsequent queries are instant. Safe to delete — it&#39;ll be rebuilt on the next call.</p></li></ul><h2 id="tips" tabindex="-1">Tips <a class="header-anchor" href="#tips" aria-label="Permalink to &quot;Tips&quot;">​</a></h2><ul><li><strong>Tell the AI what the device is.</strong> &quot;My new Siglent SDG1032X on 192.168.1.42&quot; is much more useful than &quot;add a new device&quot; — the assistant can jump straight to probing the right IP.</li><li><strong>Probe results that don&#39;t match what you expected</strong> mean either the IP/port is wrong, the instrument is in a non-SCPI mode, or the line terminator disagrees. The assistant will surface the raw IDN verbatim so you can see what the device actually reported.</li><li><strong>Check cited URLs.</strong> The LLM can misread a command table. If something doesn&#39;t look right, paste a relevant section of the manual back into chat and ask for a corrected draft.</li><li><strong>Smoke test afterwards.</strong> Ask <em>&quot;read the output voltage&quot;</em> or similar — the AI uses <code>read_property</code> against the new connector to prove the wiring works.</li></ul><h2 id="requires" tabindex="-1">Requires <a class="header-anchor" href="#requires" aria-label="Permalink to &quot;Requires&quot;">​</a></h2><ul><li>Maker tier license or higher (same gate as general AI chat).</li><li>Web search is backed by OpenRouter&#39;s built-in <code>web</code> plugin (Exa), billed at roughly $4 per 1000 results against the account running the Muxit proxy. For a single instrument setup this is typically a few cents.</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./connectors.html">Connector configs</a> — declarative schema, custom methods, polling behavior.</li><li><a href="./../reference/drivers/scpi.html">SCPI driver reference</a> — full <code>config.scpi.{properties, methods}</code> schema.</li><li><a href="./ai.html">AI chat</a> — intent classification, memory, tools.</li></ul>`,23)])])}const m=t(i,[["render",r]]);export{p as __pageData,m as default};

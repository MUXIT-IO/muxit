import{_ as t,o,c as a,ag as d}from"./chunks/framework.DkuLC1cV.js";const u=JSON.parse('{"title":"SerialProbe Driver","description":"AI-assisted serial protocol reverse-engineering — capture traffic, detect framing, correlate stimulus/response, and export a working connector config.","frontmatter":{"title":"SerialProbe Driver","description":"AI-assisted serial protocol reverse-engineering — capture traffic, detect framing, correlate stimulus/response, and export a working connector config."},"headers":[],"relativePath":"reference/drivers/serial-probe.md","filePath":"reference/drivers/serial-probe.md"}'),s={name:"reference/drivers/serial-probe.md"};function i(r,e,n,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[d(`<h1 id="serialprobe" tabindex="-1">SerialProbe <a class="header-anchor" href="#serialprobe" aria-label="Permalink to &quot;SerialProbe&quot;">​</a></h1><p><strong>Turn an unknown serial device into a working Muxit connector with AI assistance.</strong></p><p>SerialProbe is a Tier 1 JavaScript driver bundled with Muxit. It attaches to a serial port in raw-byte mode, runs local heuristics on the traffic, and exposes a compact action set that the AI follows step-by-step via its built-in <code>ai.instructions</code>. The AI never sees the raw byte stream — only summaries returned by local analysis — so a full reverse-engineering session stays cheap.</p><p>Typical targets: a thermometer with an undocumented ASCII line protocol, a JBC-style soldering station with a binary frame format, any USB-serial instrument without an obvious manual.</p><h2 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h2><ol><li>Raw bytes from the serial port are hex-encoded and fed into a ring buffer + byte histogram.</li><li>The driver auto-detects framing (<code>lines-lf</code>, <code>lines-crlf</code>, <code>stx-etx</code>, <code>fixed:N</code>, <code>ascii-printable</code>, <code>binary</code>).</li><li>The AI drives the session: picks a port, asks the user to trigger events, labels captures, diffs them, probes with <code>stimulus()</code>, and registers framing hypotheses.</li><li>Local CRC/XOR heuristics validate every new frame against the hypothesis and report a match ratio. When it stabilises, the AI calls <code>confirmHypothesis</code> and <code>exportConnector</code> to generate a ready-to-use connector <code>.js</code> file.</li></ol><h2 id="runtime-port-configuration" tabindex="-1">Runtime Port Configuration <a class="header-anchor" href="#runtime-port-configuration" aria-label="Permalink to &quot;Runtime Port Configuration&quot;">​</a></h2><p>The port, baud rate, and other link-layer settings are <strong>configured at runtime via actions</strong> — the connector config does <strong>not</strong> need to pre-create a serial transport. This lets the AI (or you from a dashboard) freely test different configurations: switch ports, try different baud rates, change parity, without editing files.</p><table tabindex="0"><thead><tr><th>Action</th><th>Args</th><th>Description</th></tr></thead><tbody><tr><td><code>listPorts</code></td><td>—</td><td>Enumerate serial ports visible to the host</td></tr><tr><td><code>openPort</code></td><td><code>{ portPath, baudRate?, dataBits?, parity?, stopBits?, delimiter?, timeoutMs? }</code></td><td>Close any active port and open a new one. Resets counters, histogram and <code>recentFrames</code>.</td></tr><tr><td><code>closePort</code></td><td>—</td><td>Close the active serial port</td></tr><tr><td><code>setBaudRate</code></td><td><code>{ baudRate }</code></td><td>Re-open the current port at a new baud rate</td></tr><tr><td><code>setPort</code></td><td><code>{ portPath, baudRate? }</code></td><td>Switch to a different serial port (optionally at a new baud)</td></tr><tr><td><code>scanBaud</code></td><td><code>{ portPath?, candidates?, durationMs?, reopenAt? }</code></td><td>Close the port, probe candidate baud rates, reopen at <code>&quot;original&quot;</code> (default), <code>&quot;recommended&quot;</code>, or a numeric baud</td></tr></tbody></table><p>Configs start disconnected — call <code>openPort</code> to connect. To pin a starting port, set <code>config.port</code> (and optionally <code>config.baudRate</code>) — see the example at the end of this page.</p><h2 id="properties" tabindex="-1">Properties <a class="header-anchor" href="#properties" aria-label="Permalink to &quot;Properties&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Access</th><th>Description</th></tr></thead><tbody><tr><td><code>connected</code></td><td>bool</td><td>R</td><td>Whether a serial port is open</td></tr><tr><td><code>portPath</code></td><td>string</td><td>R</td><td>Active serial port path (empty when closed)</td></tr><tr><td><code>baudRate</code></td><td>int</td><td>R</td><td>Active baud rate (0 when closed)</td></tr><tr><td><code>availablePorts</code></td><td>array</td><td>R</td><td>Serial ports discovered on the host</td></tr><tr><td><code>linkSettings</code></td><td>object</td><td>R</td><td>Active link-layer options (<code>dataBits</code>, <code>parity</code>, <code>stopBits</code>, <code>delimiter</code>, <code>timeoutMs</code>)</td></tr><tr><td><code>bytesReceived</code></td><td>int</td><td>R</td><td>Total bytes received on the current port (resets on <code>openPort</code>)</td></tr><tr><td><code>bytesSent</code></td><td>int</td><td>R</td><td>Total bytes sent on the current port (resets on <code>openPort</code>)</td></tr><tr><td><code>framing</code></td><td>string</td><td>R</td><td>Detected framing mode</td></tr><tr><td><code>alphabet</code></td><td>string</td><td>R</td><td><code>ascii-printable</code> / <code>binary</code> / <code>mixed</code></td></tr><tr><td><code>printableRatio</code></td><td>double</td><td>R</td><td>Fraction of printable bytes (0..1)</td></tr><tr><td><code>candidateDelimiters</code></td><td>object</td><td>R</td><td>Top recurring byte values that may be delimiters</td></tr><tr><td><code>recentFrames</code></td><td>object</td><td>R</td><td>Up to 30 most-recent frames, deduplicated with counts</td></tr><tr><td><code>captureLabels</code></td><td>object</td><td>R</td><td>Names of stored captures</td></tr><tr><td><code>currentQuestion</code></td><td>string</td><td>R</td><td>Open question for the operator (empty when none)</td></tr><tr><td><code>hypothesisStatus</code></td><td>string</td><td>R</td><td><code>none</code> / <code>testing</code> / <code>confirmed</code></td></tr><tr><td><code>hypothesisMatchRatio</code></td><td>double</td><td>R</td><td>Fraction of frames that match the active hypothesis</td></tr></tbody></table><h2 id="investigation-actions" tabindex="-1">Investigation Actions <a class="header-anchor" href="#investigation-actions" aria-label="Permalink to &quot;Investigation Actions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Action</th><th>Args</th><th>Description</th></tr></thead><tbody><tr><td><code>capture</code></td><td><code>{ label, durationMs? }</code></td><td>Start a named capture window (default 5000 ms)</td></tr><tr><td><code>stopCapture</code></td><td>—</td><td>Stop the active capture immediately</td></tr><tr><td><code>listCaptures</code></td><td>—</td><td>List captures with byte/frame counts</td></tr><tr><td><code>clearCapture</code></td><td><code>{ label }</code></td><td>Delete a stored capture</td></tr><tr><td><code>compareCaptures</code></td><td><code>{ labelA, labelB }</code></td><td>Diff two captures — returns frames unique to each</td></tr><tr><td><code>analyzeFrames</code></td><td><code>{ label }</code></td><td>Local heuristics: length histogram + CRC-16/Modbus + XOR-trailing checks</td></tr><tr><td><code>stimulus</code></td><td><code>{ send?, sendHex?, waitMs, label? }</code></td><td>Send bytes and capture the response window</td></tr><tr><td><code>askUser</code></td><td><code>{ question, choices? }</code></td><td>Register a question for the operator</td></tr><tr><td><code>answerUser</code></td><td><code>{ answer }</code></td><td>Provide an answer to the open question</td></tr><tr><td><code>hypothesize</code></td><td><code>{ framing, notes? }</code></td><td>Register a parser hypothesis to validate against incoming frames</td></tr><tr><td><code>confirmHypothesis</code></td><td>—</td><td>Mark current hypothesis as confirmed</td></tr><tr><td><code>exportConnector</code></td><td><code>{ name, driverName? }</code></td><td>Generate a connector <code>.js</code> source from the confirmed hypothesis. Requires an open port.</td></tr><tr><td><code>send</code></td><td><code>{ text }</code></td><td>Send raw text</td></tr><tr><td><code>sendHex</code></td><td><code>{ hex }</code></td><td>Send a hex string (spaces allowed: <code>AA 55 01</code>)</td></tr></tbody></table><p>Supported framing modes: <code>lines-lf</code>, <code>lines-crlf</code>, <code>stx-etx</code>, <code>fixed:N</code> (e.g. <code>fixed:16</code>), <code>modbus-rtu</code>, <code>ascii-printable</code>, <code>binary</code>.</p><p>The tables above are summary rows. Each action and property also ships extended docs via the descriptor <code>details</code> layer — visible in the <strong>Drivers → SerialProbe</strong> page (click <strong>Show details</strong> on any row), in the script editor hover popup, and in the on-demand <code>get_connector_schema</code> / <code>get_driver_schema</code> response. The five subtle actions — <code>scanBaud</code>, <code>hypothesize</code>, <code>analyzeFrames</code>, <code>stimulus</code>, <code>exportConnector</code> — are reproduced below.</p><h2 id="action-reference" tabindex="-1">Action Reference <a class="header-anchor" href="#action-reference" aria-label="Permalink to &quot;Action Reference&quot;">​</a></h2><p>The detail copy below is the source of truth shared by the driver&#39;s <code>details</code> descriptor field, the driver doc viewer, the Monaco IntelliSense hover, and the AI&#39;s full-schema response.</p><h3 id="scanbaud" tabindex="-1"><code>scanBaud</code> <a class="header-anchor" href="#scanbaud" aria-label="Permalink to &quot;\`scanBaud\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>scanBaud({ portPath?, candidates?, durationMs?, reopenAt? })</span></span></code></pre></div><p>Detaches the current transport, opens <code>portPath</code> at each baud in <code>candidates</code> for <code>durationMs</code> (default 1500 ms), then reopens at the chosen baud. Results are ranked by <code>printableRatio</code> descending, with total <code>bytes</code> as the tiebreaker.</p><p><strong>Defaults:</strong></p><ul><li><code>portPath</code> — the currently open port. Throws if no port is open and none is supplied.</li><li><code>candidates</code> — <code>[9600, 19200, 38400, 57600, 115200]</code>.</li><li><code>durationMs</code> — <code>1500</code>.</li><li><code>reopenAt</code> — <code>&quot;original&quot;</code> (reopen at whatever baud was active before scanning).</li></ul><p><strong><code>reopenAt</code> accepts:</strong></p><ul><li><code>&quot;original&quot;</code> — the baud that was active when <code>scanBaud</code> was called.</li><li><code>&quot;recommended&quot;</code> — the top-ranked candidate (highest printable ratio).</li><li>any number — an explicit baud to reopen at.</li></ul><p>If every candidate errors, <code>recommended</code> falls back to <code>originalBaud</code>. If the final reopen fails, the port is left closed and a warning is logged — check <code>connected</code> before your next call.</p><h3 id="hypothesize" tabindex="-1"><code>hypothesize</code> <a class="header-anchor" href="#hypothesize" aria-label="Permalink to &quot;\`hypothesize\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>hypothesize({ framing, notes? })</span></span></code></pre></div><p>Registers the hypothesis, resets match counters, and sets <code>hypothesisStatus</code> to <code>&quot;testing&quot;</code>. Every subsequent frame updates <code>hypothesisMatchRatio</code>. Calling <code>hypothesize</code> again replaces the current hypothesis (counters reset).</p><p><strong><code>framing</code> has two categories:</strong></p><ul><li><strong>Extractable modes</strong> — change how the framer slices raw bytes into frames: <code>lines-lf</code>, <code>lines-crlf</code>, <code>stx-etx</code>, <code>fixed:N</code> (e.g. <code>fixed:8</code>), <code>ascii-printable</code>, <code>none</code>. Setting one of these switches <code>_framingMode</code> for live extraction.</li><li><strong>Annotations</strong> — metadata only, do not change extraction: <code>modbus-rtu</code> (also triggers CRC-16 verification when computing the match ratio), and free-form strings for your own notes.</li></ul><p>Use <code>notes</code> for anything the annotation alone doesn&#39;t capture (slave ID, expected payload, manual reference). Call <code>confirmHypothesis</code> once <code>hypothesisMatchRatio</code> is high enough over a meaningful number of frames.</p><h3 id="analyzeframes" tabindex="-1"><code>analyzeFrames</code> <a class="header-anchor" href="#analyzeframes" aria-label="Permalink to &quot;\`analyzeFrames\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>analyzeFrames({ label })</span></span></code></pre></div><p>Returns a summary object with <code>frameCount</code>, <code>uniqueFrames</code>, <code>lengthHistogram</code>, <code>checksumCandidates</code>, and up to 5 <code>sampleFrames</code>.</p><p><strong>Checksum heuristics (non-configurable):</strong></p><ul><li><code>crc16Modbus</code> — assumes the last 2 bytes are CRC-16/Modbus in little-endian, over the preceding bytes.</li><li><code>xorTrailing</code> — assumes the last byte is the XOR of the preceding bytes.</li></ul><p>A ratio near 1.0 means the assumption holds on almost every frame — strong signal that framing and checksum are right. Ratios near 0 rule it out. Frames shorter than 4 bytes are skipped (the checksum fields would be returned as <code>null</code> if no frames qualified).</p><p>There is no way to configure an alternate CRC polynomial or position from this action — if your protocol uses a different checksum, treat both candidates as null and validate manually.</p><h3 id="stimulus" tabindex="-1"><code>stimulus</code> <a class="header-anchor" href="#stimulus" aria-label="Permalink to &quot;\`stimulus\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>stimulus({ send?, sendHex?, waitMs, label? })</span></span></code></pre></div><p>Opens a capture window for <code>waitMs</code>, sends the stimulus, then waits slightly longer (<code>waitMs + 60</code>) before returning the captured response.</p><p><strong>Pass exactly one of <code>send</code> or <code>sendHex</code>:</strong></p><ul><li><code>send: &quot;*IDN?\\n&quot;</code> — text with JS escape sequences; written as-is.</li><li><code>sendHex: &quot;01 03 00 00 00 0A&quot;</code> — hex string, whitespace-tolerant; each pair becomes one byte.</li></ul><p><code>label</code> defaults to <code>stim-&lt;timestamp&gt;</code> when omitted; the capture is stored under that name and is visible via <code>listCaptures</code>.</p><p><strong>Return shape (truncated for token discipline):</strong></p><ul><li><code>frames</code> — first 8 parsed frames only.</li><li><code>raw</code> — concatenated hex of every received chunk, clipped to 256 characters (128 bytes).</li><li><code>latencyMs</code> — time from capture start to first frame, or <code>null</code> if no frames arrived.</li></ul><p>Call <code>clearCapture(label)</code> if you need to reuse a label or reclaim memory.</p><h3 id="exportconnector" tabindex="-1"><code>exportConnector</code> <a class="header-anchor" href="#exportconnector" aria-label="Permalink to &quot;\`exportConnector\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>exportConnector({ name, driverName? })</span></span></code></pre></div><p>Returns a string of JavaScript suitable for saving to <code>workspace/connectors/&lt;name&gt;.js</code>. This action does <strong>not</strong> write the file — it only returns the source. Use the file tools or the UI to save it.</p><p><strong>The generated config bakes in current state:</strong></p><ul><li><code>portPath</code> and <code>baudRate</code> from the currently open port — <code>exportConnector</code> throws if no port is open, so the emitted config is never under-specified.</li><li><code>framing</code> from the confirmed hypothesis, or the driver&#39;s best auto-detection if no hypothesis was registered.</li><li><code>notes</code> from the hypothesis (or <code>&quot;(no notes)&quot;</code>).</li></ul><p><strong>The starter code is deliberately minimal:</strong></p><ul><li><code>driver</code> defaults to <code>&quot;SerialMonitor&quot;</code>; pass <code>driverName</code> to target another serial-speaking driver.</li><li>Only a single <code>sendLine</code> method is emitted. Expect to extend the connector by hand with device-specific properties and actions before using it in production.</li></ul><h2 id="streams" tabindex="-1">Streams <a class="header-anchor" href="#streams" aria-label="Permalink to &quot;Streams&quot;">​</a></h2><p>Patch any of these into a <strong>Terminal</strong> dashboard widget to watch traffic live. Each chunk is emitted as one line.</p><table tabindex="0"><thead><tr><th>Stream</th><th>Description</th></tr></thead><tbody><tr><td><code>traffic</code></td><td>Unified RX/TX log, auto hex-or-ASCII per chunk, prefixed with <code>← </code> (received) or <code>→ </code> (sent). Best default for a single terminal.</td></tr><tr><td><code>rxHex</code></td><td>Received bytes as spaced uppercase hex per chunk (e.g. <code>4B 65 69 74 68 6C 65 79</code>)</td></tr><tr><td><code>rxText</code></td><td>Received bytes as printable ASCII, with C-style escapes for control bytes (<code>\\r</code>, <code>\\n</code>, <code>\\t</code>, <code>\\xNN</code>)</td></tr><tr><td><code>txHex</code></td><td>Sent bytes as spaced uppercase hex</td></tr><tr><td><code>txText</code></td><td>Sent bytes as printable ASCII (same escape scheme as <code>rxText</code>)</td></tr><tr><td><code>frames</code></td><td>Parsed frames as they are emitted by the framer</td></tr><tr><td><code>events</code></td><td>Lifecycle events: <code>portOpened:&lt;path&gt;@&lt;baud&gt;</code>, <code>portClosed:&lt;path&gt;</code>, <code>portChanged:&lt;path&gt;@&lt;baud&gt;</code>, <code>captureStarted:&lt;label&gt;</code>, <code>captureStopped:&lt;label&gt;</code>, <code>questionOpened</code>, <code>questionAnswered</code></td></tr></tbody></table><h2 id="ai-workflow-built-in" tabindex="-1">AI Workflow (built-in) <a class="header-anchor" href="#ai-workflow-built-in" aria-label="Permalink to &quot;AI Workflow (built-in)&quot;">​</a></h2><p>The driver&#39;s <code>meta.ai.instructions</code> walks the AI through:</p><ol start="0"><li><strong>Link layer setup</strong> — if <code>connected</code> is false, call <code>listPorts</code> and <code>openPort</code> (ask the operator via <code>askUser</code> when ambiguous). Switch ports/baud any time via <code>setPort</code>, <code>setBaudRate</code>, or another <code>openPort</code>.</li><li><strong>Identify</strong> — ask for brand/model/connector/manual via <code>askUser</code>. If a known protocol is named, skip ahead.</li><li><strong>Baud probe</strong> — <code>scanBaud({ candidates: [...], reopenAt: &quot;recommended&quot; })</code> if the baud rate is uncertain.</li><li><strong>Passive observe</strong> — <code>capture({ label: &quot;idle&quot;, durationMs: 8000 })</code> and read <code>framing</code> / <code>alphabet</code> / <code>candidateDelimiters</code>.</li><li><strong>Stimulus pairs</strong> — use <code>askUser</code> to have the operator trigger events, then <code>capture</code> and <code>compareCaptures</code> to isolate relevant frames.</li><li><strong>Probe</strong> — try <code>stimulus({ send: &quot;*IDN?\\n&quot;, waitMs: 300 })</code> or <code>stimulus({ sendHex: &quot;01 03 00 00 00 0A C5 CD&quot;, waitMs: 300 })</code> for Modbus.</li><li><strong>Hypothesise</strong> — <code>hypothesize({ framing })</code>; the driver validates every new frame and exposes <code>hypothesisMatchRatio</code>.</li><li><strong>Confirm &amp; export</strong> — when ratio ≥ 0.95 over ≥ 50 frames, call <code>confirmHypothesis</code> and <code>exportConnector({ name })</code>. The returned <code>{ suggestedPath, code }</code> can be written to <code>workspace/connectors/&lt;name&gt;.js</code> via your file tools. The generated config bakes in the <code>portPath</code> and <code>baudRate</code> that were active when the hypothesis was confirmed.</li></ol><p>The AI is instructed <strong>never</strong> to dump raw traffic into chat — <code>compareCaptures</code> and <code>analyzeFrames</code> return already-summarised JSON, and <code>recentFrames</code> is capped at 30 entries with duplicate counts. This keeps a full reverse-engineering session well under a few tens of thousands of input tokens.</p><h2 id="example-connector" tabindex="-1">Example Connector <a class="header-anchor" href="#example-connector" aria-label="Permalink to &quot;Example Connector&quot;">​</a></h2><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// workspace/connectors/serial-probe.js</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  driver: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SerialProbe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  poll: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;portPath&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;baudRate&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;bytesReceived&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;framing&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;currentQuestion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;hypothesisStatus&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;hypothesisMatchRatio&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Open the connector, then in the AI chat say: <strong>&quot;Help me figure out this device&#39;s protocol.&quot;</strong> The AI picks up the <code>ai.instructions</code>, calls <code>listPorts</code> / <code>openPort</code>, and drives the session.</p><p>If you prefer to pin a starting port, add <code>port</code> and <code>baudRate</code> to <code>config</code>:</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  driver: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SerialProbe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  config: { port: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;COM3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, baudRate: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><h2 id="safety-notes" tabindex="-1">Safety Notes <a class="header-anchor" href="#safety-notes" aria-label="Permalink to &quot;Safety Notes&quot;">​</a></h2><ul><li>SerialProbe writes to the device whenever <code>send</code>, <code>sendHex</code>, or <code>stimulus</code> is called. Ask the operator before probing unknown hardware — some protocols interpret arbitrary bytes as firmware-erase commands.</li><li><code>openPort</code> / <code>setPort</code> / <code>setBaudRate</code> / <code>scanBaud</code> all close the active transport first. If another connector is sharing the port, it will lose its connection.</li><li>The generated connector from <code>exportConnector</code> uses <code>SerialMonitor</code> as its driver by default; override with <code>driverName</code> if you want something else.</li></ul>`,69)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
